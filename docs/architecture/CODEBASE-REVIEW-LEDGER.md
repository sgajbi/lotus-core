# Codebase Review Ledger

This ledger tracks systematic review work across `lotus-core`.

Companion process document:

- [CODEBASE-REVIEW-PLAYBOOK.md](./CODEBASE-REVIEW-PLAYBOOK.md)

## Review entries

| Review ID | Date | Scope / Pattern | Status | Findings | Actions Taken | Follow-up | Evidence / Sign-off |
|---|---|---|---|---|---|---|---|
| CR-001 | 2026-03-10 | Async orchestration, heavy-gate runtime, quiescence barriers | Hardened | Post-RFC-081 heavy validation exposed real orchestration defects: duplicate position-timeseries fan-out, incorrect same-day aggregation re-arm semantics, quiescence false positives, and stale runtime assumptions in test-stack orchestration. | Hardened timeseries orchestration, added quiescence-aware cleanup with quiet-window support, removed false-positive blockers from quiescence, tightened E2E setup to wait for business-complete states, and refreshed failure-recovery evidence. | Keep watching CI/main heavy gates. If hosted CI shows environment-specific regressions, treat them as extensions of this scope until stabilized. | Commits `96a17a9`, `a5e4288`; local validation: `pytest tests/e2e -q -x -s` => `56 passed`; `python scripts/failure_recovery_gate.py --build --enforce` passed; RFC 081 reopened for hardening. |
| CR-002 | 2026-03-10 | Portfolio aggregation eligibility query shape | Hardened | `find_and_claim_eligible_jobs()` in portfolio aggregation/timeseries repositories allowed a correlated subquery to degrade into an accidental `FROM daily_position_snapshots, portfolio_aggregation_jobs` pattern. This prevented first eligible portfolio-day jobs from being claimed on a fresh workflow. | Rewrote the prior-day and completeness gates to use explicit `select(1)` correlation and correlated snapshot subqueries. Added unit query-shape tests and DB-backed integration coverage for first-day, mixed-epoch, and prior-day eligibility behavior. | Converge duplicate repository implementations so the same SQL is not maintained in two places. | Commits within `96a17a9`; tests: `tests/unit/services/timeseries_generator_service/.../test_unit_timeseries_repo.py`, `tests/unit/services/portfolio_aggregation_service/repositories/test_timeseries_repository.py`, `tests/integration/services/timeseries_generator_service/test_timeseries_repository_integration.py`; `tests/e2e/test_5_day_workflow.py` => `6 passed`. |
| CR-003 | 2026-03-10 | Cashflow replay correctness and epoch-versioned persistence | Hardened | Back-dated replay and stale-event flows could fail because cashflows were not safely versioned across epochs. Lower-level contract coverage was insufficient for replay-oriented persistence behavior. | Added epoch-versioned cashflow persistence support, updated cashflow consumer/runtime behavior, added integration tests for query-side cashflow repository behavior, and added unit tests for the consumer/manager paths. | Review remaining replay-sensitive repositories for the same epoch-versioning assumptions. | Commit `8a32e00`; migration `d1e2f3a4b5c6_feat_allow_epoch_versioned_cashflows.py`; query integration and cashflow consumer unit tests. |
| CR-004 | 2026-03-10 | Valuation scheduling for current-day positions | Hardened | Current-day market prices were not always creating the required valuation work deterministically. This left heavy workflows dependent on indirect or lagging triggers. | Updated valuation repositories and price-event trigger logic, then later removed the dead duplicate trigger surface from `position_valuation_calculator` once ownership was finalized in CR-091. | Keep valuation trigger ownership in `valuation_orchestrator_service`; do not reintroduce calculator-local scheduling paths. | Commit `f262393`; enduring evidence: `tests/unit/libs/portfolio-common/test_valuation_job_repository.py`; live ownership finalized in [CR-091-POSITION-VALUATION-WORKER-OWNERSHIP-CLEANUP.md](./CR-091-POSITION-VALUATION-WORKER-OWNERSHIP-CLEANUP.md). |
| CR-005 | 2026-03-10 | Test-stack environment, ports, and module isolation | Hardened | The previous Docker-backed test harness mixed fixed-port assumptions, shared compose project names, and weak cleanup semantics. This reduced confidence and created false negatives. | Introduced dynamic runtime allocation, project-scoped compose usage, Kafka readiness, quiescence-based cleanup, and corrected E2E setup/wait behavior. | Consider splitting full-stack E2E from smaller Docker-backed integration profiles to reduce runtime cost further. | Tests under `tests/test_support/`; `tests/conftest.py`; local `e2e-all`, `integration-all`, and failure-recovery gate all green. |
| CR-006 | 2026-03-10 | Duplicate repository and service logic across split services | Hardened | Review confirmed the real structural duplication was in repository pairs, not in consumer managers. `ValuationRepository` was duplicated in full between `valuation_orchestrator_service` and `position_valuation_calculator`. `TimeseriesRepository` was near-duplicated between `portfolio_aggregation_service` and `timeseries_generator_service`, with only a worker-local `get_position_timeseries(...)` difference. | Extracted shared valuation query/claim logic into `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py` and shared timeseries query/claim logic into `src/libs/portfolio-common/portfolio_common/timeseries_repository_base.py`. Reduced all four service-local repository files to thin wrappers that preserve import paths, service-local metric hooks, and the worker-only `get_position_timeseries(...)` helper. Validated both converged paths with unit and integration repository suites. | Watch for future duplication in runtime scaffolding, but repository-body convergence for the reviewed valuation/timeseries pairs is complete. Do not force consumer-manager unification unless later review shows reusable scaffolding with a real payoff. | Evidence: [CR-006-DUPLICATE-REPOSITORY-REVIEW.md](./CR-006-DUPLICATE-REPOSITORY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `src/libs/portfolio-common/portfolio_common/timeseries_repository_base.py`; service-local repository wrappers; valuation and timeseries repository unit/integration tests. |
| CR-007 | 2026-03-10 | Failure-recovery gate semantics | Hardened | Review confirmed the gate is evaluating bounded recovery, not strict full drain. A run can pass while `drain_seconds_to_baseline = timeout` if backlog age and DLQ/replay pressure recover within allowed bounds. That behavior is coherent, but it was previously ambiguous in the gate output. | Added explicit recovery classification to the gate result: `FULLY_DRAINED`, `BOUNDED_RECOVERY`, `FAILED_RECOVERY`. Added direct unit tests for the gate decision matrix and updated the review note accordingly. | If policy tuning is desired later, revisit thresholds separately. The semantic ambiguity is now closed. | Evidence: [CR-007-FAILURE-RECOVERY-REVIEW.md](./CR-007-FAILURE-RECOVERY-REVIEW.md); `tests/unit/scripts/test_failure_recovery_gate.py` => `12 passed`; `ruff check` passed on gate + tests; report `output/task-runs/20260310T050658Z-failure-recovery-gate.md`. |
| CR-008 | 2026-03-10 | Control-plane job health query shape | Hardened | The issue in `query_service` support/operations endpoints was query fan-out, not missing worker indexes. One overview/SLO request executed many separate aggregates over the same valuation and aggregation job tables. Existing indexed columns (`portfolio_id`, `status`, business-date columns) are broadly adequate for the current control-plane predicates. | Consolidated valuation and aggregation job health into combined summary queries and updated `OperationsService` to consume those summaries. Added unit coverage for the summary-query repository methods and the updated service interface. | If hosted runtime metrics later show true DB pressure on worker claim paths, review those separately under a scheduler/backlog performance batch. Do not add speculative indexes before evidence exists. | Evidence: [CR-008-CONTROL-PLANE-JOB-HEALTH-QUERY-REVIEW.md](./CR-008-CONTROL-PLANE-JOB-HEALTH-QUERY-REVIEW.md); `src/services/query_service/app/repositories/operations_repository.py`; `src/services/query_service/app/services/operations_service.py`; unit tests on repository + service summary behavior. |
| CR-009 | 2026-03-10 | Query vs query-control-plane source ownership | Hardened | RFC 81 split the runtime/API surface correctly, but control-plane source ownership was still blurred: `query_control_plane_service` imported routers and enterprise-readiness logic directly from `query_service`, and control-plane tests were split across `query_service` paths. | Moved control-plane router modules into `src/services/query_control_plane_service/app/routers/`, moved router dependency tests into `tests/integration/services/query_control_plane_service/`, moved router unit tests into `tests/unit/services/query_control_plane_service/routers/`, added service-local enterprise-readiness middleware for the control-plane app, updated `query_control_plane_service/app/main.py` to import only local app modules for that API surface, and removed the stale control-plane router copies from `src/services/query_service/app/routers/`. | Shared DTO/service/query logic may still remain in `query_service` where that is the canonical business/query layer. If later review shows a reusable shared query package is warranted, handle that as a separate convergence batch rather than re-blurring control-plane app ownership. | Evidence: [CR-009-QUERY-CONTROL-PLANE-OWNERSHIP-REVIEW.md](./CR-009-QUERY-CONTROL-PLANE-OWNERSHIP-REVIEW.md); local control-plane router package; moved control-plane unit/integration router tests; `tests/integration/services/query_control_plane_service/*` app-contract + dependency suites; `tests/unit/services/query_control_plane_service/routers/*`. |
| CR-010 | 2026-03-10 | Worker claim-path scheduler and backlog semantics | Hardened | Worker claim paths already use sound locking/claim semantics (`FOR UPDATE SKIP LOCKED` with atomic claim updates) for valuation, aggregation, and reprocessing jobs. The main residual structural risk is duplicated claim repository code, already tracked in `CR-006`. One concrete inconsistency did exist: aggregation stale-job resets did not refresh `updated_at` while valuation stale-job resets did. | Reviewed worker claim implementations and stale-reset paths. Hardened both aggregation repository copies so stale resets now set `updated_at = now()` in addition to resetting status. Added unit coverage for the reset statement shape. | Converge duplicated valuation and aggregation repositories under `CR-006` so future claim-path fixes land once. Consider consistent reset metrics across worker types later. | Evidence: [CR-010-WORKER-CLAIM-PATH-REVIEW.md](./CR-010-WORKER-CLAIM-PATH-REVIEW.md); `src/services/timeseries_generator_service/app/repositories/timeseries_repository.py`; `src/services/portfolio_aggregation_service/app/repositories/timeseries_repository.py`; unit repository tests for stale reset. |
| CR-013 | 2026-03-10 | Financial calculator engine scope and ownership | Hardened | 
inancial-calculator-engine is not dead, but only its core/ and logic/ layers are truly shared. The ngine/ layer (TransactionProcessor + recalculation metrics) was only used by cost_calculator_service, so it was service-owned orchestration disguised as shared engine code. | Moved TransactionProcessor and recalculation monitoring into cost_calculator_service, moved the direct transaction-processor unit test into the service test area, updated manifest/doc references, and removed the dead 
inancial-calculator-engine/src/engine/ package. | If the remaining shared library is renamed later, do it as a dedicated vocabulary/package-governance slice. The current ownership fix is sufficient without broader package churn. | Evidence: [CR-013-FINANCIAL-CALCULATOR-ENGINE-SCOPE-REVIEW.md](./CR-013-FINANCIAL-CALCULATOR-ENGINE-SCOPE-REVIEW.md); src/services/calculators/cost_calculator_service/app/transaction_processor.py; src/services/calculators/cost_calculator_service/app/monitoring.py; moved transaction-processor unit tests. |
| CR-014 | 2026-03-10 | Startup/runtime contracts for dependency-critical services | Hardened | The hot-path startup review found that `ingestion_service` tolerated Kafka producer initialization failure by logging a fatal error and continuing startup with `app_state[\"kafka_producer\"] = None`. That let a write-ingress service advertise readiness while unable to fulfill its core publish contract. | Hardened `ingestion_service` to fail fast on Kafka producer initialization failure and added lifecycle-level integration coverage asserting startup raises when Kafka initialization fails. | Continue the same review pattern across other dependency-critical services; only treat dependency failure as degraded if the service contract is truly still valid. | Evidence: [CR-014-STARTUP-RUNTIME-CONTRACT-REVIEW.md](./CR-014-STARTUP-RUNTIME-CONTRACT-REVIEW.md); `src/services/ingestion_service/app/main.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`. |
| CR-015 | 2026-03-10 | Web worker startup/runtime contract convergence | Hardened | Web-backed worker services were all implementing the same startup/metrics/runtime/shutdown pattern in their own `main.py` files. The duplication had already drifted into inconsistent fatal-error semantics: some workers re-raised `ConsumerManager` failures, others logged critical errors and exited without re-raising. | Extracted the shared runtime contract into `portfolio_common.worker_runtime.run_instrumented_worker_service(...)`, migrated the web-backed worker services onto it, and standardized fatal worker runtime failures to log and re-raise consistently. Added direct unit tests for the shared helper. | Review remaining non-web worker mains separately. Do not force `persistence_service` or pure HTTP services into this helper unless their contracts genuinely converge. | Evidence: [CR-015-WEB-WORKER-STARTUP-CONTRACT-REVIEW.md](./CR-015-WEB-WORKER-STARTUP-CONTRACT-REVIEW.md); `src/libs/portfolio-common/portfolio_common/worker_runtime.py`; updated worker `main.py` files; `tests/unit/libs/portfolio-common/test_worker_runtime.py`. |
| CR-016 | 2026-03-10 | Pure HTTP app bootstrap duplication | Hardened | The pure HTTP/control-plane apps (`query_service`, `query_control_plane_service`, `event_replay_service`, `financial_reconciliation_service`, `ingestion_service`) repeated a large shared bootstrap pattern: Prometheus exposure, OpenAPI metrics override/enrichment, correlation/request/trace middleware, HTTP observability middleware, exception normalization, and health-router registration. | Extracted the shared layer into `portfolio_common.http_app_bootstrap` and migrated the five HTTP apps onto it while keeping service-local ownership for enterprise middleware, lifespan/startup behavior, dependency-critical startup policy, health dependency sets, and router selection. Validated the migrated apps with their existing app-contract suites. | If future convergence is needed, extend only the shared middleware/OpenAPI/router-registration layer. Do not turn the helper into a generic app factory unless startup contracts truly converge. | Evidence: [CR-016-PURE-HTTP-BOOTSTRAP-REVIEW.md](./CR-016-PURE-HTTP-BOOTSTRAP-REVIEW.md); `src/libs/portfolio-common/portfolio_common/http_app_bootstrap.py`; app mains for query, query-control-plane, event-replay, financial-reconciliation, and ingestion; `pytest` app-contract suites for query, query-control-plane, event-replay, and ingestion => `25 passed`. |
| CR-017 | 2026-03-10 | Financial engine dead config/runtime surface | Hardened | The engine still contained an orphaned `core/config/settings.py` layer describing app/runtime settings for a standalone API that no longer existed in `lotus-core`. The module had no live production/test/script imports, but it kept `pydantic-settings` and `python-dotenv` in the dependency graph. | Removed the dead `core/config/settings.py` module and dropped the now-unused engine-package dependencies. | `core/enums/cost_method.py` was reviewed in CR-018. Remaining live engine ownership was finalized later in CR-020. | Evidence: [CR-017-FINANCIAL-ENGINE-DEAD-CONFIG-REVIEW.md](./CR-017-FINANCIAL-ENGINE-DEAD-CONFIG-REVIEW.md). |
| CR-018 | 2026-03-10 | Cost-basis vocabulary ownership and alias control | Hardened | `cost_basis_method` was still live business vocabulary, but the only explicit enum lived in `cost-calculator-service engine package` and had become dead after config cleanup. The live repo surface also mixed canonical `AVCO` with stale alias `AVERAGE_COST`. | Promoted cost-basis vocabulary into `portfolio-common`, updated live portfolio/event/consumer paths to use it, normalized persistence writes around the canonical enum, added regression tests for canonical values and explicit rejection of stale alias values, and removed the dead engine-local enum. | Do not add compatibility aliases unless the project later has a real external backward-compatibility obligation. | Evidence: [CR-018-COST-BASIS-VOCABULARY-REVIEW.md](./CR-018-COST-BASIS-VOCABULARY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/cost_basis.py`; persistence + sell-linkage + cost-consumer updates; new unit/integration tests. |
| CR-019 | 2026-03-10 | Financial engine model surface ownership | Hardened | The remaining engine still carried dead standalone request/response models and vague top-level `core/` and `logic/` source trees. Only `Transaction` and `ErroredTransaction` were still live models. | Repackaged the live engine into a meaningful `domain/` and `processing/` structure, kept only the live models, and deleted dead `request.py` / `response.py` plus the obsolete generic source files. | Final service ownership was completed in CR-020 once production usage confirmed the engine was no longer truly shared. | Evidence: [CR-019-FINANCIAL-ENGINE-MODEL-SURFACE-REVIEW.md](./CR-019-FINANCIAL-ENGINE-MODEL-SURFACE-REVIEW.md). |
| CR-020 | 2026-03-10 | Cost engine ownership finalization | Hardened | After CR-019, the remaining production imports of the cost engine were all inside `cost_calculator_service`. The package was no longer genuinely shared, so keeping it under `src/libs` created a false architectural boundary. | Folded the engine into `src/services/calculators/cost_calculator_service/app/cost_engine`, moved engine unit tests under service ownership, removed the obsolete `src/services/calculators/cost_calculator_service/app/cost_engine/` package, and updated current-state docs/runtime tooling to match. | Update historical RFC references opportunistically when those RFCs are touched again. Current-state code and living docs now reflect the correct ownership boundary. | Evidence: [CR-020-COST-ENGINE-OWNERSHIP-FINALIZATION.md](./CR-020-COST-ENGINE-OWNERSHIP-FINALIZATION.md); `src/services/calculators/cost_calculator_service/app/cost_engine/*`; `tests/unit/services/calculators/cost_calculator_service/engine/*`; `src/services/calculators/cost_calculator_service/Dockerfile`; `scripts/test_manifest.py`. |
| CR-021 | 2026-03-10 | Docker image preflight and local runtime drift | Hardened | Docker-backed heavy tests still assumed required base images were already present locally, which caused late compose failures when `cp-zookeeper` / `cp-kafka` images were missing. `docker-compose.yml` also still carried a stale `financial-calculator-engine` volume mount after CR-020 removed that package. | Added Docker image preflight/pull behavior to `tests/test_support/docker_stack.py`, added unit coverage for missing-image and failed-pull cases, and removed the stale `financial-calculator-engine` volume mount from `cost_calculator_service` in `docker-compose.yml`. | If image sourcing becomes a recurring problem, define explicit mirror/cache warm-up policy in dev/CI bootstrap. The concrete local runtime drift reviewed here is now closed. | Evidence: [CR-021-DOCKER-IMAGE-PREFLIGHT-REVIEW.md](./CR-021-DOCKER-IMAGE-PREFLIGHT-REVIEW.md); `tests/test_support/docker_stack.py`; `tests/unit/test_support/test_docker_stack.py`; `docker-compose.yml`. |
| CR-022 | 2026-03-10 | Unit-test async cleanup warning debt | Hardened | Branch-scoped unit premerge runs still emitted `aclose()`-not-awaited warnings from test doubles around `get_async_db_session`, plus one Kafka payload-format test that exercised DB recording even though DB persistence was not the behavior under test. | Replaced raw async-generator monkeypatches with explicit single-yield async iterators in the affected tests, mocked `_record_consumer_dlq_event` in the Kafka payload-format test, and re-ran the warning-producing tests with warnings enabled to prove the warnings were gone. | Keep future async cleanup warnings classified as test-harness debt first unless a targeted reproduction proves a production resource-lifecycle bug. | Evidence: [CR-022-UNIT-TEST-ASYNC-CLEANUP-WARNING-REVIEW.md](./CR-022-UNIT-TEST-ASYNC-CLEANUP-WARNING-REVIEW.md); warning-producing unit tests rerun clean with `-W default`. |
| CR-023 | 2026-03-10 | Unit Kafka producer noise and stale legacy test leftovers | Hardened | The unit manifest still printed noisy librdkafka connection failures because the unit tier allowed accidental construction of the real `confluent_kafka.Producer`. The repo also still had three stale legacy analytics test directories that only contained `__pycache__` leftovers. | Added a unit-tier autouse fixture that monkeypatches `portfolio_common.kafka_utils.Producer` to a `MagicMock` and resets the producer singleton before/after each unit test, added explicit producer shutdown support in `portfolio_common.kafka_utils`, re-ran the full unit manifest to prove the suite is quiet, and removed the stale legacy test directories. | If future tests need a real Kafka client, move them to integration coverage instead of weakening the unit tier. | Evidence: [CR-023-UNIT-KAFKA-PRODUCER-NOISE-REVIEW.md](./CR-023-UNIT-KAFKA-PRODUCER-NOISE-REVIEW.md); `tests/unit/conftest.py`; `src/libs/portfolio-common/portfolio_common/kafka_utils.py`; `python scripts/test_manifest.py --suite unit --quiet` => `893 passed, 6 deselected` with no producer-noise stderr. |
| CR-024 | 2026-03-10 | Disabled-endpoint compatibility residue | Hardened | `src/services/query_service/app/routers/legacy_gone.py` was dead compatibility residue. No production route imported it, no integration or E2E behavior depended on it, and only its own unit test plus a few historical RFCs still referenced it. | Deleted the dead helper and its unit test, then updated historical RFCs so they point to the surviving disabled-route evidence: route inventory and shared E2E assertions. | Continue removing fake compatibility surfaces when runtime ownership is gone. Keep only route-inventory and shared-assertion evidence for disabled-endpoint policy. | Evidence: [CR-024-DISABLED-ENDPOINT-COMPATIBILITY-RESIDUE-REVIEW.md](./CR-024-DISABLED-ENDPOINT-COMPATIBILITY-RESIDUE-REVIEW.md); `tests/integration/services/query_service/test_main_app.py` => `9 passed`. |
| CR-025 | 2026-03-10 | Cost-basis documentation drift in transaction specs | Hardened | Multiple transaction specs still advertised `LIFO`, `AVERAGE_COST`, `SPECIFIC_ID`, or `SPECIFIC_LOT` as active lotus-core cost-basis policies even though the code now supports only canonical `FIFO` and `AVCO`. That created direct documentation drift against runtime behavior. | Updated the affected transaction specs to reflect the actual supported policies and canonical vocabulary. | Continue removing unsupported policy names from current-state docs unless and until the implementation actually supports them. | Evidence: `docs/rfc-transaction-specs/transactions/SELL/RFC-SELL-01.md`; `docs/rfc-transaction-specs/transactions/misc/RFC-REDEMPTION-01 Canonical Fixed Income Redemption Family Specification.md`; `docs/rfc-transaction-specs/transactions/FX/RFC-FX-01 Canonical FX Transaction Specification.md`. |
| CR-026 | 2026-03-10 | OpenAPI and vocabulary governance baseline | In Review | Baseline RFC-0067 scan shows the current `openapi_quality_gate.py` and `api_vocabulary_inventory.py --validate-only` already pass. The remaining gap is quality depth, not basic compliance: Swagger/example richness needs to become an explicit review stream, and the only alias-like code hit found was sanctioned header aliasing for `X-Correlation-ID`. | Added this workstream to the review program and established the baseline evidence for future OpenAPI/example hardening slices. | Target richer request/response examples and schema descriptions on services where the gate is only minimally satisfied, while keeping no-alias governance enforced for API-facing contracts. | Evidence: `python scripts/openapi_quality_gate.py` => pass; `python scripts/api_vocabulary_inventory.py --validate-only` => pass; alias scan limited to header alias usage in `financial_reconciliation_service`. |
| CR-027 | 2026-03-10 | Financial reconciliation Swagger depth | Hardened | `financial_reconciliation_service` already passed the OpenAPI gate, but it still relied too heavily on operation-level examples. Field-level schema descriptions and parameter documentation were inconsistent, which made Swagger usable but not gold-standard. | Added field-level descriptions/examples across reconciliation request and response DTOs, enriched header/query parameter documentation, documented 404 response examples for `GET /reconciliation/runs/{run_id}`, and added an OpenAPI contract test that asserts the schema contains the richer documentation. | Continue the same pass across the other active HTTP services, starting with the weakest control-plane surfaces. | Evidence: [CR-027-FINANCIAL-RECONCILIATION-SWAGGER-DEPTH-REVIEW.md](./CR-027-FINANCIAL-RECONCILIATION-SWAGGER-DEPTH-REVIEW.md); `python -m pytest tests/integration/services/financial_reconciliation_service/test_financial_reconciliation_app.py -q` => `7 passed`; OpenAPI gate and vocabulary inventory still pass. |
| CR-028 | 2026-03-10 | Event replay Swagger depth | Hardened | `event_replay_service` already had operation-level examples, but the main operational endpoints still under-documented path/query filters and common error responses. That made the OpenAPI surface less useful for operators despite passing the baseline gate. | Added descriptions/examples for the highest-value operational path and query parameters, documented `404`/`409` response examples on the main replay endpoints, and added an OpenAPI integration test that asserts those parameters and error examples are present. | Continue the same pass on the next active HTTP surface with the thinnest remaining Swagger depth. | Evidence: `src/services/event_replay_service/app/routers/ingestion_operations.py`; `tests/integration/services/event_replay_service/test_event_replay_app.py` => `7 passed`; OpenAPI gate and vocabulary inventory still pass. |
| CR-029 | 2026-03-10 | Query control-plane operations Swagger depth | Hardened | `query_control_plane_service` already exposed the right support/lineage operations, but path/query parameter examples and 404 response examples were too thin for an operator-facing control-plane API. | Added explicit path/query parameter descriptions/examples and documented 404 response examples on the support overview, calculator SLO, valuation job, aggregation job, and lineage endpoints. Added an OpenAPI integration test asserting the schema contains these richer parameter and error docs. | Continue the same Swagger-depth pass across the remaining control-plane routers with the weakest schema quality. | Evidence: `src/services/query_control_plane_service/app/routers/operations.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py` => `6 passed`. |
| CR-030 | 2026-03-10 | Simulation Swagger depth | Hardened | `query_control_plane_service` simulation endpoints were functionally correct but still under-documented: path parameters had no examples, not-found/invalid-state responses lacked concrete examples, and the shared DTO schema depth was thin. | Added field-level schema descriptions/examples to shared simulation DTOs, enriched simulation router path parameter docs and response examples, and added an OpenAPI integration assertion that locks the richer contract in place. | Continue the same Swagger-depth pass on the next weakest active HTTP surface. Prefer shared DTO enrichment only where the schema is genuinely shared. | Evidence: [CR-030-SIMULATION-SWAGGER-DEPTH-REVIEW.md](./CR-030-SIMULATION-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/dtos/simulation_dto.py`; `src/services/query_control_plane_service/app/routers/simulation.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-031 | 2026-03-10 | Analytics inputs Swagger depth | Hardened | The analytics-input surface already had good shared DTOs, but the control-plane router still lacked explicit path parameter docs and concrete operator-facing error examples on the highest-value portfolio/export endpoints. | Added path parameter descriptions/examples and concrete `400`/`404`/`422` response examples to the analytics-input control-plane router, then added an OpenAPI integration assertion to lock those docs in place. | Continue the same depth pass on the next weakest active HTTP surface. Favor router-local response examples for service-owned behavior even when request/response DTOs are shared. | Evidence: [CR-031-ANALYTICS-INPUTS-SWAGGER-DEPTH-REVIEW.md](./CR-031-ANALYTICS-INPUTS-SWAGGER-DEPTH-REVIEW.md); `src/services/query_control_plane_service/app/routers/analytics_inputs.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-032 | 2026-03-10 | Integration contracts Swagger depth | Hardened | The integration router had solid endpoint coverage and shared DTOs, but the main policy/core-snapshot/instrument-enrichment endpoints still lacked explicit path/query docs and concrete service-owned error examples. | Added parameter descriptions/examples and concrete policy/core-snapshot/enrichment error examples, then added an OpenAPI integration assertion to keep that richer control-plane contract locked in place. | Continue the same depth pass on the remaining reference/integration endpoints where operation text still carries more meaning than explicit parameter/response contracts. | Evidence: [CR-032-INTEGRATION-CONTRACTS-SWAGGER-DEPTH-REVIEW.md](./CR-032-INTEGRATION-CONTRACTS-SWAGGER-DEPTH-REVIEW.md); `src/services/query_control_plane_service/app/routers/integration.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-033 | 2026-03-10 | Capabilities Swagger depth | Hardened | The capabilities endpoint already had a good shared response model, but its query-parameter contract was still thin compared with the other hardened control-plane routes. | Added examples and clearer descriptions for `consumer_system` and `tenant_id`, then added an OpenAPI integration assertion that locks those defaults and descriptions in place. | Continue the review program outside the main control-plane Swagger surfaces, starting with stale historical references or the next weakest HTTP contract outside `query_control_plane_service`. | Evidence: [CR-033-CAPABILITIES-SWAGGER-DEPTH-REVIEW.md](./CR-033-CAPABILITIES-SWAGGER-DEPTH-REVIEW.md); `src/services/query_control_plane_service/app/routers/capabilities.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-034 | 2026-03-10 | Query transactions Swagger depth | Hardened | The core read-plane transaction ledger already had the right endpoint shape and FX-aware filters, but router-level parameter descriptions/examples and the portfolio-not-found response were too thin for a high-value investigative API. | Added explicit descriptions/examples for the transaction filter set, documented a concrete `404` portfolio-not-found example, and added an OpenAPI integration assertion to lock the richer ledger contract in place. | Continue the same depth pass on the next weakest `query_service` read-plane endpoint, not by broad random edits. | Evidence: [CR-034-QUERY-TRANSACTIONS-SWAGGER-DEPTH-REVIEW.md](./CR-034-QUERY-TRANSACTIONS-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/transactions.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-035 | 2026-03-10 | Positions Swagger depth | Hardened | The `positions` and `position-history` endpoints already had the correct response models and not-found handling, but their path/query parameter docs and concrete `404` examples were still thin compared with the hardened transaction ledger and control-plane surfaces. | Added explicit descriptions/examples for the holdings and history filters, documented concrete portfolio-not-found examples for both endpoints, and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on the next weakest `query_service` read-plane endpoint. | Evidence: [CR-035-POSITIONS-SWAGGER-DEPTH-REVIEW.md](./CR-035-POSITIONS-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/positions.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-036 | 2026-03-10 | Cashflow projection Swagger depth | Hardened | The cashflow projection endpoint already had the correct response model and business description, but its path/query parameter docs and portfolio-not-found response were still thin compared with the hardened read-plane endpoints. | Added explicit descriptions/examples for the projection controls, documented a concrete `404` portfolio-not-found example, and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on the remaining `query_service` read-plane endpoints. | Evidence: [CR-036-CASHFLOW-PROJECTION-SWAGGER-DEPTH-REVIEW.md](./CR-036-CASHFLOW-PROJECTION-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/cashflow_projection.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-037 | 2026-03-10 | Portfolio discovery Swagger depth | Hardened | The top-level portfolio discovery endpoints were functionally correct, but their query-filter docs, single-portfolio description, and concrete `404` example were still below the hardened read-plane standard. | Added explicit descriptions/examples for the discovery filters, documented a concrete `404` example for single-portfolio lookup, and added an OpenAPI integration assertion to lock the richer discovery contract in place. | Continue the same depth pass on the remaining `query_service` read-plane endpoints. | Evidence: [CR-037-PORTFOLIO-DISCOVERY-SWAGGER-DEPTH-REVIEW.md](./CR-037-PORTFOLIO-DISCOVERY-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/portfolios.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-038 | 2026-03-10 | Reference market data Swagger depth | Hardened | The `prices` and `fx-rates` endpoints already had the correct response models and business descriptions, but their parameter examples and wording were still thin relative to the hardened read-plane standard. | Added explicit descriptions/examples for the market-data request parameters and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on the remaining weaker `query_service` reference/read endpoints. | Evidence: [CR-038-REFERENCE-MARKET-DATA-SWAGGER-DEPTH-REVIEW.md](./CR-038-REFERENCE-MARKET-DATA-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/prices.py`; `src/services/query_service/app/routers/fx_rates.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-039 | 2026-03-10 | Lookup catalog Swagger depth | Hardened | The lookup catalog endpoints already had the correct response model and business descriptions, but their selector/search parameter examples were still too thin for frequent Swagger-driven exploration. | Added explicit examples for the main lookup selector/search parameters and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on any remaining weaker `query_service` reference/read endpoints. | Evidence: [CR-039-LOOKUP-CATALOG-SWAGGER-DEPTH-REVIEW.md](./CR-039-LOOKUP-CATALOG-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/lookups.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-040 | 2026-03-10 | Instrument reference Swagger depth | Hardened | The instrument reference endpoint already had the correct response model and pagination behavior, but router-level filter docs were thin and the shared `InstrumentRecord` schema lacked field-level descriptions/examples for its extended FX-aware fields. | Added explicit filter descriptions/examples, enriched `InstrumentRecord` with field-level descriptions/examples, and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on any remaining thinner DTO-heavy API surfaces. | Evidence: [CR-040-INSTRUMENT-REFERENCE-SWAGGER-DEPTH-REVIEW.md](./CR-040-INSTRUMENT-REFERENCE-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/instruments.py`; `src/services/query_service/app/dtos/instrument_dto.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-041 | 2026-03-10 | BUY/SELL state Swagger depth | Hardened | The investigative BUY/SELL state endpoints already had strong business descriptions, but their path parameters and not-found responses were still too thin for audit/reconciliation use from Swagger. | Added explicit path parameter descriptions/examples, documented concrete `404` examples for BUY/SELL state and cash-linkage not-found cases, and added an OpenAPI integration assertion to lock the richer contract in place. | Continue the same depth pass on any remaining thinner API surfaces. | Evidence: [CR-041-BUY-SELL-STATE-SWAGGER-DEPTH-REVIEW.md](./CR-041-BUY-SELL-STATE-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/routers/buy_state.py`; `src/services/query_service/app/routers/sell_state.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-042 | 2026-03-10 | Read model schema Swagger depth | Hardened | Router-level contracts had improved significantly, but several shared read/reference DTO schemas still lacked field-level description/example depth, leaving Swagger partially informative only at the endpoint level. | Added field-level descriptions/examples to the shared market-price, FX-rate, and lookup DTOs, tightened the portfolio-query response description, and added an OpenAPI integration assertion to lock the richer schema depth in place. | Continue the same schema-depth pass where routers are already strong but shared components are still thin. | Evidence: [CR-042-READ-MODEL-SCHEMA-SWAGGER-DEPTH-REVIEW.md](./CR-042-READ-MODEL-SCHEMA-SWAGGER-DEPTH-REVIEW.md); `src/services/query_service/app/dtos/portfolio_dto.py`; `src/services/query_service/app/dtos/price_dto.py`; `src/services/query_service/app/dtos/fx_rate_dto.py`; `src/services/query_service/app/dtos/lookup_dto.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-043 | 2026-03-10 | Event replay shared schema Swagger depth | Hardened | The event replay router layer was already strong, but shared replay and ingestion-operations DTO components from `ingestion_service` were still thinner at field level than the surrounding control-plane contracts. | Tightened field-level descriptions on the shared replay and operations DTOs, added an OpenAPI integration assertion that locks the richer shared-schema contract in place, and kept the existing router-level examples untouched. | Continue the same schema-depth pass on shared DTOs used by active HTTP services where router docs are already strong. | Evidence: [CR-043-EVENT-REPLAY-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-043-EVENT-REPLAY-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/ingestion_job_dto.py`; `tests/integration/services/event_replay_service/test_event_replay_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-044 | 2026-03-10 | Position, cashflow, and valuation schema Swagger depth | Hardened | Router-level `query_service` contracts were already strong, but the shared positions/cashflow/valuation component schemas were still thinner than the endpoint layer. That left Swagger less useful for client generation and schema-first inspection. | Added field-level descriptions/examples to `CashflowRecord` and `ValuationData`, tightened position-history schema descriptions/examples, and added an OpenAPI integration assertion to lock the richer schema depth in place. | Continue the same schema-depth pass on the next weakest shared DTO surface after positions/cashflow/valuation. | Evidence: [CR-044-POSITION-CASHFLOW-VALUATION-SCHEMA-SWAGGER-REVIEW.md](./CR-044-POSITION-CASHFLOW-VALUATION-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/cashflow_dto.py`; `src/services/query_service/app/dtos/valuation_dto.py`; `src/services/query_service/app/dtos/position_dto.py`; `tests/integration/services/query_service/test_main_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-045 | 2026-03-10 | Capabilities shared schema Swagger depth | Hardened | The capabilities endpoint already had strong query-parameter documentation, but its nested capability/workflow component schemas still lacked schema-level examples. That made the reusable component models weaker than the top-level endpoint contract. | Added field-level examples to `FeatureCapability` and `WorkflowCapability`, then added an OpenAPI integration assertion that locks the richer component-schema descriptions in place. | Continue the same schema-depth pass on other shared control-plane DTOs where endpoints are already strong but nested components are still thin. | Evidence: [CR-045-CAPABILITIES-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-045-CAPABILITIES-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/capabilities_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-046 | 2026-03-10 | Operations shared schema Swagger depth | Hardened | The operations router already had strong parameter and error documentation, but its nested SLO and list-wrapper component schemas still lacked reusable examples. That made the component layer weaker than the operator-facing endpoint text. | Added examples to the nested calculator SLO, lineage-key list, and support-job list schema properties, then added an OpenAPI integration assertion that locks the richer component-schema descriptions in place. | Continue the same schema-depth pass on the next weakest shared control-plane DTO surface. | Evidence: [CR-046-OPERATIONS-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-046-OPERATIONS-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-047 | 2026-03-10 | Integration shared schema Swagger depth | Hardened | The integration router already had strong parameter and error documentation, but the shared policy-provenance and instrument-enrichment component schemas still needed stronger schema-level examples and clearer request wording. | Added reusable examples for `policy_provenance`, tightened the bulk enrichment request description, and added an OpenAPI integration assertion that locks the richer component-schema descriptions in place. | Continue the same schema-depth pass on the next weakest shared integration DTO surface. | Evidence: [CR-047-INTEGRATION-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-047-INTEGRATION-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/integration_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-048 | 2026-03-10 | Analytics export shared schema Swagger depth | Hardened | The analytics-input router already had strong parameter and error documentation, but the reusable paging and export-result component schemas were still lighter than the endpoint layer. That weakened schema-first inspection of export contracts. | Added an explicit `next_page_token` example, added a concrete export row example for `AnalyticsExportJsonResultResponse.data`, and added an OpenAPI integration assertion that locks these richer component-schema descriptions in place. | Continue the same schema-depth pass on the next weakest shared analytics-input DTO surface. | Evidence: [CR-048-ANALYTICS-EXPORT-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-048-ANALYTICS-EXPORT-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/analytics_input_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-050 | 2026-03-10 | Simulation shared schema Swagger depth | Hardened | The simulation router already had good parameter and error documentation, but the reusable session, change-list, and projected-position component schemas still relied mostly on field text without concrete nested examples. | Added schema-level nested examples for session metadata, change lists, and projected positions, then added an OpenAPI integration assertion that locks these richer component-schema descriptions in place. | Continue the same schema-depth pass on the next weakest shared DTO surface. | Evidence: [CR-050-SIMULATION-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-050-SIMULATION-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/simulation_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-051 | 2026-03-10 | Core snapshot shared schema Swagger depth | Hardened | The core-snapshot router already had strong parameter and error documentation, but the reusable governance, freshness, and section component schemas were still too thin for schema-first inspection in Swagger. | Added concrete examples for governance section lists and policy provenance, added baseline `snapshot_timestamp` freshness metadata, deepened reusable section payload examples, and added an OpenAPI integration assertion that locks the richer component-schema contract in place. | Continue the same schema-depth pass on the next weakest shared integration/reference DTO surface. | Evidence: [CR-051-CORE-SNAPSHOT-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-051-CORE-SNAPSHOT-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/core_snapshot_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-052 | 2026-03-10 | Benchmark and reference integration Swagger depth | Hardened | The integration router had already been hardened for policy and core-snapshot endpoints, but the lower benchmark/reference endpoints still lacked explicit path parameter examples and concrete 404 examples for the benchmark assignment/definition cases. | Added path parameter descriptions/examples for `portfolio_id`, `benchmark_id`, and `index_id`, documented explicit benchmark assignment/definition not-found examples, and added an OpenAPI integration assertion that locks those parameter and error contracts in place. | Continue the Swagger-depth pass on the next weakest active integration/reference endpoint surface. | Evidence: [CR-052-BENCHMARK-REFERENCE-INTEGRATION-SWAGGER-REVIEW.md](./CR-052-BENCHMARK-REFERENCE-INTEGRATION-SWAGGER-REVIEW.md); `src/services/query_control_plane_service/app/routers/integration.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-053 | 2026-03-10 | Benchmark and reference shared schema Swagger depth | Hardened | The integration router for benchmark/reference data had become materially stronger in CR-052, but the shared benchmark, index, risk-free, coverage, and classification DTOs still had thinner example coverage for catalog, lineage, and coverage metadata. | Added schema-level examples for catalog responses, lineage metadata, coverage fields, and taxonomy records, then added an OpenAPI integration assertion that locks those richer component contracts in place. | Continue the same schema-depth pass on the next weakest active shared DTO surface. | Evidence: [CR-053-BENCHMARK-REFERENCE-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-053-BENCHMARK-REFERENCE-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/query_service/app/dtos/reference_integration_dto.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-054 | 2026-03-10 | Bulk upload Swagger depth | Hardened | The upload endpoints already exposed the correct basic contracts, but parameter descriptions and operational error examples were still thin. The shared upload DTOs also lacked field-level descriptions/examples, leaving Swagger more useful at the route level than at the reusable component level. | Added field-level descriptions/examples to the shared upload DTOs, enriched the upload router parameter docs, documented explicit `400`, `410`, `429`, and `503` response examples, and added an OpenAPI integration assertion that locks the richer upload contract in place. | Continue the write-plane Swagger pass on the next weakest ingestion router or shared ingestion DTO surface. | Evidence: [CR-054-BULK-UPLOAD-SWAGGER-DEPTH-REVIEW.md](./CR-054-BULK-UPLOAD-SWAGGER-DEPTH-REVIEW.md); `src/services/ingestion_service/app/DTOs/upload_dto.py`; `src/services/ingestion_service/app/routers/uploads.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-055 | 2026-03-10 | Portfolio bundle Swagger depth | Hardened | The portfolio-bundle endpoint already had a strong high-level description, but its operational error responses were still thin. The shared `PortfolioBundleIngestionRequest` schema also relied too heavily on one large example and lacked field-level descriptions/examples for each entity collection. | Added field-level descriptions/examples for each entity collection in the bundle request, documented explicit `410`, `429`, and `503` response examples on the router, and added an OpenAPI integration assertion that locks the richer bundle contract in place. | Continue the write-plane Swagger pass on the next weakest ingestion DTO or router surface. | Evidence: [CR-055-PORTFOLIO-BUNDLE-SWAGGER-DEPTH-REVIEW.md](./CR-055-PORTFOLIO-BUNDLE-SWAGGER-DEPTH-REVIEW.md); `src/services/ingestion_service/app/DTOs/portfolio_bundle_dto.py`; `src/services/ingestion_service/app/routers/portfolio_bundle.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-056 | 2026-03-10 | Reprocessing Swagger depth | Hardened | The reprocessing endpoint already had the right business description, but lacked explicit operational error examples. The shared `ReprocessingRequest` schema was minimal and the acknowledgment components relied on descriptions without explicit required-field markers in the field definitions. | Added explicit examples to the reprocessing request DTO, marked shared acknowledgment fields explicitly required in their field declarations, documented `409`, `429`, and `503` response examples on the router, and added an OpenAPI integration assertion that locks the richer reprocessing contract in place. | Continue the write-plane Swagger pass on the next weakest ingestion router or shared ingestion DTO surface. | Evidence: [CR-056-REPROCESSING-SWAGGER-DEPTH-REVIEW.md](./CR-056-REPROCESSING-SWAGGER-DEPTH-REVIEW.md); `src/services/ingestion_service/app/DTOs/ingestion_ack_dto.py`; `src/services/ingestion_service/app/DTOs/reprocessing_dto.py`; `src/services/ingestion_service/app/routers/reprocessing.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-057 | 2026-03-10 | Business date shared schema Swagger depth | Hardened | The `business_dates` router had already been hardened at the operational response layer, but the shared DTO schema still lacked field-level examples and a descriptive request-wrapper contract. That left the write-plane component schema weaker than the route itself. | Added field-level descriptions/examples to `BusinessDate`, added a described example-backed `business_dates` collection field to `BusinessDateIngestionRequest`, and added an ingestion app OpenAPI contract assertion to lock the richer schema depth in place. | Continue the ingestion shared-schema pass on the next weakest DTO family after business dates. | Evidence: [CR-057-BUSINESS-DATE-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-057-BUSINESS-DATE-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/business_date_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-058 | 2026-03-10 | Portfolio, market price, and FX rate ingestion shared schema Swagger depth | Hardened | These three ingestion DTO families were still below the rest of the write-plane Swagger standard. They relied on sparse examples or top-level model examples rather than strong field-level component documentation, leaving the router layer richer than the reusable component schemas. | Added field-level descriptions/examples to `Portfolio`, `MarketPrice`, and `FxRate`; added described example-backed request-wrapper collection fields to their ingestion request models; and added ingestion app OpenAPI assertions to lock the richer schema behavior in place. | Continue the ingestion shared-schema pass on the next weakest DTO family after portfolio/market/FX. | Evidence: [CR-058-PORTFOLIO-MARKET-FX-INGESTION-SCHEMA-SWAGGER-REVIEW.md](./CR-058-PORTFOLIO-MARKET-FX-INGESTION-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/portfolio_dto.py`; `src/services/ingestion_service/app/DTOs/market_price_dto.py`; `src/services/ingestion_service/app/DTOs/fx_rate_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-059 | 2026-03-10 | Instrument ingestion shared schema Swagger depth | Hardened | The instrument ingestion router had already been hardened at the operational response layer, but the shared component schema still looked like early scaffolding: sparse field examples, thin issuer/FX-contract field documentation, and no described example-backed request-wrapper collection field. | Added field-level descriptions/examples across the live `Instrument` fields, added a described example-backed `instruments` collection field to `InstrumentIngestionRequest`, and added ingestion app OpenAPI assertions to lock the richer schema behavior in place. | Continue the ingestion shared-schema pass on the next weakest DTO family after instruments. | Evidence: [CR-059-INSTRUMENT-INGESTION-SCHEMA-SWAGGER-REVIEW.md](./CR-059-INSTRUMENT-INGESTION-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/instrument_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-060 | 2026-03-10 | Transaction core schema Swagger depth | Hardened | `transaction_dto.py` already carried many examples, but the most important core ledger fields still lacked strong field-level descriptions, and the wrapper request model was too thin for schema-first clients. This was the largest remaining active ingestion schema surface, so the highest-value first step was to harden the canonical core fields before the longer specialized tail. | Added field-level descriptions to the core transaction ledger fields, added a described example-backed `transactions` collection field to `TransactionIngestionRequest`, and added ingestion app OpenAPI assertions to lock the richer core-schema behavior in place. | Continue the transaction DTO Swagger pass on the next weakest specialized field family after the core ledger contract. | Evidence: [CR-060-TRANSACTION-CORE-SCHEMA-SWAGGER-REVIEW.md](./CR-060-TRANSACTION-CORE-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/transaction_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-061 | 2026-03-10 | Transaction dual-leg and income schema Swagger depth | Hardened | After CR-060, the transaction core ledger fields were stronger, but the dual-leg and income-related field family still used older wording and weaker schema-level descriptions. This is an active business surface for linked product/cash legs, adjustment-style linkage, and interest reconciliation. | Tightened field-level descriptions across the dual-leg and income field family and added ingestion app OpenAPI assertions to lock the richer schema behavior in place. | Continue the transaction DTO Swagger pass on the next weakest specialized field family after dual-leg and income semantics. | Evidence: [CR-061-TRANSACTION-DUAL-LEG-INCOME-SCHEMA-SWAGGER-REVIEW.md](./CR-061-TRANSACTION-DUAL-LEG-INCOME-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/transaction_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-062 | 2026-03-10 | Transaction FX schema Swagger depth | Hardened | After CR-060 and CR-061, the transaction core, dual-leg, and income fields were stronger, but the FX-specific field family still used thinner wording despite being an active contract surface for FX spot/forward/swap ingestion, contract lifecycle linkage, settlement-leg semantics, and realized FX P&L population. | Tightened field-level descriptions across the FX-specific field family in `transaction_dto.py` and added ingestion app OpenAPI assertions to lock the richer FX schema behavior in place. | Continue the transaction DTO Swagger pass on the remaining specialized transaction tails after FX. | Evidence: [CR-062-TRANSACTION-FX-SCHEMA-SWAGGER-REVIEW.md](./CR-062-TRANSACTION-FX-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/transaction_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-063 | 2026-03-10 | Transaction corporate-action and synthetic-flow schema Swagger depth | Hardened | After CR-060 through CR-062, the transaction core, dual-leg, income, and FX field families were materially stronger, but the corporate-action and synthetic-flow tail still used thinner wording despite being an active contract surface for parent/child CA linkage, transfer and cash-in-lieu settlement relationships, and position-level synthetic-flow analytics. | Tightened field-level descriptions across the corporate-action and synthetic-flow field family in `transaction_dto.py` and added ingestion app OpenAPI assertions to lock the richer schema behavior in place. | Continue the transaction DTO Swagger pass only if a remaining specialist tail still falls materially below the now-hardened standard. | Evidence: [CR-063-TRANSACTION-CORPORATE-ACTION-SCHEMA-SWAGGER-REVIEW.md](./CR-063-TRANSACTION-CORPORATE-ACTION-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/transaction_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-064 | 2026-03-10 | Reference data ingestion shared schema Swagger depth | Hardened | The reference-data router layer was already operationally strong, but the shared component-schema layer still had thin request-wrapper descriptions and sparse provenance examples, which made schema-first client inspection weaker than the endpoint layer. | Tightened reusable provenance-field descriptions/examples across `reference_data_dto.py`, added described example-backed request-wrapper collection fields for the main reference-data ingestion payload families, and added ingestion app OpenAPI assertions to lock the richer shared-schema behavior in place. | Review only the remaining low-signal ingestion DTOs if they still fall materially below the now-hardened write-plane standard. | Evidence: [CR-064-REFERENCE-DATA-INGESTION-SCHEMA-SWAGGER-REVIEW.md](./CR-064-REFERENCE-DATA-INGESTION-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/reference_data_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-065 | 2026-03-10 | Ingestion job shared schema Swagger depth | Hardened | The event-replay and ingestion-operations router surfaces were already operationally strong, but the shared ingestion-job DTO layer still had thin list-wrapper examples, thinner backlog summary metadata, and no explicit shared policy flag for replay dry-run support. | Added example-backed list-wrapper schema depth for ingestion jobs, job failures, and reprocessing queue health; added explicit shared operational metadata for the oldest backlog job and replay dry-run support; and added event-replay app OpenAPI assertions to lock the richer shared-schema behavior in place. | Review only the remaining operational DTOs if they still fall materially below the now-hardened control-plane standard. | Evidence: [CR-065-INGESTION-JOB-SHARED-SCHEMA-SWAGGER-REVIEW.md](./CR-065-INGESTION-JOB-SHARED-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/ingestion_job_dto.py`; `tests/integration/services/event_replay_service/test_event_replay_app.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-066 | 2026-03-10 | Write-plane acknowledgement schema Swagger depth | Hardened | After the larger write-plane DTO families were strengthened, the smaller shared acknowledgement and replay-request DTOs were still acceptable but used older, thinner operational wording. | Tightened the shared acknowledgement and replay-request descriptions in `ingestion_ack_dto.py` and `reprocessing_dto.py`, then added an ingestion app OpenAPI assertion to lock the stronger wording in place. | Remaining Swagger work should now focus only on surfaces that still show materially weaker schema depth than the current baseline. | Evidence: [CR-066-WRITE-PLANE-ACKNOWLEDGEMENT-SCHEMA-SWAGGER-REVIEW.md](./CR-066-WRITE-PLANE-ACKNOWLEDGEMENT-SCHEMA-SWAGGER-REVIEW.md); `src/services/ingestion_service/app/DTOs/ingestion_ack_dto.py`; `src/services/ingestion_service/app/DTOs/reprocessing_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; OpenAPI gate and vocabulary inventory still pass. |
| CR-067 | 2026-03-10 | Contract tooling quiet mode | Hardened | The OpenAPI and API vocabulary scripts were functionally healthy but still emitted routine service-startup JSON logs, which made contract-gate output harder to scan in both local review loops and CI. | Added a tooling-only quiet flag in shared logging setup, enabled it by default in the OpenAPI/vocabulary scripts, and added unit coverage proving quiet-mode vs normal-mode logging levels. | Keep future contract tooling on the same quiet-mode path unless a script genuinely needs live service logs. | Evidence: [CR-067-CONTRACT-TOOLING-QUIET-MODE-REVIEW.md](./CR-067-CONTRACT-TOOLING-QUIET-MODE-REVIEW.md); `src/libs/portfolio-common/portfolio_common/logging_utils.py`; `scripts/openapi_quality_gate.py`; `scripts/api_vocabulary_inventory.py`; `tests/unit/libs/portfolio-common/test_logging_utils.py`. |
| CR-068 | 2026-03-10 | Test manifest legacy alias cleanup | Hardened | `scripts/test_manifest.py` still exposed backward-compatible suite aliases such as `sell-rfc` and `fx-rfc`, even though the current project direction is no legacy support unless it protects a real live compatibility contract. These aliases only blurred the canonical suite vocabulary. | Removed the legacy suite aliases and corresponding environment-profile mappings from `scripts/test_manifest.py`, then updated the manifest unit tests to use the canonical suite names. | Keep the manifest vocabulary canonical. If a future external caller genuinely needs a compatibility alias, add it only with explicit justification. | Evidence: [CR-068-TEST-MANIFEST-LEGACY-ALIAS-REVIEW.md](./CR-068-TEST-MANIFEST-LEGACY-ALIAS-REVIEW.md); `scripts/test_manifest.py`; `tests/unit/services/query_service/test_test_manifest.py`. |
| CR-069 | 2026-03-10 | Canonical contract naming cleanup | Hardened | Active RFCs, conformance reports, and the `Makefile` still referenced deleted suite aliases (`buy-rfc`, `sell-rfc`, `fx-rfc`, etc.) even after the manifest had been canonicalized. That left current-state documentation and entrypoints out of sync with live CI and the actual test manifest. | Removed the legacy alias targets from `Makefile`, updated active RFC/conformance docs to the canonical `transaction-*-contract` names, and regenerated the API vocabulary artifact so current-state standards reflect the live OpenAPI surface. | Historical review records may still mention removed aliases intentionally as migration evidence. Keep only current-state docs/tooling alias-free. | Evidence: [CR-069-CANONICAL-CONTRACT-NAMING-CLEANUP.md](./CR-069-CANONICAL-CONTRACT-NAMING-CLEANUP.md); `Makefile`; active RFC/conformance docs; `docs/standards/api-vocabulary/lotus-core-api-vocabulary.v1.json`. |
| CR-070 | 2026-03-10 | JSON error response example enforcement | Hardened | The remaining Swagger debt was systemic: many JSON error responses, especially FastAPI-generated `422` validation errors, still had no examples even though the router and DTO passes had already improved success-path documentation. | Extended the shared OpenAPI enrichment layer to generate non-2xx JSON response examples, tightened the OpenAPI quality gate to enforce error-response examples, and added unit coverage for both the enrichment and the gate. | New HTTP apps must keep using the shared enrichment path or they will regress immediately under the strengthened gate. | Evidence: [CR-070-ERROR-RESPONSE-EXAMPLE-GATE-REVIEW.md](./CR-070-ERROR-RESPONSE-EXAMPLE-GATE-REVIEW.md); `src/libs/portfolio-common/portfolio_common/openapi_enrichment.py`; `scripts/openapi_quality_gate.py`; `tests/unit/libs/portfolio-common/test_openapi_enrichment.py`; `tests/unit/services/query_service/test_openapi_quality_gate.py`. |
| CR-071 | 2026-03-10 | Parameter description enforcement | Hardened | After the router and DTO passes, a small but real Swagger usability gap remained: some query/path parameters still had examples but no descriptions, concentrated in `query_service` lookups and `event_replay_service` operations endpoints. | Added the missing parameter descriptions/examples on the remaining weak endpoints and tightened `scripts/openapi_quality_gate.py` so parameter descriptions are mandatory for declared parameters. | New endpoints must document every parameter on day one or they will fail the strengthened gate. | Evidence: [CR-071-PARAMETER-DESCRIPTION-GATE-REVIEW.md](./CR-071-PARAMETER-DESCRIPTION-GATE-REVIEW.md); `src/services/query_service/app/routers/lookups.py`; `src/services/event_replay_service/app/routers/ingestion_operations.py`; `scripts/openapi_quality_gate.py`; `tests/unit/services/query_service/test_openapi_quality_gate.py`. |
| CR-072 | 2026-03-10 | Empty transaction batch contract alignment | Hardened | `POST /ingest/transactions` rejected `{"transactions": []}` with `422` because `TransactionIngestionRequest.transactions` still had `min_length=1`, even though the publish path already handled an empty list safely and the E2E/API expectation treats an empty batch as a valid no-op submission. | Removed the `min_length=1` constraint, updated the transaction ingestion schema description/examples to make the no-op semantics explicit, added direct ingestion router coverage, and aligned the OpenAPI contract test with the new example set. | None for the empty-batch path; the behavior is now covered below E2E and matches the public contract. | Evidence: [CR-072-EMPTY-TRANSACTION-BATCH-CONTRACT-REVIEW.md](./CR-072-EMPTY-TRANSACTION-BATCH-CONTRACT-REVIEW.md); `src/services/ingestion_service/app/DTOs/transaction_dto.py`; `tests/integration/services/ingestion_service/test_ingestion_routers.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; `tests/e2e/test_ingestion_service_api.py`. |
| CR-073 | 2026-03-11 | Control-plane documentation ownership drift | Hardened | Active RFCs and ADRs still referenced deleted `query_service` control-plane router paths even after RFC 81 moved that surface into `query_control_plane_service`. This left current-state architecture docs pointing to the wrong implementation tree. | Rewrote active RFC and ADR references to the live `query_control_plane_service` router paths and corrected the stale source-path header comment in `src/services/query_control_plane_service/app/routers/simulation.py`. Left review-history artifacts unchanged where they intentionally describe pre-refactor state. | Continue scanning for current-state docs that still point at removed runtime files whenever future ownership moves land. Do not rewrite review notes that intentionally document the prior state. | Evidence: [CR-073-CONTROL-PLANE-DOCUMENTATION-OWNERSHIP-DRIFT-REVIEW.md](./CR-073-CONTROL-PLANE-DOCUMENTATION-OWNERSHIP-DRIFT-REVIEW.md); active RFC/ADR path rewrites; `src/services/query_control_plane_service/app/routers/simulation.py`. |
| CR-074 | 2026-03-11 | Grafana service-topology visibility | Hardened | Prometheus already scraped the post-RFC-81 service set, but the bundled Grafana dashboard still exposed only generic DB/outbox counters. Operators had no direct panel for per-service request load or scrape availability across the split service topology. | Added service-scoped HTTP request-rate and service-availability panels to `grafana/dashboards/portfolio_analytics.json`, keyed exactly to the live Prometheus `job_name` set from `prometheus/prometheus.yml`. | Add deeper domain-specific panels later if operators need reconciliation/replay/stage-gate visibility directly in Grafana. The basic topology-visibility gap is now closed. | Evidence: [CR-074-GRAFANA-SERVICE-TOPOLOGY-REVIEW.md](./CR-074-GRAFANA-SERVICE-TOPOLOGY-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `prometheus/prometheus.yml`. |
| CR-075 | 2026-03-11 | Test harness output control | Hardened | `tests/conftest.py` emitted detailed lifecycle output unconditionally, including per-service health confirmations and DB cleanup chatter. That made routine Docker-backed validation harder to scan even when the underlying tests were healthy. | Added `tests/test_support/output_control.py`, introduced `LOTUS_TESTS_VERBOSE`, and routed `tests/conftest.py` lifecycle output through the helper so only the highest-value milestones print by default while detailed noise becomes opt-in. Added direct unit coverage for the helper and reran representative integration/E2E slices. | Keep new harness output behind the same explicit verbosity switch. If future fixtures add lifecycle chatter, route them through the shared helper rather than printing directly. | Evidence: [CR-075-TEST-HARNESS-OUTPUT-CONTROL-REVIEW.md](./CR-075-TEST-HARNESS-OUTPUT-CONTROL-REVIEW.md); `tests/test_support/output_control.py`; `tests/conftest.py`; `tests/unit/test_support/test_output_control.py`; `tests/integration/services/ingestion_service/test_ingestion_main_app_contract.py`; `tests/e2e/test_ingestion_service_api.py`. |
| CR-076 | 2026-03-11 | E2E failure-scenario output noise | Hardened | `tests/e2e/test_failure_scenarios.py` still emitted verbose recovery-step chatter directly to stdout, which made a successful outage-recovery run noisy even after CR-075 quieted the shared harness. | Routed failure-scenario progress output through `tests.test_support.output_control.emit_test_output`, keeping major recovery milestones visible while moving repetitive readiness/DLQ chatter behind verbose mode. | Apply the same pattern to any future high-noise E2E modules instead of adding direct `print(...)` calls. | Evidence: [CR-076-E2E-FAILURE-SCENARIO-OUTPUT-NOISE-REVIEW.md](./CR-076-E2E-FAILURE-SCENARIO-OUTPUT-NOISE-REVIEW.md); `tests/e2e/test_failure_scenarios.py`; `tests/test_support/output_control.py`; `tests/conftest.py`; `python -m pytest tests/e2e/test_failure_scenarios.py -q`. |
| CR-077 | 2026-03-11 | DLQ replayer test cleanup noise | Hardened | `tests/integration/tools/test_dlq_replayer.py` still printed Kafka topic-deletion cleanup failures directly to stdout during teardown. That was low-frequency but inconsistent with the broader effort to keep healthy test runs quiet. | Replaced the direct cleanup print with `warnings.warn(...)`, keeping teardown anomalies visible without polluting normal test output. | Follow the same rule for future teardown-only diagnostics: warnings, not unconditional prints. | Evidence: [CR-077-DLQ-REPLAYER-TEST-CLEANUP-NOISE-REVIEW.md](./CR-077-DLQ-REPLAYER-TEST-CLEANUP-NOISE-REVIEW.md); `tests/integration/tools/test_dlq_replayer.py`; `python -m pytest tests/integration/tools/test_dlq_replayer.py -q`. |
| CR-078 | 2026-03-11 | Financial reconciliation observability | Hardened | `financial_reconciliation_service` had no business-level Prometheus telemetry for run outcomes, findings volume, or run duration, even though it is now a first-class control-plane service. That left Grafana blind to the actual reconciliation workload. | Added reconciliation run/finding/duration metrics in `portfolio_common.monitoring`, instrumented the reconciliation service at run finalization, added unit coverage for the metrics hook, and extended the bundled Grafana dashboard with reconciliation outcome and finding panels. | Add queue-depth or backlog gauges later only if the service gains a worker-claim/backlog model. Keep the current instrumentation focused on completed-run behavior. | Evidence: [CR-078-FINANCIAL-RECONCILIATION-OBSERVABILITY-REVIEW.md](./CR-078-FINANCIAL-RECONCILIATION-OBSERVABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/services/financial_reconciliation_service/app/services/reconciliation_service.py`; `tests/unit/services/financial_reconciliation_service/test_reconciliation_service.py`; `grafana/dashboards/portfolio_analytics.json`. |
| CR-079 | 2026-03-11 | Replay observability dashboard coverage | Hardened | Replay telemetry already existed in the shared monitoring layer and was being incremented by `ingestion_job_service`, but the bundled Grafana dashboard did not surface it. That left replay pressure invisible unless operators started from APIs or raw Prometheus queries. | Added Grafana panels for replay audit outcomes and replay duplicate/failure pressure using the existing replay metrics. | If operators later want a single ingestion recovery view, compose replay, backlog, and DLQ signals together in a dedicated dashboard slice rather than overloading the generic overview further. | Evidence: [CR-079-REPLAY-OBSERVABILITY-DASHBOARD-REVIEW.md](./CR-079-REPLAY-OBSERVABILITY-DASHBOARD-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/services/ingestion_service/app/services/ingestion_job_service.py`. |
| CR-080 | 2026-03-11 | Ingestion backlog and reprocessing dashboard coverage | Hardened | Ingestion backlog age and reprocessing worker pressure were already exposed through Prometheus, but the bundled Grafana dashboard did not show them. That forced operators to leave the dashboard to understand whether ingestion delay or replay pressure was building. | Added dashboard panels for backlog age plus active reprocessing keys and for reprocessing worker claimed/completed/failed throughput by job type. | If operators later need a dedicated ingestion recovery dashboard, split backlog/replay/DLQ/worker signals into a focused recovery view rather than endlessly growing the overview dashboard. | Evidence: [CR-080-INGESTION-BACKLOG-REPROCESSING-DASHBOARD-REVIEW.md](./CR-080-INGESTION-BACKLOG-REPROCESSING-DASHBOARD-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `src/libs/portfolio-common/portfolio_common/monitoring.py`. |
| CR-081 | 2026-03-11 | Outbox and valuation dashboard coverage | Hardened | Outbox backlog/publish pressure and valuation scheduler/worker health metrics were already exported, but the bundled Grafana overview did not surface them. Operators therefore had no direct dashboard signal for event publication buildup or valuation worker health. | Added dashboard panels for outbox pending/published/failed/retried pressure and for valuation job creation, terminal failures, worker claim rate, and stale reset rate. | If the overview becomes too crowded, move the operational reliability panels into a dedicated dashboard rather than removing them entirely. | Evidence: [CR-081-OUTBOX-VALUATION-DASHBOARD-REVIEW.md](./CR-081-OUTBOX-VALUATION-DASHBOARD-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `src/libs/portfolio-common/portfolio_common/monitoring.py`. |
| CR-082 | 2026-03-11 | Timeseries feature doc ownership drift | Hardened | The feature docs under `docs/features/timeseries_generator/` still described `timeseries_generator_service` as owning both position and portfolio time-series stages even after portfolio aggregation moved to `portfolio_aggregation_service`. This sent readers to the wrong ownership model and to stale file paths. | Updated the overview, methodology guide, and developer guide to describe the split correctly and corrected contributor file/test paths to the live `portfolio_aggregation_service` sources where appropriate. | If the split grows further, consider separate feature-doc directories for position-timeseries generation and portfolio aggregation. | Evidence: [CR-082-TIMESERIES-FEATURE-DOC-OWNERSHIP-DRIFT-REVIEW.md](./CR-082-TIMESERIES-FEATURE-DOC-OWNERSHIP-DRIFT-REVIEW.md); `docs/features/timeseries_generator/01_Feature_Timeseries_Generator_Overview.md`; `docs/features/timeseries_generator/03_Methodology_Guide.md`; `docs/features/timeseries_generator/05_Developer_Guide.md`. |
| CR-083 | 2026-03-11 | Integration schema skip cleanup | Hardened | A small set of integration tests still used `pytest.skip(...)` when active schema objects were absent, even though those tables and columns are now part of the supported baseline. That weakened the integration tier and could hide migration regressions. | Replaced the schema-guard skips with explicit assertions for required tables and columns so the integration tests now fail loudly on schema drift instead of silently bypassing coverage. | If future schema evolution introduces optional surfaces, handle that at the fixture/profile level rather than with ad hoc skips inside integration tests. | Evidence: [CR-083-INTEGRATION-SCHEMA-SKIP-CLEANUP-REVIEW.md](./CR-083-INTEGRATION-SCHEMA-SKIP-CLEANUP-REVIEW.md); `tests/integration/services/calculators/cost_calculator_service/test_int_cost_repository_lot_offset.py`; `tests/integration/services/persistence_service/repositories/test_repositories.py`. |
| CR-084 | 2026-03-11 | Analytics export dashboard coverage | Hardened | Analytics export job outcome, duration, result-size, and page-depth metrics already existed, but the bundled Grafana overview did not surface them. Operators therefore had no dashboard-level visibility into export health or payload growth. | Added dashboard panels for analytics export job outcomes and p95 export depth/latency characteristics by dataset type. | If the overview becomes too crowded, move analytics export panels to a dedicated control-plane dashboard rather than removing visibility. | Evidence: [CR-084-ANALYTICS-EXPORT-DASHBOARD-REVIEW.md](./CR-084-ANALYTICS-EXPORT-DASHBOARD-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `src/libs/portfolio-common/portfolio_common/monitoring.py`. |
| CR-085 | 2026-03-11 | Database catalog control-plane usage references | Hardened | The schema catalog still cited `query_control_plane_service/app/main.py` as a table-usage reference in several places. That was too coarse to be useful for ownership tracing because `main.py` is only app assembly, not the actual table-owning router/service surface. | Replaced those references with the actual control-plane ownership paths for simulation, integration, and analytics-input contracts. | Keep the catalog pointed at the owning router/service/repository path rather than generic app entrypoints when future refactors land. | Evidence: [CR-085-DATABASE-CATALOG-CONTROL-PLANE-USAGE-REVIEW.md](./CR-085-DATABASE-CATALOG-CONTROL-PLANE-USAGE-REVIEW.md); `docs/Database-Schema-Catalog.md`. |
| CR-086 | 2026-03-11 | Current-state ownership evidence drift | Hardened | A few current-state docs still used `query_control_plane_service/app/main.py` or other service mains as ownership evidence for behavior that is actually implemented in concrete router modules. That weakened traceability in standards and the RFC index. | Replaced the weak evidence references with the live router-level ownership paths in `durability-consistency.md` and the RFC-081 index row. | Keep future implementation evidence pointed at the concrete owning module unless the startup contract itself is the subject. | Evidence: [CR-086-CURRENT-STATE-OWNERSHIP-EVIDENCE-DRIFT-REVIEW.md](./CR-086-CURRENT-STATE-OWNERSHIP-EVIDENCE-DRIFT-REVIEW.md); `docs/standards/durability-consistency.md`; `docs/RFCs/RFC-INDEX.md`. |
| CR-087 | 2026-03-11 | Stale control epoch emission | Hardened | The durable `FINANCIAL_RECONCILIATION` control-stage row was already monotonic, and the read path already chose the latest epoch. The remaining gap was that an older reconciliation completion could still publish a stale `PortfolioDayControlsEvaluated` event after a newer epoch already existed for the same portfolio-day. | Added latest-epoch resolution to `PipelineStageRepository` and suppressed stale control-event emission in `PipelineOrchestratorService` when the incoming completion is not for the latest epoch. Added unit and integration coverage for the stale-vs-latest epoch invariant. | Apply the same review pattern to any future control-plane event that persists multiple epochs for the same business scope: durable monotonicity is not enough if emission semantics are still stale. | Evidence: [CR-087-STALE-CONTROL-EPOCH-EMISSION-REVIEW.md](./CR-087-STALE-CONTROL-EPOCH-EMISSION-REVIEW.md); `src/services/pipeline_orchestrator_service/app/services/pipeline_orchestrator_service.py`; `src/services/pipeline_orchestrator_service/app/repositories/pipeline_stage_repository.py`; `tests/unit/services/pipeline_orchestrator_service/services/test_pipeline_orchestrator_service.py`; `tests/integration/services/pipeline_orchestrator_service/test_int_pipeline_stage_repository.py`. |
| CR-088 | 2026-03-11 | Stale valuation epoch job creation | Hardened | `ValuationJobRepository.upsert_job(...)` previously accepted any epoch for a given `(portfolio_id, security_id, valuation_date)` scope, so an older readiness event could still create or re-arm obsolete valuation work after a newer epoch already existed. | Added latest-epoch resolution to `ValuationJobRepository` and skipped stale older-epoch upserts when a newer epoch already exists for the same valuation scope. Added unit and DB-backed integration coverage for the stale-vs-latest valuation job invariant. | Apply the same stale-epoch review to any other scheduler/work-set repository that keys work by business scope plus epoch. | Evidence: [CR-088-STALE-VALUATION-EPOCH-JOB-REVIEW.md](./CR-088-STALE-VALUATION-EPOCH-JOB-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_job_repository.py`; `tests/unit/libs/portfolio-common/test_valuation_job_repository.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_valuation_repo.py`. |
| CR-089 | 2026-03-11 | Current-epoch valuation trigger selection | Hardened | The immediate price-event path used `find_open_position_keys_for_security_on_date(...)`, which previously returned any open epoch for a key as of the target date. That meant obsolete historical epochs could still be selected as valuation work candidates even when `PositionState` had advanced the key to a newer current epoch. | Tightened `find_open_position_keys_for_security_on_date(...)` to join against `PositionState` and return only the current epoch for each active key. Added DB-backed integration coverage proving mixed historical epochs resolve to the current epoch only. | Apply the same ownership rule to any work-candidate query: if a key has a canonical current-state owner, candidate generation must align with that owner instead of inferring “latest enough” from history alone. | Evidence: [CR-089-CURRENT-EPOCH-VALUATION-TRIGGER-REVIEW.md](./CR-089-CURRENT-EPOCH-VALUATION-TRIGGER-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_valuation_repo.py`. |
| CR-090 | 2026-03-11 | Epoch-fenced watermark advance | Hardened | `bulk_update_states(...)` updated `PositionState` rows by key only, so a scheduler pass that observed epoch `N` could still overwrite the newer current row after the key had already advanced to epoch `N+1`. This was a stale-writer race on canonical current state, not merely stale job creation. | Added `expected_epoch` fencing to `bulk_update_states(...)`, updated the valuation scheduler implementations to pass the observed epoch through with each watermark/status advance, and added unit coverage proving stale expected epochs do not overwrite newer rows and scheduler updates carry the observed epoch. | Apply the same epoch-fence rule to any other bulk state mutation path that derives writes from an earlier scheduler snapshot. | Evidence: [CR-090-EPOCH-FENCED-WATERMARK-ADVANCE-REVIEW.md](./CR-090-EPOCH-FENCED-WATERMARK-ADVANCE-REVIEW.md); `src/libs/portfolio-common/portfolio_common/position_state_repository.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/libs/portfolio-common/test_position_state_repository.py`. |
| CR-091 | 2026-03-11 | Position valuation worker ownership cleanup | Hardened | `position_valuation_calculator` was already a pure worker runtime, but it still carried dead scheduler, reprocessing-worker, readiness-consumer, price-event-consumer, and worker-local settings files from before the valuation split. These paths were no longer wired into the service runtime and risked future fixes landing in the wrong service. | Deleted the dead orchestration/runtime residue and the orphaned worker-local tests, corrected current-state schema catalog references to the live `valuation_orchestrator_service` ownership surface, fixed stale copied path headers in the surviving orchestrator files, and restored the still-valid consumer/scheduler/reprocessing tests under `tests/unit/services/valuation_orchestrator_service/` so coverage follows the live owner. | Keep `position_valuation_calculator` worker-only. Any future scheduling or replay logic belongs in `valuation_orchestrator_service`, not the worker service. | Evidence: [CR-091-POSITION-VALUATION-WORKER-OWNERSHIP-CLEANUP.md](./CR-091-POSITION-VALUATION-WORKER-OWNERSHIP-CLEANUP.md); `src/services/calculators/position_valuation_calculator/app/consumer_manager.py`; `src/services/valuation_orchestrator_service/app/consumer_manager.py`; `tests/unit/services/valuation_orchestrator_service/consumers/*`; `tests/unit/services/valuation_orchestrator_service/core/*`; `docs/Database-Schema-Catalog.md`. |
| CR-092 | 2026-03-11 | Stale reprocessing job recovery | Hardened | `reprocessing_jobs` tracked attempt metadata but had no stale `PROCESSING` reset path. A dead `ReprocessingWorker` could therefore leave `RESET_WATERMARKS` jobs stuck forever, unlike valuation and aggregation jobs which already had stale-processing recovery. | Added `ReprocessingJobRepository.find_and_reset_stale_jobs(timeout_minutes=15)`, wired `ReprocessingWorker` to run that recovery step before claiming the next batch, and added unit coverage for repository stale-reset behavior and worker ordering. | If reprocessing job types expand later, keep them on the same durable stale-reset contract rather than adding type-specific recovery behavior ad hoc. | Evidence: [CR-092-STALE-REPROCESSING-JOB-RECOVERY-REVIEW.md](./CR-092-STALE-REPROCESSING-JOB-RECOVERY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `tests/unit/libs/portfolio-common/test_reprocessing_job_repository.py`; `tests/unit/services/valuation_orchestrator_service/core/test_reprocessing_worker.py`. |
| CR-093 | 2026-03-11 | Position state mutation timestamp integrity | Hardened | `PositionState` is the canonical replay/watermark state table, but its two replay-critical mutation paths did not explicitly refresh `updated_at`. That left timestamp integrity dependent on implicit ORM/update behavior rather than explicit state mutation intent. | Added `updated_at=func.now()` to `increment_epoch_and_reset_watermark(...)` and `update_watermarks_if_older(...)`, and extended DB-backed repository tests to assert the mutated rows keep a populated timestamp. | Keep current-state timestamp fields explicit on all future repository mutation statements instead of relying on implicit column `onupdate` behavior. | Evidence: [CR-093-POSITION-STATE-MUTATION-TIMESTAMP-INTEGRITY-REVIEW.md](./CR-093-POSITION-STATE-MUTATION-TIMESTAMP-INTEGRITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/position_state_repository.py`; `tests/unit/libs/portfolio-common/test_position_state_repository.py`. |
| CR-094 | 2026-03-11 | Position consumer replay boundary | Hardened | `TransactionEventConsumer` already distinguished stage-gate payloads from replay/original transaction payloads, but there was no explicit unit-level proof that replay payloads bypass canonical transaction lookup while preserving replay epoch and idempotency behavior. | Added consumer-level tests proving the replay path bypasses canonical lookup, preserves the incoming epoch, and still marks idempotency on the direct replay contract path. | Keep future consumer changes aligned to the same boundary: stage-gate events may hydrate canonical rows; replay payloads should not. | Evidence: [CR-094-POSITION-CONSUMER-REPLAY-BOUNDARY-REVIEW.md](./CR-094-POSITION-CONSUMER-REPLAY-BOUNDARY-REVIEW.md); `src/services/calculators/position_calculator/app/consumers/transaction_event_consumer.py`; `tests/unit/services/calculators/position_calculator/consumers/test_position_calculator_consumer.py`. |
| CR-095 | 2026-03-11 | Replay correlation propagation | Hardened | `TransactionEventConsumer.process_message(...)` assumed the Kafka runtime had already populated `correlation_id_var`. That was true inside `BaseConsumer.run()`, but false for direct consumer invocation paths, so replay-path idempotency records could fall back to `"<not-set>"` even when message headers already carried a real `correlation_id`. | Extracted reusable header resolution into `BaseConsumer._resolve_message_correlation_id(...)`, updated `TransactionEventConsumer` to set/reset correlation context locally when direct paths do not already have it, and added tests proving replay and stage-gate direct execution persist the Kafka header correlation. | Apply the same pattern to any other consumer whose `process_message(...)` can be invoked directly outside `BaseConsumer.run()`; direct-path audit fidelity should not depend on runtime-loop side effects. | Evidence: [CR-095-REPLAY-CORRELATION-PROPAGATION-REVIEW.md](./CR-095-REPLAY-CORRELATION-PROPAGATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/kafka_consumer.py`; `src/services/calculators/position_calculator/app/consumers/transaction_event_consumer.py`; `tests/unit/services/calculators/position_calculator/consumers/test_position_calculator_consumer.py`. |
| CR-096 | 2026-03-11 | Direct consumer correlation context | Hardened | The same direct-path correlation assumption from CR-095 existed more broadly in control-plane and valuation consumers: they read `correlation_id_var` inside `process_message(...)` without ensuring the context had been initialized locally. That could silently degrade audit/idempotency fidelity on direct invocation paths. | Added shared `BaseConsumer._message_correlation_context(...)` to unify current-context reuse, header resolution, deterministic fallback ids, and payload-correlation preference. Applied it to the reviewed high-value control-plane and valuation consumers and extended direct consumer tests accordingly. | Continue applying the same helper to any remaining consumer whose `process_message(...)` is intentionally invoked directly in tests or in-process replay/control paths. | Evidence: [CR-096-DIRECT-CONSUMER-CORRELATION-CONTEXT-REVIEW.md](./CR-096-DIRECT-CONSUMER-CORRELATION-CONTEXT-REVIEW.md); `src/libs/portfolio-common/portfolio_common/kafka_consumer.py`; reviewed consumer files under `valuation_orchestrator_service`, `financial_reconciliation_service`, and `pipeline_orchestrator_service`; corresponding unit tests. |
| CR-097 | 2026-03-11 | Watermark advance mismatch visibility | Hardened | Epoch fencing in `bulk_update_states(...)` prevents stale overwrites, but the valuation scheduler still treated partial advancement as routine success. That made stale-epoch skips safe but operationally invisible. | Updated `ValuationScheduler._advance_watermarks(...)` to warn when fewer rows are advanced than were prepared, including prepared/updated/skipped counts and sample keys. Added unit coverage for the partial-advance warning path. | If later metrics work is needed, expose the stale-skipped count as explicit telemetry. The immediate observability gap is now closed at log/test level. | Evidence: [CR-097-WATERMARK-ADVANCE-MISMATCH-VISIBILITY-REVIEW.md](./CR-097-WATERMARK-ADVANCE-MISMATCH-VISIBILITY-REVIEW.md); `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py`. |
| CR-098 | 2026-03-11 | Terminal reprocessing normalization | Hardened | The valuation scheduler only considered lagging keys (`watermark_date < latest_business_date`), so a key could remain `status="REPROCESSING"` even after its watermark had already reached the latest business date. That left stale control state and undercounted the reprocessing gauge. | Added `get_terminal_reprocessing_states(...)` to the valuation repository and updated the scheduler to normalize terminal `REPROCESSING` rows back to `CURRENT` via epoch-fenced bulk updates. Added unit coverage for the normalization path. | If later operational reporting needs to distinguish genuinely active reprocessing from just-normalized terminal residue, expose the normalized count as explicit telemetry rather than inferring it indirectly. | Evidence: [CR-098-TERMINAL-REPROCESSING-NORMALIZATION-REVIEW.md](./CR-098-TERMINAL-REPROCESSING-NORMALIZATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py`. |
| CR-099 | 2026-03-12 | Terminal reprocessing normalization visibility | Hardened | CR-098 corrected stale terminal `REPROCESSING` rows by normalizing them back to `CURRENT`, but the normalization path still treated partial success as routine. Because `bulk_update_states(...)` is epoch-fenced, terminal normalization can legitimately skip stale rows once a newer epoch already exists. That is safe, but leaving it silent weakens runtime diagnosability in the same way CR-097 previously did for watermark advancement. | Updated `ValuationScheduler._advance_watermarks(...)` to warn when fewer terminal reprocessing states are normalized than were prepared, including prepared count, updated count, stale-skipped count, and sample keys. Added unit coverage for the partial-normalization warning path. | If this path becomes operationally noisy, surface the skipped count as explicit telemetry rather than removing the warning. | Evidence: [CR-099-TERMINAL-REPROCESSING-VISIBILITY-REVIEW.md](./CR-099-TERMINAL-REPROCESSING-VISIBILITY-REVIEW.md); `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py`. |
| CR-100 | 2026-03-12 | Reprocessing fan-out stale skip visibility | Hardened | `ReprocessingWorker` fans out watermark resets across all portfolios holding a security, but its success logging treated partial updates as routine success. Because `PositionStateRepository.update_watermarks_if_older(...)` can legitimately skip rows when newer or already-older state exists, epoch/current-state fencing could silently reduce the actual fan-out without any runtime signal. | Updated `ReprocessingWorker._process_batch()` to warn when fewer watermark resets are applied than were targeted, including targeted count, updated count, stale-skipped count, and sample keys. Added unit coverage for the partial fan-out warning path. | If this path becomes noisy in production, expose the skipped count as explicit telemetry instead of removing the warning. | Evidence: [CR-100-REPROCESSING-FANOUT-VISIBILITY-REVIEW.md](./CR-100-REPROCESSING-FANOUT-VISIBILITY-REVIEW.md); `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `tests/unit/services/valuation_orchestrator_service/core/test_reprocessing_worker.py`. |
| CR-101 | 2026-03-12 | Back-dated replay duplicate-trigger deduplication | Hardened | When a back-dated original transaction triggered epoch bump and replay, `PositionCalculator.calculate(...)` always appended the triggering event after loading historical transactions from the database. If the triggering transaction had already been persisted to the canonical transaction table, it was replayed twice in the same epoch. | Deduplicated the replay batch by `transaction_id` before appending the triggering event, preserving deterministic ordering while preventing double replay of the same canonical transaction. Added unit coverage for the persisted-trigger replay case. | If future replay flows can legitimately emit the same `transaction_id` with different semantic payloads, deduplication must move to a stronger replay-key contract. For the current canonical transaction model, `transaction_id` is the correct replay identity. | Evidence: [CR-101-BACKDATED-REPLAY-DEDUP-REVIEW.md](./CR-101-BACKDATED-REPLAY-DEDUP-REVIEW.md); `src/services/calculators/position_calculator/app/core/position_logic.py`; `tests/unit/services/calculators/position_calculator/core/test_position_logic.py`. |
| CR-102 | 2026-03-12 | DB-backed replay deduplication proof | Hardened | CR-101 fixed duplicate replay of a persisted triggering transaction at the unit level, but the invariant still needed proof on the real atomic outbox path. Without a DB-backed assertion, the system could regress while still appearing correct under mocks. | Added an integration test that persists the triggering back-dated transaction, runs the real atomic replay path, and asserts the outbox batch contains each `transaction_id` only once. | If future replay batching evolves beyond transaction identity, extend the proof to the stronger replay-key contract rather than relying only on `transaction_id` uniqueness. | Evidence: [CR-102-DB-BACKED-REPLAY-DEDUP-PROOF.md](./CR-102-DB-BACKED-REPLAY-DEDUP-PROOF.md); `tests/integration/services/calculators/position_calculator/test_int_reprocessing_atomicity.py`. |
| CR-103 | 2026-03-12 | Replay outbox correlation preservation | Hardened | The back-dated replay path created durable outbox events without explicitly propagating the active correlation context into the outbox row. That meant the replay payload could be correct while the durable outbox lineage still lost the triggering correlation id. | Updated `PositionCalculator.calculate(...)` to persist the active `correlation_id_var` value on replay outbox events when present, and added both unit and DB-backed integration coverage proving the outbox rows retain that correlation. | Apply the same durability test pattern to any other outbox-backed replay or fan-out path that currently relies on ambient context rather than explicit outbox correlation propagation. | Evidence: [CR-103-REPLAY-OUTBOX-CORRELATION-REVIEW.md](./CR-103-REPLAY-OUTBOX-CORRELATION-REVIEW.md); `src/services/calculators/position_calculator/app/core/position_logic.py`; `tests/unit/services/calculators/position_calculator/core/test_position_logic.py`; `tests/integration/services/calculators/position_calculator/test_int_reprocessing_atomicity.py`. |
| CR-104 | 2026-03-12 | Dead timeseries scheduler residue | Hardened | After the RFC 81 service split, `timeseries_generator_service` still carried a dead `AggregationScheduler` copy and test even though scheduling ownership had moved to `portfolio_aggregation_service`. The file was no longer imported by production code and only blurred ownership. | Deleted the dead scheduler and its unit test from `timeseries_generator_service`, and corrected current-state feature docs so they attribute aggregation scheduling to `portfolio_aggregation_service`. | Keep scheduler ownership singular. Future aggregation scheduling changes should only land in `portfolio_aggregation_service`. | Evidence: [CR-104-DEAD-TIMESERIES-SCHEDULER-RESIDUE-REVIEW.md](./CR-104-DEAD-TIMESERIES-SCHEDULER-RESIDUE-REVIEW.md); deleted `src/services/timeseries_generator_service/app/core/aggregation_scheduler.py`; deleted `tests/unit/services/timeseries_generator_service/timeseries-generator-service/core/test_aggregation_scheduler.py`; updated `docs/features/timeseries_generator/02_API_Specification_Timeseries_Generator.md`; updated `docs/features/timeseries_generator/04_Operations_Troubleshooting_Guide.md`. |
| CR-105 | 2026-03-12 | Calculator consumer direct-path correlation context | Hardened | Three calculator consumers could be invoked directly without `BaseConsumer.run()`, but still assumed `correlation_id_var` had already been initialized by the runtime loop. That weakened durable outbox/idempotency lineage on direct execution paths. | Applied `_message_correlation_context(...)` to the cashflow, cost, and position-valuation consumers so they resolve Kafka header correlation ids locally on direct paths. Also corrected the `cost_calculator_service` success path so `mark_event_processed(...)` runs after successful publication even when no instrument events exist. Added targeted unit coverage across all three consumers. | Continue applying the same rule to any other direct-invocation consumer path: durable audit fields must not depend on ambient runtime context. | Evidence: [CR-105-CALCULATOR-CONSUMER-CORRELATION-CONTEXT-REVIEW.md](./CR-105-CALCULATOR-CONSUMER-CORRELATION-CONTEXT-REVIEW.md); `src/services/calculators/cashflow_calculator_service/app/consumers/transaction_consumer.py`; `src/services/calculators/cost_calculator_service/app/consumer.py`; `src/services/calculators/position_valuation_calculator/app/consumers/valuation_consumer.py`; calculator consumer unit tests. |
| CR-106 | 2026-03-12 | Portfolio aggregation consumer ownership and correlation context | Hardened | `PortfolioTimeseriesConsumer` still existed in both `portfolio_aggregation_service` and `timeseries_generator_service`, even though only `portfolio_aggregation_service` owned it at runtime. The duplicate implementation and test ownership drift created a real risk of future fixes landing in the dead copy. | Deleted the dead duplicate consumer from `timeseries_generator_service`, moved the unit tests under `portfolio_aggregation_service`, corrected stale file headers, and applied `_message_correlation_context(...)` to the live portfolio aggregation consumer and the adjacent position-timeseries worker so direct invocation preserves Kafka header correlation ids. | Keep aggregation consumer ownership singular. Portfolio aggregation belongs only to `portfolio_aggregation_service`; `timeseries_generator_service` should remain position-timeseries only. | Evidence: [CR-106-PORTFOLIO-AGGREGATION-CONSUMER-OWNERSHIP-REVIEW.md](./CR-106-PORTFOLIO-AGGREGATION-CONSUMER-OWNERSHIP-REVIEW.md); `src/services/portfolio_aggregation_service/app/consumers/portfolio_timeseries_consumer.py`; deleted `src/services/timeseries_generator_service/app/consumers/portfolio_timeseries_consumer.py`; `tests/unit/services/portfolio_aggregation_service/consumers/test_portfolio_timeseries_consumer.py`; `tests/unit/services/timeseries_generator_service/timeseries-generator-service/consumers/test_position_timeseries_consumer.py`. |
| CR-107 | 2026-03-12 | Persistence consumer direct-path correlation context | Hardened | `GenericPersistenceConsumer` still relied on ambient `correlation_id_var` state for durable outbox/idempotency writes, even though persistence consumers are invoked directly in unit and in-process paths. That weakened durable lineage across the persistence layer. | Updated the shared base consumer to use `_message_correlation_context(...)` and strengthened representative persistence consumer tests so direct invocation preserves the Kafka header correlation id in both outbox publication and `mark_event_processed(...)`. | Keep persistence correlation handling centralized in the base class. Future subtype fixes should not bypass the shared contract. | Evidence: [CR-107-PERSISTENCE-CONSUMER-CORRELATION-CONTEXT-REVIEW.md](./CR-107-PERSISTENCE-CONSUMER-CORRELATION-CONTEXT-REVIEW.md); `src/services/persistence_service/app/consumers/base_consumer.py`; `tests/unit/services/persistence_service/consumers/test_persistence_transaction_consumer.py`; `tests/unit/services/persistence_service/consumers/test_persistence_instrument_consumer.py`; `tests/unit/services/persistence_service/consumers/test_persistence_market_price_consumer.py`. |
| CR-108 | 2026-03-12 | Shared direct-consumer correlation helper convergence | Hardened | `PriceEventConsumer` and `TransactionEventConsumer` still implemented direct-path correlation handling manually even after the shared helper existed. That preserved correct behavior but left the contract inconsistent by implementation, increasing drift risk. | Replaced the manual context setup in both consumers with `_message_correlation_context(...)` and verified behavior against the existing direct-path unit suites. | Keep direct consumer correlation handling on the shared helper path. Avoid manual `ContextVar` token management in individual consumers unless there is a demonstrated exception. | Evidence: [CR-108-SHARED-CORRELATION-CONTEXT-CONVERGENCE-REVIEW.md](./CR-108-SHARED-CORRELATION-CONTEXT-CONVERGENCE-REVIEW.md); `src/services/valuation_orchestrator_service/app/consumers/price_event_consumer.py`; `src/services/calculators/position_calculator/app/consumers/transaction_event_consumer.py`; corresponding unit tests. |
| CR-109 | 2026-03-12 | Cost reprocessing consumer direct-path correlation context | Hardened | `cost_calculator_service` still had one unreviewed direct-invocation consumer: `ReprocessingConsumer`. It republished transactions through `ReprocessingRepository`, but its `process_message(...)` path still relied on ambient `correlation_id_var` state instead of explicitly adopting the Kafka header correlation id. | Wrapped the consumer body in `_message_correlation_context(...)` and added unit coverage proving the repository executes under the header-provided correlation id during direct invocation. | Keep any future replay/reprocessing consumer on the same shared correlation-context contract. Direct paths must not depend on `BaseConsumer.run()` side effects for durable audit lineage. | Evidence: [CR-109-COST-REPROCESSING-CONSUMER-CORRELATION-CONTEXT-REVIEW.md](./CR-109-COST-REPROCESSING-CONSUMER-CORRELATION-CONTEXT-REVIEW.md); `src/services/calculators/cost_calculator_service/app/consumers/reprocessing_consumer.py`; `tests/unit/services/calculators/cost_calculator_service/consumer/test_reprocessing_consumer.py`. |
| CR-110 | 2026-03-12 | Set-based `position_state` bulk updates | Hardened | `PositionStateRepository.bulk_update_states(...)` was still issuing one SQL `UPDATE` per row. That preserved correctness, but it was not a gold-standard shape for a hot control table mutated by the valuation scheduler and replay normalization paths. | Replaced the row-by-row loop with a single set-based epoch-fenced update using a derived values table, while preserving the `(portfolio_id, security_id, expected_epoch)` fence and explicit `updated_at = now()` semantics. Added multi-row timestamp integrity coverage. | Keep future bulk control-table mutations set-based unless there is a specific reason not to. Per-row loops on scheduler hot paths should be treated as a performance smell by default. | Evidence: [CR-110-SET-BASED-POSITION-STATE-BULK-UPDATE-REVIEW.md](./CR-110-SET-BASED-POSITION-STATE-BULK-UPDATE-REVIEW.md); `src/libs/portfolio-common/portfolio_common/position_state_repository.py`; `tests/unit/libs/portfolio-common/test_position_state_repository.py`. |
| CR-111 | 2026-03-12 | Reprocessing request ordering determinism | Hardened | `ReprocessingRepository.reprocess_transactions_by_ids(...)` queried `transaction_id IN (...)` with no ordering. That made multi-transaction replay requests nondeterministic even though the API accepts an ordered list of transaction ids. | Added explicit ordering by caller-provided `transaction_ids` and added unit coverage proving republished message order matches request order. | Preserve deterministic replay order whenever a replay API accepts multiple ids or events. Row-return order from the database is not a valid replay contract. | Evidence: [CR-111-REPROCESSING-REQUEST-ORDERING-REVIEW.md](./CR-111-REPROCESSING-REQUEST-ORDERING-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_repository.py`. |
| CR-112 | 2026-03-12 | Reprocessing request duplicate-ID deduplication | Hardened | CR-111 fixed deterministic ordering for multi-transaction replay requests, but the replay API still accepted duplicate transaction IDs and would republish the same canonical transaction multiple times in one request. That is a real replay-correctness gap, not just inefficiency. | Deduplicated caller-provided `transaction_ids` while preserving first-seen order before querying and republishing, and added unit coverage proving duplicate IDs result in one republish per canonical transaction in deterministic order. | Keep replay request handling idempotent at the API boundary. If future replay workflows need explicit duplicate semantics, define that as a separate contract rather than falling out of raw input lists. | Evidence: [CR-112-REPROCESSING-REQUEST-DEDUP-REVIEW.md](./CR-112-REPROCESSING-REQUEST-DEDUP-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_repository.py`. |
| CR-113 | 2026-03-12 | Replay API correlation header hygiene | Hardened | The replay API boundary now preserved deterministic ordering and deduplicated duplicate transaction ids, but it still treated the default `correlation_id_var` value `"<not-set>"` as a real correlation header. That could republish replay messages with poisoned lineage metadata even though no real correlation id had been established. | Normalized `"<not-set>"` to `None` before constructing Kafka headers and added unit coverage proving replay publication omits the correlation header when the ambient context is unset. | Apply the same normalization rule to any remaining publication paths that still convert ambient correlation context directly into durable headers. | Evidence: [CR-113-REPLAY-API-CORRELATION-HYGIENE-REVIEW.md](./CR-113-REPLAY-API-CORRELATION-HYGIENE-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_repository.py`. |
| CR-114 | 2026-03-12 | Write-plane lineage sentinel normalization | Hardened | The ingestion write plane still treated the default context sentinel `"<not-set>"` as real lineage metadata. That could leak into Kafka headers and acknowledgement/request-lineage payloads even when no real correlation, request, or trace id had been established. | Normalized `"<not-set>"` to `None` in `IngestionService._get_headers(...)` and in `get_request_lineage()`, then added unit coverage for both the Kafka-header path and the shared request-lineage helper. | Apply the same sentinel-normalization rule to any other remaining API-layer helpers that echo ambient lineage context directly into durable or client-visible metadata. | Evidence: [CR-114-WRITE-PLANE-LINEAGE-SENTINEL-NORMALIZATION-REVIEW.md](./CR-114-WRITE-PLANE-LINEAGE-SENTINEL-NORMALIZATION-REVIEW.md); `src/services/ingestion_service/app/services/ingestion_service.py`; `src/services/ingestion_service/app/request_metadata.py`; ingestion-service unit tests. |
| CR-115 | 2026-03-12 | Enterprise readiness lineage sentinel normalization | Hardened | Both enterprise-readiness middleware implementations still treated the default ambient context sentinel `"<not-set>"` as a real correlation id. That meant deny/write audit events could record poisoned client-visible lineage metadata even when no real correlation id had been established. | Added correlation normalization in both enterprise-readiness implementations so `None`, empty string, and `"<not-set>"` collapse to `None` before audit emission, and added unit coverage for deny and successful write audit paths in both owners. | Apply the same normalization rule to any remaining middleware or API helpers that echo ambient lineage state into logs, audits, or client-visible metadata. | Evidence: [CR-115-ENTERPRISE-READINESS-LINEAGE-NORMALIZATION-REVIEW.md](./CR-115-ENTERPRISE-READINESS-LINEAGE-NORMALIZATION-REVIEW.md); query-service and query-control-plane enterprise readiness modules; corresponding unit tests. |
| CR-116 | 2026-03-12 | Reprocessing ingress duplicate suppression | Hardened | The replay API consumer and repository were already being hardened for deterministic ordering and duplicate suppression, but the ingress router still accepted duplicate transaction IDs and published duplicate `transactions_reprocessing_requested` commands before the downstream layers saw them. | Deduplicated caller-provided `transaction_ids` at ingress while preserving first-seen order, and used the deduplicated list consistently for rate limiting, job accepted count, persisted request payload, publication, and failure bookkeeping. Added router-level integration coverage proving duplicate IDs produce one command per canonical transaction. | Keep replay request deduplication at the earliest boundary possible. Downstream consumers may still defend themselves, but ingress should not knowingly inject duplicate commands. | Evidence: [CR-116-REPROCESSING-INGRESS-DUPLICATE-SUPPRESSION-REVIEW.md](./CR-116-REPROCESSING-INGRESS-DUPLICATE-SUPPRESSION-REVIEW.md); `src/services/ingestion_service/app/routers/reprocessing.py`; `tests/integration/services/ingestion_service/test_ingestion_routers.py`. |
| CR-117 | 2026-03-12 | Reset-watermarks durable job coalescing | Hardened | Repeated instrument-level reprocessing triggers for the same security could create multiple pending `RESET_WATERMARKS` jobs even before the worker claimed the first one. That inflated the durable queue with redundant work and weakened recovery determinism. | `ReprocessingJobRepository.create_job(...)` now coalesces pending `RESET_WATERMARKS` jobs by `security_id`, updating the pending job to the earliest impacted date instead of creating duplicates. Added unit coverage for both same/later-date coalescing and earlier-date merge behavior. | If duplicate fanout pressure still shows up under production load, consider a database-level uniqueness contract for pending reset-watermarks jobs keyed by `(job_type, payload->>security_id)` instead of relying only on repository behavior. | Evidence: [CR-117-RESET-WATERMARKS-JOB-COALESCING-REVIEW.md](./CR-117-RESET-WATERMARKS-JOB-COALESCING-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_job_repository.py`. |
| CR-118 | 2026-03-12 | Instrument reprocessing trigger priority order | Hardened | Instrument-level replay triggers were claimed by `updated_at` only. Under backlog, that lets row recency override the actual business priority, which is the oldest impacted date. | `ValuationRepositoryBase.get_instrument_reprocessing_triggers(...)` now orders by `earliest_impacted_date`, then `updated_at`, then `security_id`, and a DB-backed integration test proves the scheduler-facing order is oldest-impact-first. | If replay backlog becomes large enough to need explicit fairness guarantees, consider exposing queue-age and oldest-impacted-date telemetry directly rather than relying only on deterministic ordering. | Evidence: [CR-118-INSTRUMENT-REPROCESSING-PRIORITY-ORDER-REVIEW.md](./CR-118-INSTRUMENT-REPROCESSING-PRIORITY-ORDER-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_instrument_reprocessing_repo.py`. |
| CR-119 | 2026-03-12 | Reset-watermarks worker claim priority order | Hardened | After CR-118, instrument-level triggers were prioritized by oldest impacted date, but the durable `RESET_WATERMARKS` worker queue still claimed work by `created_at` only. That left the replay pipeline with inconsistent priority once work crossed the durable queue boundary. | `ReprocessingJobRepository.find_and_claim_jobs(...)` now claims `RESET_WATERMARKS` jobs by `(payload->>'earliest_impacted_date')::date`, then `created_at`, then `id`, while leaving other job types on the generic created-at order. Added repository tests locking both query branches. | If the durable queue later needs stronger starvation protection across job types, move from job-type-specific ordering to an explicit priority model instead of relying on implicit SQL branches. | Evidence: [CR-119-RESET-WATERMARKS-WORKER-CLAIM-PRIORITY-REVIEW.md](./CR-119-RESET-WATERMARKS-WORKER-CLAIM-PRIORITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_job_repository.py`. |
| CR-120 | 2026-03-12 | Dead instrument reprocessing repository ownership | Hardened | `InstrumentReprocessingStateRepository` still existed in both `valuation_orchestrator_service` and `position_valuation_calculator`, but only the valuation orchestrator copy was live. The worker-local duplicate represented dead alternate ownership for an active replay-trigger contract. | Deleted the dead `position_valuation_calculator` copy and kept ownership singular under `valuation_orchestrator_service`, where back-dated price-event replay triggers actually originate. | Keep instrument-level replay trigger ownership singular. If the repository ever becomes truly shared again, move it to a real shared package instead of restoring service-local duplicates. | Evidence: [CR-120-DEAD-INSTRUMENT-REPROCESSING-REPOSITORY-OWNERSHIP-REVIEW.md](./CR-120-DEAD-INSTRUMENT-REPROCESSING-REPOSITORY-OWNERSHIP-REVIEW.md); deleted `src/services/calculators/position_valuation_calculator/app/repositories/instrument_reprocessing_state_repository.py`; live owner remains `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`. |
| CR-121 | 2026-03-12 | Historical reset-watermarks duplicate normalization | Hardened | CR-117 prevented new duplicate pending `RESET_WATERMARKS` jobs through the repository, but historical duplicates could still already exist in the durable queue and would eventually be processed redundantly by the worker. | Added `normalize_pending_reset_watermarks_duplicates()` and invoked it before `RESET_WATERMARKS` claim. The queue now keeps one pending row per `security_id`, updates the keeper to the minimum impacted date, and deletes redundant pending rows. Added repository tests that lock the normalization and claim sequencing. | If duplicate residue still matters operationally, surface the deleted-count as explicit replay queue telemetry rather than relying only on the log line. | Evidence: [CR-121-HISTORICAL-RESET-WATERMARKS-DUPLICATE-NORMALIZATION-REVIEW.md](./CR-121-HISTORICAL-RESET-WATERMARKS-DUPLICATE-NORMALIZATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_job_repository.py`. |
| CR-122 | 2026-03-12 | DB-backed reset-watermarks normalization proof | Hardened | CR-121 hardened the repository logic, but the durable queue behavior still needed proof against the real database path. Without that, normalization remained a unit-level claim rather than a demonstrated SQL contract. | Added DB-backed integration coverage proving duplicate pending `RESET_WATERMARKS` rows collapse to one row per security with the earliest impacted date, while other job types remain untouched by the normalization logic. | Keep durable replay queue semantics proven below the worker layer. If the SQL changes again, this proof should fail before E2E is needed. | Evidence: [CR-122-DB-BACKED-RESET-WATERMARKS-NORMALIZATION-PROOF.md](./CR-122-DB-BACKED-RESET-WATERMARKS-NORMALIZATION-PROOF.md); `tests/integration/services/calculators/position_valuation_calculator/test_int_reprocessing_job_repository.py`. |
| CR-123 | 2026-03-12 | Instrument trigger coalescing DB proof | Hardened | The live `InstrumentReprocessingStateRepository` is the durable source of instrument-level replay triggers, but its coalescing behavior had not been proven against the real database path. Repeated back-dated price events could have regressed into multiple rows or widened impacted dates without being caught below scheduler/E2E level. | Added DB-backed integration coverage proving repeated upserts leave one trigger row per security and preserve the earliest impacted date, and corrected the stale module header so the file reflects its real owner path. | Keep the trigger-source contract proven below the scheduler layer. If the trigger model evolves, extend these tests rather than relying on scheduler behavior to catch source drift. | Evidence: [CR-123-INSTRUMENT-TRIGGER-COALESCING-DB-PROOF.md](./CR-123-INSTRUMENT-TRIGGER-COALESCING-DB-PROOF.md); `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; `tests/integration/services/valuation_orchestrator_service/test_instrument_reprocessing_state_repository.py`. |
| CR-124 | 2026-03-12 | Fenced instrument trigger deletion | Hardened | `ValuationScheduler` converted fetched instrument-level triggers into durable `RESET_WATERMARKS` jobs and then deleted trigger rows by `security_id` only. A newer back-dated price event could lower the impacted date between fetch and delete, letting an older scheduler cycle erase the newer replay requirement. | Changed trigger deletion to use `(security_id, earliest_impacted_date)` fences, updated the scheduler to pass the fetched trigger state instead of raw ids, and added DB-backed proof that a stale delete cannot remove a trigger row whose impacted date was lowered after fetch. | Keep trigger consumption fenced by fetched state, not just identity. If trigger rows later gain more mutable state, extend the fence rather than regressing to identity-only deletes. | Evidence: [CR-124-FENCED-INSTRUMENT-TRIGGER-DELETION-REVIEW.md](./CR-124-FENCED-INSTRUMENT-TRIGGER-DELETION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_instrument_reprocessing_repo.py`. |
| CR-125 | 2026-03-12 | Pending reset-watermarks uniqueness | Hardened | Duplicate pending `RESET_WATERMARKS` work was already coalesced and normalized in repository logic, but the durable queue still relied on Python behavior rather than schema enforcement. A direct insert path or future regression could still reintroduce duplicate pending replay jobs for the same security. | Added an Alembic migration that normalizes historical duplicate pending rows and creates a partial unique index on pending `RESET_WATERMARKS` jobs by `payload->>'security_id'`. Added DB-backed integration coverage proving the database rejects a second pending row for the same security. | Keep replay-critical uniqueness in the schema where possible. Repository coalescing should remain as the ergonomic path, but the durable queue invariant must now be treated as a DB contract. | Evidence: [CR-125-PENDING-RESET-WATERMARKS-UNIQUENESS-REVIEW.md](./CR-125-PENDING-RESET-WATERMARKS-UNIQUENESS-REVIEW.md); `alembic/versions/e4f5a6b7c8d9_feat_add_pending_reset_watermarks_uniqueness.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_reprocessing_job_repository.py`. |
| CR-126 | 2026-03-12 | Atomic reset-watermarks creation | Hardened | CR-125 strengthened the queue by enforcing uniqueness in the database, but `ReprocessingJobRepository.create_job(...)` still used a read-then-insert/update pattern. Under concurrency, that could race into an integrity failure instead of coalescing replay intent. | Replaced the read-then-write path with one SQL upsert that inserts or merges a pending `RESET_WATERMARKS` job and returns the surviving row. Added unit and DB-backed integration coverage proving repeated creation leaves one pending row with the earliest impacted date. | Keep schema-backed uniqueness paired with atomic repository behavior. Once the database owns the invariant, repository code should use one atomic write path rather than a best-effort read-modify-write sequence. | Evidence: [CR-126-ATOMIC-RESET-WATERMARKS-CREATION-REVIEW.md](./CR-126-ATOMIC-RESET-WATERMARKS-CREATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `tests/unit/libs/portfolio-common/test_reprocessing_job_repository.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_reprocessing_job_repository.py`. |
| CR-127 | 2026-03-12 | Typed instrument trigger upsert | Hardened | The instrument trigger source was already atomic, but it still embedded the incoming impacted date inside raw SQL text for the `LEAST(...)` merge expression. That was weaker than the queue path we just hardened and left an avoidable raw-SQL seam on an active replay boundary. | Replaced the string-built `LEAST(...)` expression with a typed SQLAlchemy `func.least(...)` upsert using `stmt.excluded.earliest_impacted_date`, while preserving explicit `updated_at = now()` semantics. Re-ran the DB-backed trigger repository proof to verify behavior stayed correct. | Keep replay-trigger upserts typed and explicit. Atomicity alone is not enough; the merge expression should be structurally owned in code rather than assembled from raw SQL fragments. | Evidence: [CR-127-TYPED-INSTRUMENT-TRIGGER-UPSERT-REVIEW.md](./CR-127-TYPED-INSTRUMENT-TRIGGER-UPSERT-REVIEW.md); `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; `tests/integration/services/valuation_orchestrator_service/test_instrument_reprocessing_state_repository.py`. |
| CR-128 | 2026-03-12 | Latest processing-date fallback for scheduler normalization | Hardened | The valuation scheduler assumed `business_dates` always exists. In real workflows, valuation jobs and snapshots can complete without that table being seeded, leaving `get_latest_business_date()` as `None`. That made watermark advancement and terminal `REPROCESSING -> CURRENT` normalization a no-op even though the data plane was already complete. | Changed `ValuationRepository.get_latest_business_date()` to fall back to the latest observed valuation-processing date from `daily_position_snapshots` and `portfolio_valuation_jobs` when the business calendar is empty. Added DB-backed repository proof and reran the exact AVCO -> cashflow E2E sequence that had been leaking `REPROCESSING` state across module cleanup. | Use the business calendar as the primary source of truth, but do not let an empty calendar strand durable replay state when actual valuation work has already completed. Scheduler normalization needs a deterministic latest-processing-date fallback. | Evidence: [CR-128-LATEST-PROCESSING-DATE-FALLBACK-REVIEW.md](./CR-128-LATEST-PROCESSING-DATE-FALLBACK-REVIEW.md); `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_valuation_repo.py`; `tests/e2e/test_5_day_workflow.py`; `tests/e2e/test_avco_workflow.py`; `tests/e2e/test_cashflow_pipeline.py`. |
| CR-129 | 2026-03-12 | Quiescence active-row activity window | Hardened | The quiescence snapshot already excluded non-blocking work correctly, but the quiet-window timestamp still read the max `updated_at` across whole blocking tables. That let historical completed rows hold the quiet window open even when the live blocking snapshot was all zeros, causing false E2E cleanup timeouts. | Changed `read_pipeline_last_activity_at()` to apply the same blocking predicates used by the live snapshot when computing last activity time, and added unit proof that non-blocking and inactive rows do not influence the quiet window. | Keep quiescence logic symmetric: the activity-time window must be derived from the same live blocking set as the snapshot counts, otherwise cleanup becomes sensitive to unrelated historical churn. | Evidence: [CR-129-QUIESCENCE-ACTIVE-ROW-ACTIVITY-REVIEW.md](./CR-129-QUIESCENCE-ACTIVE-ROW-ACTIVITY-REVIEW.md); `tests/test_support/pipeline_quiescence.py`; `tests/unit/test_support/test_pipeline_quiescence.py`. |
| CR-130 | 2026-03-12 | Typed replay-job date binding | Hardened | CR-126 moved pending `RESET_WATERMARKS` creation onto one atomic upsert, but the real asyncpg path still treated the impacted date parameter as ambiguous. The repository contract was correct in shape but not fully valid on the actual driver path. | Explicitly bound `security_id` as `String` and `earliest_impacted_date` as `Date`, and executed the upsert with a parsed `date` value. DB-backed integration proof now passes on the real asyncpg path. | Replay queue hardening is only complete once the atomic path is proven on the real driver, not just in unit tests or abstract SQL form. | Evidence: [CR-130-TYPED-REPLAY-JOB-DATE-BINDING-REVIEW.md](./CR-130-TYPED-REPLAY-JOB-DATE-BINDING-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_reprocessing_job_repository.py`. |
| CR-131 | 2026-03-12 | Integration cleanup contract for replay queue | Hardened | `clean_db` truncated most durable state but omitted `reprocessing_jobs`, so integration tests on the replay queue could inherit historical pending/processing rows and fail for the wrong reason. That made repository tests appear flaky when the issue was test-harness residue. | Added `reprocessing_jobs` to the global truncate list in `tests/conftest.py` and revalidated the replay-job integration suite plus the full integration tier. | Keep `TABLES_TO_TRUNCATE` aligned with every durable control/work queue table as new infrastructure tables are introduced. | Evidence: `tests/conftest.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_reprocessing_job_repository.py`; full `python -m pytest tests/integration -x -q` run green on branch. |
| CR-132 | 2026-03-12 | E2E identity collision and predicate correctness | Hardened | Two broad-suite E2E failures were caused by test-contract issues rather than runtime defects: `test_fx_lifecycle` used a strict-subset check instead of true key containment, and `test_timeseries_pipeline` reused static portfolio/security/transaction identifiers that collided across the full suite. | Fixed the FX cash-position predicate to use `issubset`, randomized the timeseries fixture identifiers with a per-module suffix, and revalidated the exact files plus the full `tests/e2e` tier. | Keep E2E scenarios on unique identities by default and treat broad-suite failures as possible contract-collision defects before assuming pipeline regressions. | Evidence: `tests/e2e/test_fx_lifecycle.py`; `tests/e2e/test_timeseries_pipeline.py`; full `python -m pytest tests/e2e -x -q` run green on branch. |
| CR-133 | 2026-03-12 | Durable replay correlation lineage | Hardened | Back-dated price events preserved correlation on the immediate valuation-job path, but the durable replay chain still lost it: `instrument_reprocessing_state` carried no correlation id and coalesced `RESET_WATERMARKS` jobs carried no correlation id. That weakened auditability exactly where durable replay should be strongest. | Added nullable `correlation_id` to both `instrument_reprocessing_state` and `reprocessing_jobs`, propagated the price-event correlation through trigger upsert and scheduler job creation, and proved the earliest impacted trigger owns the persisted correlation on both the trigger row and the durable replay job. | Continue treating durable control rows as first-class lineage records. If future replay intents are coalesced by another key, carry correlation through the durable trigger and the durable job, not just transient event payloads. | Evidence: [CR-125-DURABLE-REPLAY-CORRELATION-LINEAGE-REVIEW.md](./CR-125-DURABLE-REPLAY-CORRELATION-LINEAGE-REVIEW.md); `alembic/versions/f0a1b2c3d4e5_feat_add_replay_correlation_lineage.py`; `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; trigger/scheduler unit tests; DB-backed trigger/job integration tests. |
| CR-134 | 2026-03-12 | Coalesced replay lineage backfill | Hardened | Durable replay lineage was preserved when a newer event lowered the impacted date, but it was still dropped when the stored trigger/job row had `correlation_id = NULL` and a later duplicate for the same impacted date arrived with a real correlation id. That left coalesced replay state under-audited even though enough lineage had become available. | Updated both trigger coalescing and pending `RESET_WATERMARKS` job coalescing so `NULL` correlation ids are backfilled from later same-date duplicates, and added DB-backed proofs for both paths. | Treat missing lineage on durable coalesced rows as recoverable state, not as final truth. If better lineage arrives for the same business impact, the durable row should absorb it. | Evidence: [CR-134-COALESCED-REPLAY-LINEAGE-BACKFILL-REVIEW.md](./CR-134-COALESCED-REPLAY-LINEAGE-BACKFILL-REVIEW.md); `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; DB-backed trigger/job integration tests. |
| CR-135 | 2026-03-12 | Reprocessing worker correlation context and dead helper removal | Hardened | Durable replay correlation was preserved on disk, but `ReprocessingWorker` still processed claimed `RESET_WATERMARKS` jobs outside the job's correlation context. At the same time, `ValuationRepositoryBase.find_portfolios_for_security(...)` had become dead after the worker switched to date-aware fanout and only survived through obsolete tests. | The worker now executes each durable replay job under `job.correlation_id` and restores ambient context afterward. Added unit proof for that context propagation. Removed the dead `find_portfolios_for_security(...)` helper and its obsolete integration test coverage. | Keep runtime lineage aligned with durable lineage. When a replay helper is no longer part of the real business path, delete it instead of letting old tests preserve fake ownership. | Evidence: [CR-135-REPROCESSING-WORKER-CORRELATION-CONTEXT-AND-DEAD-HELPER-REVIEW.md](./CR-135-REPROCESSING-WORKER-CORRELATION-CONTEXT-AND-DEAD-HELPER-REVIEW.md); `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; reprocessing worker unit tests; valuation repository integration tests. |
| CR-136 | 2026-03-12 | Date-aware fanout latest-history proof | Hardened | The worker-facing date-aware replay fanout contract had proof for pre-impact closures and future opens, but not for the more realistic mixed case where a portfolio closes before the impacted date and reopens after it. Without that proof, a future query refactor could regress to “any historical holding” semantics. | Added a DB-backed integration test proving `find_portfolios_holding_security_on_date(...)` uses the latest history row on or before the impacted date: the portfolio is excluded while closed on the impacted date and included again after reopening. | Keep the replay fanout contract anchored to the latest holding state as of the impacted date, not any older positive holding record. | Evidence: [CR-136-DATE-AWARE-FANOUT-LATEST-HISTORY-PROOF.md](./CR-136-DATE-AWARE-FANOUT-LATEST-HISTORY-PROOF.md); `tests/integration/services/calculators/position_valuation_calculator/test_int_instrument_reprocessing_repo.py`. |
| CR-137 | Current-Epoch Date-Aware Fanout | Replay / valuation repository | Hardened | `valuation_repository_base.find_portfolios_holding_security_on_date(...)` now fences `PositionHistory` against the live `PositionState.epoch`, and DB-backed proofs now seed explicit current state rows so worker fanout follows the real control model. | `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_instrument_reprocessing_repo.py`; `docs/architecture/CR-137-CURRENT-EPOCH-DATE-AWARE-FANOUT-REVIEW.md` |
| CR-138 | Valuation Holdings Fixture Current-State Alignment | Test quality / valuation repository | Hardened | Older holdings fixtures were asserting a pre-fence world with no live `PositionState` control rows. The fixtures now seed explicit current state so unit and integration proofs match the real current-epoch repository contract. | `tests/unit/services/calculators/position_valuation_calculator/repositories/test_unit_valuation_repo.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_valuation_repo.py`; `docs/architecture/CR-138-VALUATION-HOLDINGS-FIXTURE-CURRENT-STATE-ALIGNMENT-REVIEW.md` |
| CR-139 | Position Epoch Bump Fence | Replay / position state machine | Hardened | `increment_epoch_and_reset_watermark(...)` is now fenced by expected epoch, and the stale loser path in `PositionCalculator.calculate(...)` no longer stages replay outbox events. This closes a real concurrent back-dated replay race. | `src/libs/portfolio-common/portfolio_common/position_state_repository.py`; `src/services/calculators/position_calculator/app/core/position_logic.py`; `tests/unit/libs/portfolio-common/test_position_state_repository.py`; `tests/unit/services/calculators/position_calculator/core/test_position_logic.py`; `docs/architecture/CR-139-POSITION-EPOCH-BUMP-FENCE-REVIEW.md` |
| CR-140 | Position History Replay Fanout Index | DB query quality / replay performance | Hardened | Added a replay-fanout-oriented covering index on `position_history` for the worker-facing `(security_id, epoch, latest position_date/id -> portfolio_id)` access path, instead of relying only on the older portfolio-oriented index. | `src/libs/portfolio-common/portfolio_common/database_models.py`; `alembic/versions/f1b2c3d4e5f6_perf_add_position_history_replay_fanout_index.py`; `docs/architecture/CR-140-POSITION-HISTORY-REPLAY-FANOUT-INDEX-REVIEW.md` |
| CR-141 | Atomic Instrument Trigger Claim | Replay / valuation scheduler | Hardened | Replaced the scheduler's fetch-then-delete trigger consumption with a transaction-safe lock-and-delete claim path keyed by the real domain identity (`security_id`). This removes the read/delete race without relying on the unstable one-statement SQL shape. | `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py`; `tests/integration/services/calculators/position_valuation_calculator/test_int_instrument_reprocessing_repo.py`; `docs/architecture/CR-141-ATOMIC-INSTRUMENT-TRIGGER-CLAIM-REVIEW.md` |
| CR-142 | Replay Ordering Indexes | DB query quality / replay performance | Hardened | The replay trigger claim and pending `RESET_WATERMARKS` worker claim now use business-correct oldest-impact ordering, but the database still lacked indexes that support those exact orderings. That leaves unnecessary sort/scan pressure on active replay control tables under backlog. | `src/libs/portfolio-common/portfolio_common/database_models.py`; `alembic/versions/f2b3c4d5e6f7_perf_add_replay_ordering_indexes.py`; `docs/architecture/CR-142-REPLAY-ORDERING-INDEX-REVIEW.md` |
| CR-143 | Instrument Trigger Repository Ownership Convergence | Modularity / replay ownership | Hardened | Instrument replay trigger claim/count behavior was still sitting on the shared `ValuationRepositoryBase` even though only the valuation orchestrator owns and uses that path. The apparent “shared” status was being preserved by misplaced tests rather than real production ownership. | `src/services/valuation_orchestrator_service/app/repositories/valuation_repository.py`; `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/valuation_orchestrator_service/test_replay_trigger_repository.py`; `docs/architecture/CR-143-INSTRUMENT-TRIGGER-REPOSITORY-OWNERSHIP-CONVERGENCE-REVIEW.md` |
| CR-144 | Date-Aware Holdings Repository Ownership Convergence | Modularity / replay ownership | Hardened | The date-aware holdings lookup used by `ReprocessingWorker` still lived on the shared `ValuationRepositoryBase` even though it is only part of the valuation orchestrator replay path. That kept worker-specific fanout logic on the shared surface. | `src/services/valuation_orchestrator_service/app/repositories/valuation_repository.py`; `src/libs/portfolio-common/portfolio_common/valuation_repository_base.py`; `tests/integration/services/valuation_orchestrator_service/test_replay_trigger_repository.py`; `docs/architecture/CR-144-DATE-AWARE-HOLDINGS-REPOSITORY-OWNERSHIP-CONVERGENCE-REVIEW.md` |
| CR-145 | Scheduler Backfill Correlation Scope | Observability / valuation lineage | Hardened | Scheduler-created backfill jobs were stamped with a date-only correlation id, which is too weak for banking-grade lineage because multiple portfolios, securities, and epochs on the same date collapse onto the same pattern. | `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `tests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py`; `docs/architecture/CR-145-SCHEDULER-BACKFILL-CORRELATION-SCOPE-REVIEW.md` |
## Active next batch

1. Continue higher-value orchestrator, scheduler, and replay invariant review, not just observability/document cleanup
2. Review remaining warning-producing or noisy test harness patterns beyond the Kafka/noise and async cleanup fixes in CR-022/CR-023
3. Review remaining replay/watermark terminal-state invariants adjacent to CR-087 through CR-111


| CR-146 | Scheduler Dispatch Correlation Header Review | Observability / valuation lineage | Hardened | Scheduler dispatch no longer emits empty Kafka correlation_id headers; unit coverage now proves both present and absent lineage cases. | src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py; 	ests/unit/services/valuation_orchestrator_service/core/test_valuation_scheduler.py; docs/architecture/CR-146-SCHEDULER-DISPATCH-CORRELATION-HEADER-REVIEW.md |
| CR-147 | Aggregation Scheduler Correlation Header Review | Observability / aggregation lineage | Hardened | Portfolio aggregation scheduler no longer emits empty Kafka correlation_id headers; direct unit coverage now proves both present and absent lineage cases. | src/services/portfolio_aggregation_service/app/core/aggregation_scheduler.py; 	ests/unit/services/portfolio_aggregation_service/core/test_aggregation_scheduler.py; docs/architecture/CR-147-AGGREGATION-SCHEDULER-CORRELATION-HEADER-REVIEW.md |
| CR-148 | DLQ Correlation Header Normalization Review | Observability / consumer lineage | Hardened | Shared Kafka consumer DLQ emission now normalizes `"<not-set>"` to missing lineage and omits empty Kafka `correlation_id` headers; unit coverage now proves both correlated and uncorrelated DLQ paths. | `src/libs/portfolio-common/portfolio_common/kafka_consumer.py`; `tests/unit/libs/portfolio-common/test_kafka_consumer.py`; `docs/architecture/CR-148-DLQ-CORRELATION-HEADER-NORMALIZATION-REVIEW.md` |
| CR-149 | Enterprise Audit Payload Correlation Normalization Review | Observability / read-control-plane lineage | Hardened | Enterprise audit payloads now preserve missing correlation as `None` instead of `""`; direct unit proofs now cover both query-service and query-control-plane owners. | `src/services/query_service/app/enterprise_readiness.py`; `src/services/query_control_plane_service/app/enterprise_readiness.py`; corresponding unit tests; `docs/architecture/CR-149-ENTERPRISE-AUDIT-PAYLOAD-CORRELATION-NORMALIZATION-REVIEW.md` |
| CR-150 | Control Queue Hot Path Index Review | Performance / control-table scalability | Hardened | Added composite indexes aligned to replay, valuation, and aggregation claim/stale-reset predicates so hot control-table scans no longer rely on generic single-column coverage. | `src/libs/portfolio-common/portfolio_common/database_models.py`; `alembic/versions/f3c4d5e6f7a8_perf_add_control_queue_hot_path_indexes.py`; `docs/architecture/CR-150-CONTROL-QUEUE-HOT-PATH-INDEX-REVIEW.md` |
| CR-151 | Position State Hot Path Index Review | Performance / replay-valuation control state | Hardened | Added composite indexes aligned to lagging-state, terminal-reprocessing, and watermark-ordered scheduler scans on `position_state`. | `src/libs/portfolio-common/portfolio_common/database_models.py`; `alembic/versions/f4d5e6f7a8b9_perf_add_position_state_hot_path_indexes.py`; `docs/architecture/CR-151-POSITION-STATE-HOT-PATH-INDEX-REVIEW.md` |

| CR-152 | 2026-03-12 | Replay duplicate normalization observability | Hardened | Durable replay duplicate normalization had become a meaningful control path, but it was only visible through logs and tests. Operators could not see whether duplicate `RESET_WATERMARKS` rows were actively being collapsed under backlog pressure. | Added `reprocessing_duplicates_normalized_total{scope=...}` to the shared monitoring layer, emitted it from pending reset-watermarks normalization, and surfaced it in Grafana. | Keep future durable replay normalization paths on metric-backed observability, not log-only visibility. | Evidence: [CR-152-REPLAY-DUPLICATE-NORMALIZATION-OBSERVABILITY-REVIEW.md](./CR-152-REPLAY-DUPLICATE-NORMALIZATION-OBSERVABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `grafana/dashboards/portfolio_analytics.json`. |
| CR-153 | 2026-03-12 | Replay stale-skip observability | Hardened | The replay and scheduler paths already warned when epoch fencing caused partial updates, but stale-skip counts were not exported as metrics. That made replay pressure invisible unless operators searched logs. | Added `reprocessing_stale_skips_total{stage=...}` to the shared monitoring layer, emitted it from reset-watermarks fanout, watermark advancement, and terminal reprocessing normalization, and surfaced it in Grafana. | Keep stale-skip pressure metric-backed across replay/state-machine paths so operators can see recurring fence pressure before it becomes a support incident. | Evidence: [CR-153-REPLAY-STALE-SKIP-OBSERVABILITY-REVIEW.md](./CR-153-REPLAY-STALE-SKIP-OBSERVABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `grafana/dashboards/portfolio_analytics.json`. |
| CR-154 | 2026-03-12 | Shared lineage normalization contract | Hardened | Sentinel lineage normalization was duplicated across transport, write-plane, replay, and audit code. That created drift risk for a banking-critical contract. | Added `normalize_lineage_value(...)` to `portfolio_common.logging_utils` and replaced local sentinel checks across the affected modules, with direct helper proof and affected-surface revalidation. | Keep lineage sentinel handling centralized. New transport or audit boundaries should reuse the shared helper instead of reintroducing local `"<not-set>"` checks. | Evidence: [CR-154-SHARED-LINEAGE-NORMALIZATION-CONTRACT-REVIEW.md](./CR-154-SHARED-LINEAGE-NORMALIZATION-CONTRACT-REVIEW.md); `src/libs/portfolio-common/portfolio_common/logging_utils.py`; affected write-plane, replay, transport, and audit modules. |
| CR-155 | 2026-03-12 | Outbox terminal failure and hot path | Hardened | `OutboxDispatcher` had `MAX_RETRIES` defined but did not enforce it. Permanently failing rows stayed `PENDING` forever and kept re-entering the live queue. The table also lacked the composite indexes that match the dispatcher's actual claim pattern. | Added terminal failure semantics so retry-threshold rows become `FAILED` instead of remaining `PENDING`, and added `(status, created_at)` plus `(status, last_attempted_at)` indexes for `outbox_events`. Added integration proof for the terminal transition. | If operators later need explicit alerting on terminal outbox failures, add a dedicated Prometheus metric for `FAILED` transitions rather than inferring only from logs. | Evidence: [CR-155-OUTBOX-TERMINAL-FAILURE-AND-HOT-PATH-REVIEW.md](./CR-155-OUTBOX-TERMINAL-FAILURE-AND-HOT-PATH-REVIEW.md); `src/libs/portfolio-common/portfolio_common/outbox_dispatcher.py`; `src/libs/portfolio-common/portfolio_common/database_models.py`; `tests/integration/libs/portfolio-common/test_outbox_dispatcher.py`. |
| CR-156 | 2026-03-12 | Outbox backlog and terminal failure observability | Hardened | After CR-155, the dispatcher prevented permanently failing rows from poisoning the live queue, but operators still could not directly see durable `FAILED` residue or the age of the oldest pending row. Existing dashboards only exposed pending count plus published/failed/retried rates. | Added `outbox_events_failed_stored` and `outbox_events_oldest_pending_age_seconds`, updated the dispatcher gauge read to publish them, added DB-backed proof for the gauge contract, and surfaced both metrics on the existing Grafana outbox pressure panel. | If terminal-failure alerting is needed later, derive it from the durable-failure gauge rather than retry-rate heuristics. | Evidence: [CR-156-OUTBOX-BACKLOG-AND-TERMINAL-FAILURE-OBSERVABILITY-REVIEW.md](./CR-156-OUTBOX-BACKLOG-AND-TERMINAL-FAILURE-OBSERVABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/libs/portfolio-common/portfolio_common/outbox_dispatcher.py`; `tests/integration/libs/portfolio-common/test_outbox_dispatcher.py`; `grafana/dashboards/portfolio_analytics.json`. |
| CR-157 | 2026-03-13 | Coalesced replay lineage null-earlier-date preservation | Hardened | Durable replay coalescing still had one lineage-loss path: if an older impacted date arrived without a correlation id, it could replace a stored non-null correlation with `NULL` simply because the date was older. That weakened auditability on both the trigger row and the durable replay job. | Updated both trigger and `RESET_WATERMARKS` job coalescing so an earlier incoming row with `correlation_id = NULL` preserves an existing non-null durable correlation. Added DB-backed proofs for both paths. | Keep earliest business impact and best-known lineage as separate concerns. Older date should win ordering, but missing lineage should not erase already-known lineage. | Evidence: [CR-157-COALESCED-REPLAY-LINEAGE-NULL-EARLIER-DATE-REVIEW.md](./CR-157-COALESCED-REPLAY-LINEAGE-NULL-EARLIER-DATE-REVIEW.md); `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; replay lineage DB-backed integration tests. |

| CR-158 | 2026-03-13 | Stale reprocessing retry ceiling and model drift | Hardened | Durable RESET_WATERMARKS jobs could cycle between PROCESSING and PENDING forever after repeated worker crashes, and the SQLAlchemy model still carried the old invalid replay-ordering index expression even though the migration had already been corrected. | Added configurable REPROCESSING_WORKER_MAX_ATTEMPTS with a default of 3, changed stale replay recovery to mark over-limit jobs FAILED with a durable reason instead of resetting forever, passed that ceiling through ReprocessingWorker, and aligned the ReprocessingJob model index expression to the real migration contract. | If operators need explicit alerting on replay jobs that hit the retry ceiling, add a dedicated metric rather than relying only on logs and durable FAILED state. | Evidence: [CR-158-STALE-REPROCESSING-RETRY-CEILING-AND-MODEL-DRIFT-REVIEW.md](./CR-158-STALE-REPROCESSING-RETRY-CEILING-AND-MODEL-DRIFT-REVIEW.md); src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py; src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py; src/services/valuation_orchestrator_service/app/settings.py; src/libs/portfolio-common/portfolio_common/database_models.py; unit replay repository/worker proof. |

| CR-159 | 2026-03-13 | Valuation stale retry ceiling | Hardened | PortfolioValuationJob stale recovery still reset PROCESSING rows back to PENDING forever, leaving the valuation queue inconsistent with the replay queue after CR-158 and allowing repeated worker crashes to create infinite retry cycles. | Added configurable VALUATION_SCHEDULER_MAX_ATTEMPTS with default 3, changed stale valuation recovery to mark over-limit rows FAILED with a durable reason instead of resetting forever, and passed that ceiling through ValuationScheduler. | Apply the same terminal stale-retry symmetry to the aggregation queue so all durable control queues share one failure model. | Evidence: [CR-159-VALUATION-STALE-RETRY-CEILING-REVIEW.md](./CR-159-VALUATION-STALE-RETRY-CEILING-REVIEW.md); src/libs/portfolio-common/portfolio_common/valuation_repository_base.py; src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py; src/services/valuation_orchestrator_service/app/settings.py; valuation unit and DB-backed integration proof. |
| CR-160 | 2026-03-13 | Aggregation stale retry ceiling | Hardened | PortfolioAggregationJob stale recovery still reset PROCESSING rows back to PENDING forever and the queue lacked durable retry metadata entirely, leaving aggregation inconsistent with replay and valuation after CR-158 and CR-159. | Added durable `attempt_count` and `failure_reason` to `portfolio_aggregation_jobs`, introduced configurable `AGGREGATION_SCHEDULER_MAX_ATTEMPTS` with default 3, changed stale aggregation recovery to mark over-limit rows FAILED instead of resetting forever, and incremented attempt count on claim. | Surface terminal FAILED aggregation jobs in explicit telemetry if operators need alerting beyond logs and queue inspection. | Evidence: [CR-160-AGGREGATION-STALE-RETRY-CEILING-REVIEW.md](./CR-160-AGGREGATION-STALE-RETRY-CEILING-REVIEW.md); `src/libs/portfolio-common/portfolio_common/database_models.py`; `src/libs/portfolio-common/portfolio_common/timeseries_repository_base.py`; `src/services/portfolio_aggregation_service/app/core/aggregation_scheduler.py`; `src/services/portfolio_aggregation_service/app/settings.py`; `alembic/versions/f5e6f7a8b9c0_feat_add_aggregation_retry_metadata.py`; aggregation unit and DB-backed integration proof. |
| CR-161 | 2026-03-13 | Aggregation job retry metadata visibility | Hardened | CR-160 hardened the durable aggregation queue, but the support/control-plane API still returned aggregation jobs as if they had no retry metadata. That hid active retry and terminal-failure context from operators. | Updated `OperationsService.get_aggregation_jobs(...)` to expose `attempt_count` and `failure_reason`, and strengthened unit and control-plane contract tests to prove those fields are visible on the support API. | Surface aggregation retry/failure pressure in dashboards if row-level support listings are not sufficient for operators. | Evidence: [CR-161-AGGREGATION-JOB-RETRY-METADATA-VISIBILITY-REVIEW.md](./CR-161-AGGREGATION-JOB-RETRY-METADATA-VISIBILITY-REVIEW.md); `src/services/query_service/app/services/operations_service.py`; `tests/unit/services/query_service/services/test_operations_service.py`; `tests/integration/services/query_control_plane_service/test_operations_router_dependency.py`. |
| CR-162 | 2026-03-13 | Outbox runtime configurability | Hardened | `OutboxDispatcher` enforced terminal failure semantics, but the retry ceiling still lived as a hardcoded constant inside a shared durable component and the shared polling/batch policy was effectively buried in constructor defaults. That made the banking-critical delivery-control policy opaque and harder to tune safely under load. | Added `OutboxRuntimeSettings` with `OUTBOX_DISPATCHER_POLL_INTERVAL_SECONDS`, `OUTBOX_DISPATCHER_BATCH_SIZE`, and `OUTBOX_DISPATCHER_MAX_RETRIES`; kept constructor override support for deterministic callers/tests; removed the dead backoff constant; and added both unit and DB-backed proof that terminal failure behavior follows the configured threshold. | If service-specific outbox policy ever diverges, expose it intentionally at the service runtime boundary instead of reintroducing hardcoded forks inside the shared dispatcher. | Evidence: [CR-162-OUTBOX-RETRY-CONFIGURABILITY-REVIEW.md](./CR-162-OUTBOX-RETRY-CONFIGURABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/outbox_dispatcher.py`; `tests/unit/libs/portfolio-common/test_outbox_dispatcher.py`; `tests/integration/libs/portfolio-common/test_outbox_dispatcher.py`. |
| CR-163 | 2026-03-13 | Stale timeout runtime controls | Hardened | Replay, valuation, and aggregation queues exposed retry ceilings, but the companion stale-processing timeout still lived as a hardcoded repository default (`15 minutes`). That left a key recovery threshold implicit at the data-access layer rather than owned by the runtime that applies it. | Added `VALUATION_SCHEDULER_STALE_TIMEOUT_MINUTES`, `REPROCESSING_WORKER_STALE_TIMEOUT_MINUTES`, and `AGGREGATION_SCHEDULER_STALE_TIMEOUT_MINUTES`, and wired the owning runtimes to pass those values explicitly to stale-reset repository calls. | Keep recovery thresholds runtime-owned. New durable control queues should expose both stale timeout and retry ceiling together instead of hardcoding one side at the repository layer. | Evidence: [CR-163-STALE-TIMEOUT-RUNTIME-CONTROLS-REVIEW.md](./CR-163-STALE-TIMEOUT-RUNTIME-CONTROLS-REVIEW.md); `src/services/valuation_orchestrator_service/app/settings.py`; `src/services/valuation_orchestrator_service/app/core/valuation_scheduler.py`; `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `src/services/portfolio_aggregation_service/app/settings.py`; `src/services/portfolio_aggregation_service/app/core/aggregation_scheduler.py`. |
| CR-164 | 2026-03-13 | Duplicate-blocked replay observability proof | Hardened | Replay duplicate-blocked outcomes were already surfaced through APIs, alerts, and Grafana, and the shared monitoring layer already defined `ingestion_replay_duplicate_blocked_total`, but there was no lower-level proof that the service path actually incremented the metric on duplicate-blocked audits. | Added direct unit proofs for `IngestionJobService.record_consumer_dlq_replay_audit(...)` covering both `duplicate_blocked` and `failed` status paths, including the non-cross-increment contract between duplicate-blocked and failure counters. | Keep replay-pressure metrics proved at the service boundary so dashboards and alerts remain evidence-backed instead of assumed. | Evidence: [CR-164-DUPLICATE-BLOCKED-REPLAY-OBSERVABILITY-PROOF.md](./CR-164-DUPLICATE-BLOCKED-REPLAY-OBSERVABILITY-PROOF.md); `tests/unit/services/ingestion_service/services/test_ingestion_job_service_guardrails.py`; `src/services/ingestion_service/app/services/ingestion_job_service.py`; `src/libs/portfolio-common/portfolio_common/monitoring.py`. |
| CR-165 | 2026-03-13 | Epoch fencer service ownership contract | Hardened | `EpochFencer` still allowed `service_name` to default to `"<not-set>"`, which meant a stale-epoch drop on an active replay/control path could silently lose real service ownership in the emitted metric. | Made `service_name` mandatory on `EpochFencer` construction and updated the shared unit proofs to require an explicit owner. | Keep service ownership explicit on control-path observability helpers; do not rely on sentinel defaults for metrics that drive operational triage. | Evidence: [CR-165-EPOCH-FENCER-SERVICE-OWNERSHIP-CONTRACT.md](./CR-165-EPOCH-FENCER-SERVICE-OWNERSHIP-CONTRACT.md); `src/libs/portfolio-common/portfolio_common/reprocessing.py`; `tests/unit/libs/portfolio-common/test_reprocessing.py`. |
| CR-166 | 2026-03-13 | Reprocessing worker no-op outcome observability | Hardened | `RESET_WATERMARKS` jobs that found no impacted portfolios were treated as successful completions, but that outcome was only visible in logs. Operators could not distinguish no-op replay completion from real watermark fanout. | Added `reprocessing_worker_jobs_noop_total{job_type, reason}` and emitted it for `no_impacted_portfolios`, with unit proof that no-op jobs do not mutate state and still complete cleanly. | If replay operations need richer operator visibility, surface the no-op counter in Grafana alongside replay claim/completion/failure metrics. | Evidence: [CR-166-REPROCESSING-WORKER-NOOP-OBSERVABILITY-REVIEW.md](./CR-166-REPROCESSING-WORKER-NOOP-OBSERVABILITY-REVIEW.md); `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`; `tests/unit/services/valuation_orchestrator_service/core/test_reprocessing_worker.py`. |
| CR-167 | 2026-03-13 | Replay no-op dashboard signal | Hardened | `reprocessing_worker_jobs_noop_total{job_type,reason}` was emitted by the valuation reprocessing worker, but operators still could not see it in the live dashboard or the usage guide. The runtime signal existed without an operational consumer. | Added the no-op replay rate to the `Reprocessing Worker Throughput and Backlog` panel and updated the Grafana guide workflow so operators know how to interpret `noop RESET_WATERMARKS / no_impacted_portfolios`. | Keep new runtime observability signals paired with dashboard and runbook coverage; do not stop at metric emission alone. | Evidence: [CR-167-REPLAY-NOOP-DASHBOARD-SIGNAL-REVIEW.md](./CR-167-REPLAY-NOOP-DASHBOARD-SIGNAL-REVIEW.md); `grafana/dashboards/portfolio_analytics.json`; `docs/operations/Grafana-Dashboard-Guide.md`; `src/libs/portfolio-common/portfolio_common/monitoring.py`; `src/services/valuation_orchestrator_service/app/core/reprocessing_worker.py`. |
| CR-168 | 2026-03-13 | Durable replay lineage boundary normalization | Hardened | Sentinel lineage normalization had already been enforced across API, consumer, scheduler, and audit boundaries, but the durable replay repositories still accepted caller-provided `correlation_id` values verbatim. A future regression upstream could still persist `"<not-set>"` into replay trigger or job rows. | Normalized `correlation_id` inside `ReprocessingJobRepository.create_job(...)` and `InstrumentReprocessingStateRepository.upsert_state(...)`, with unit and DB-backed proof that sentinel lineage is stored as `NULL`. | Keep durable lineage normalization enforced at the repository boundary, not only at callers. Persistence contracts should remain safe even if an upstream path regresses. | Evidence: [CR-168-DURABLE-REPLAY-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md](./CR-168-DURABLE-REPLAY-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/reprocessing_job_repository.py`; `src/services/valuation_orchestrator_service/app/repositories/instrument_reprocessing_state_repository.py`; unit and integration proofs. |
| CR-169 | 2026-03-13 | Shared durable lineage boundary normalization | Hardened | Replay repositories had already been hardened against sentinel lineage, but other shared durable repositories still accepted raw `correlation_id` values. A regression upstream could still persist `"<not-set>"` into outbox, processed-event, or valuation-job rows. | Normalized `correlation_id` inside `OutboxRepository.create_outbox_event(...)`, `IdempotencyRepository.mark_event_processed(...)`, and `ValuationJobRepository.upsert_job(...)`, with direct unit proof for each boundary. | Keep durable lineage normalization enforced at repository boundaries across the shared persistence layer; do not rely on callers alone. | Evidence: [CR-169-SHARED-DURABLE-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md](./CR-169-SHARED-DURABLE-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md); shared repository modules and corresponding unit tests. |
| CR-170 | 2026-03-13 | Reconciliation run lineage boundary normalization | Hardened | Durable replay and shared persistence repositories had been hardened, but `FinancialReconciliationRun` creation still accepted raw `correlation_id` values. A control-plane path regression could still persist `"<not-set>"` into reconciliation runs. | Normalized `correlation_id` inside `ReconciliationRepository.create_run(...)` and added direct unit proof that sentinel lineage is stored as `NULL`. | Keep control-plane durable lineage contracts aligned with replay and outbox boundaries. Reconciliation persistence should not rely on caller discipline alone. | Evidence: [CR-170-RECONCILIATION-RUN-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md](./CR-170-RECONCILIATION-RUN-LINEAGE-BOUNDARY-NORMALIZATION-REVIEW.md); `src/services/financial_reconciliation_service/app/repositories/reconciliation_repository.py`; `tests/unit/services/financial_reconciliation_service/test_reconciliation_repository.py`. |
| CR-171 | 2026-03-13 | Reprocessing job model-schema uniqueness alignment | Hardened | The database already enforced a partial unique index for pending `RESET_WATERMARKS` jobs keyed by `payload->>'security_id'`, but the SQLAlchemy model still only declared the non-unique priority index. That left ORM metadata drifting away from the real durable replay invariant. | Added the missing partial unique index declaration to `ReprocessingJob` and added a metadata-level unit test proving the model advertises the same uniqueness contract as the migration-backed database. | Keep durable queue invariants aligned across migration history, ORM metadata, and tests. Banking-grade replay behavior should not depend on hidden schema rules that the model does not describe. | Evidence: [CR-171-REPROCESSING-JOB-MODEL-SCHEMA-UNIQUENESS-ALIGNMENT-REVIEW.md](./CR-171-REPROCESSING-JOB-MODEL-SCHEMA-UNIQUENESS-ALIGNMENT-REVIEW.md); `src/libs/portfolio-common/portfolio_common/database_models.py`; `tests/unit/libs/portfolio-common/test_database_models.py`; `alembic/versions/e4f5a6b7c8d9_feat_add_pending_reset_watermarks_uniqueness.py`. |
| CR-172 | 2026-03-13 | Direct consumer correlation context simplification | Hardened | Several direct-invocation consumers had already adopted `_message_correlation_context(...)`, but still re-read `correlation_id_var` inside the context body. That left the code depending on ambient context even after entering the explicit message-scoped contract. | Switched the reviewed consumers to use the correlation id yielded by `_message_correlation_context(...)` directly and removed the now-redundant `correlation_id_var` imports. | Keep direct consumer lineage handling explicit: once a message-scoped context helper exists, consumers should use its yielded value instead of reaching back into ambient context state. | Evidence: [CR-172-DIRECT-CONSUMER-CORRELATION-CONTEXT-SIMPLIFICATION-REVIEW.md](./CR-172-DIRECT-CONSUMER-CORRELATION-CONTEXT-SIMPLIFICATION-REVIEW.md); reviewed consumer modules across timeseries, persistence, cashflow, valuation, cost, and position paths. |
| CR-173 | 2026-03-13 | Portfolio aggregation correlation context simplification | Hardened | `PortfolioTimeseriesConsumer` already used `_message_correlation_context(...)`, but still re-read `correlation_id_var` inside the context body. That left one more active aggregation-path consumer coupled to ambient state after entering the explicit message-scoped contract. | Switched the consumer to use the correlation id yielded by `_message_correlation_context(...)` directly and removed the redundant `correlation_id_var` import. | Keep aggregation-path lineage handling aligned with the rest of the reviewed direct consumers: use the yielded message-scoped correlation id, not ambient state lookups. | Evidence: [CR-173-PORTFOLIO-AGGREGATION-CORRELATION-CONTEXT-SIMPLIFICATION-REVIEW.md](./CR-173-PORTFOLIO-AGGREGATION-CORRELATION-CONTEXT-SIMPLIFICATION-REVIEW.md); `src/services/portfolio_aggregation_service/app/consumers/portfolio_timeseries_consumer.py`; `tests/unit/services/portfolio_aggregation_service/consumers/test_portfolio_timeseries_consumer.py`. |
| CR-174 | 2026-03-13 | Control queue observability contract | Hardened | Valuation, aggregation, and replay queues exposed throughput and failure rates, but they did not expose first-class durable-state gauges for pending volume, terminal failed rows, and oldest pending age. Operators had to infer queue pressure indirectly. | Added a reusable `control_queue_*{queue=...}` metric family, queue stats repository methods, scheduler/worker publication, a Grafana `Control Queue Pressure` panel, and guide updates. | Keep durable control queues on the same observability contract as outbox. New control queues should publish pending, terminal failed, and oldest pending age directly instead of relying only on rate charts. | Evidence: [CR-174-CONTROL-QUEUE-OBSERVABILITY-CONTRACT-REVIEW.md](./CR-174-CONTROL-QUEUE-OBSERVABILITY-CONTRACT-REVIEW.md); monitoring/repository/scheduler/worker files; dashboard and guide updates. |
| CR-175 | 2026-03-13 | Control-plane queue pressure visibility | Hardened | Grafana exposed durable queue pressure for valuation and aggregation, but the support/control-plane API still exposed only partial job health. Operators using `/support/portfolios/{portfolio_id}/overview` could not see failed valuation jobs, failed aggregation jobs, or aggregation backlog age over HTTP. | Extended `SupportOverviewResponse` and `OperationsService.get_support_overview(...)` to expose failed valuation jobs, aggregation processing/stale/failed counts, oldest pending aggregation date, and aggregation backlog age. Strengthened unit and OpenAPI contract tests so the HTTP support surface now proves the same queue-pressure truth as the dashboard. | Consider surfacing replay queue pressure on the same overview once a stable portfolio-scoped replay backlog contract exists. | Evidence: [CR-175-CONTROL-PLANE-QUEUE-PRESSURE-VISIBILITY-REVIEW.md](./CR-175-CONTROL-PLANE-QUEUE-PRESSURE-VISIBILITY-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `src/services/query_service/app/services/operations_service.py`; operations unit and control-plane integration tests. |
| CR-176 | 2026-03-13 | HTTP bootstrap lineage header normalization | Hardened | The shared pure-HTTP bootstrap middleware still trusted incoming lineage headers verbatim. If a caller sent `"<not-set>"` or an empty value, services echoed that sentinel back into response headers and log context instead of treating it as missing lineage. | Normalized incoming `X-Correlation-ID`, `X-Request-Id`, and `X-Trace-Id` through `normalize_lineage_value(...)`, generated fresh ids when the incoming values were missing or sentinel, and added integration proofs for `query_service`, `query_control_plane_service`, and `event_replay_service`. | Apply the same boundary rule to any non-HTTP transport that still accepts raw external lineage values before shared runtime or persistence handling. | Evidence: [CR-176-HTTP-BOOTSTRAP-LINEAGE-HEADER-NORMALIZATION-REVIEW.md](./CR-176-HTTP-BOOTSTRAP-LINEAGE-HEADER-NORMALIZATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/http_app_bootstrap.py`; app-contract integration tests for query, control-plane, and replay services. |

| CR-177 | 2026-03-13 | Analytics export queue pressure visibility | Hardened | Analytics export is a durable queue with accepted, running, and failed states, but the support overview API exposed queue pressure only for valuation and aggregation. Operators using the control-plane HTTP surface could not see export backlog or failed export pressure. | Added analytics export queue health summary to `OperationsRepository`, extended `SupportOverviewResponse`/`OperationsService.get_support_overview(...)` with pending/running/stale/failed/backlog fields, and strengthened unit plus control-plane OpenAPI coverage. | If analytics export gets its own scheduler or retry model later, keep its queue pressure on the same first-class support surface as the other durable control queues. | Evidence: [CR-177-ANALYTICS-EXPORT-QUEUE-PRESSURE-VISIBILITY-REVIEW.md](./CR-177-ANALYTICS-EXPORT-QUEUE-PRESSURE-VISIBILITY-REVIEW.md); operations repository/service/DTO files; `tests/unit/services/query_service/services/test_operations_service.py`; `tests/integration/services/query_control_plane_service/test_operations_router_dependency.py`; `tests/integration/services/query_control_plane_service/test_control_plane_app.py`. |

| CR-178 | 2026-03-13 | Analytics export hot-path index review | Hardened | Analytics export was promoted to a first-class durable queue on the support API, but the underlying table still exposed only single-column indexes even though the active queue and support scans filter by `portfolio_id`, `status`, `created_at`, and `updated_at`. | Added composite queue indexes for `(portfolio_id, status, created_at)` and `(status, updated_at)` at both ORM metadata and Alembic levels, with metadata-level proof in `test_database_models.py`. | Keep durable queue visibility and queue-path indexing aligned. When a table becomes an operational queue, its hot-path indexes should be reviewed as part of the same slice. | Evidence: [CR-178-ANALYTICS-EXPORT-HOT-PATH-INDEX-REVIEW.md](./CR-178-ANALYTICS-EXPORT-HOT-PATH-INDEX-REVIEW.md); `src/libs/portfolio-common/portfolio_common/database_models.py`; `alembic/versions/faa7b8c9d0e1_perf_add_analytics_export_hot_path_indexes.py`; `tests/unit/libs/portfolio-common/test_database_models.py`. |
| CR-179 | 2026-03-13 | Analytics export durable lifecycle review | Hardened | Analytics export jobs only persisted terminal failure state for expected `AnalyticsInputError` paths. An unexpected processing exception could roll back the entire job row and erase the durable support/audit record for that export attempt. | Refactored `AnalyticsTimeseriesService.create_export_job(...)` into explicit durable lifecycle steps: reserve job, mark running, and finalize complete or failed in separate transactions. Unexpected exceptions now persist a durable `failed` job with a stable error message before re-raising, and lower-level tests prove both expected and unexpected failure paths. | Keep durable job lifecycles explicit. Long-running or failure-prone control paths should not rely on one large transaction that can erase operational history on exception. | Evidence: `src/services/query_service/app/services/analytics_timeseries_service.py`; `tests/unit/services/query_service/services/test_analytics_timeseries_service.py`; `tests/integration/services/query_control_plane_service/test_analytics_inputs_router_dependency.py`. |
| CR-180 | 2026-03-13 | Analytics export stale-running reuse contract | Hardened | Durable analytics export jobs could remain in `accepted` or `running` if a process died mid-request. The fingerprint reuse path would then keep reusing that stale row forever instead of superseding it with a new export attempt. | Added configurable `LOTUS_CORE_ANALYTICS_EXPORT_STALE_TIMEOUT_MINUTES` and changed analytics export reservation so completed jobs are reused, fresh accepted/running jobs are reused, and stale accepted/running jobs are first marked `failed` with a durable superseded reason before a new job is created. Added unit proofs for both fresh-running reuse and stale-running replacement. | Keep durable reuse contracts time-aware. Fingerprint deduplication is valuable, but it must not pin stale work indefinitely. | Evidence: `src/services/query_service/app/settings.py`; `src/services/query_service/app/services/analytics_timeseries_service.py`; `tests/unit/services/query_service/services/test_analytics_timeseries_service.py`. |
| CR-181 | 2026-03-13 | Analytics export support listing | Hardened | The support plane exposed analytics export queue health in aggregate, but operators still had no first-class control-plane endpoint to inspect the underlying durable job rows for a portfolio. That forced support/debug flows to infer state from summary counters instead of inspecting the actual failed or stuck jobs. | Added `AnalyticsExportJobRecord` and `AnalyticsExportJobListResponse`, repository queries for paged export-job listing, `OperationsService.get_analytics_export_jobs(...)`, and `GET /support/portfolios/{portfolio_id}/analytics-export-jobs`. Added repository, service, router, and OpenAPI contract proof. | Keep support-plane queue visibility paired: if aggregate queue health is exposed, the underlying durable job rows should also be queryable through the same operational surface. | Evidence: [CR-181-ANALYTICS-EXPORT-SUPPORT-LISTING-REVIEW.md](./CR-181-ANALYTICS-EXPORT-SUPPORT-LISTING-REVIEW.md); operations DTO/repository/service/router files; operations unit and control-plane integration tests. |
| CR-182 | 2026-03-13 | Shared logging lineage normalization | Hardened | Transport, audit, and persistence lineage had been normalized, but the shared structured-logging filter still emitted raw sentinel values like `"<not-set>"`. That left logs on a weaker contract than the rest of the runtime. | Normalized `correlation_id`, `request_id`, and `trace_id` inside `CorrelationIdFilter` and added unit proof that unset lineage is logged as `None`, not as sentinel strings. | Keep structured logging on the same lineage contract as transport, audit, and persistence. Logs should not be the last place fake lineage survives. | Evidence: [CR-182-SHARED-LOGGING-LINEAGE-NORMALIZATION-REVIEW.md](./CR-182-SHARED-LOGGING-LINEAGE-NORMALIZATION-REVIEW.md); `src/libs/portfolio-common/portfolio_common/logging_utils.py`; `tests/unit/libs/portfolio-common/test_logging_utils.py`. |
| CR-183 | 2026-03-13 | Reconciliation support listing and portfolio ownership fence | Hardened | The support plane exposed reconciliation status only through aggregate overview fields. Operators could not inspect durable reconciliation runs/findings directly, and the partial findings path was only keyed by `run_id`, which risked crossing portfolio boundaries. | Added support-plane reconciliation run and finding listing endpoints, introduced dedicated support DTOs, and fenced findings lookup by `(portfolio_id, run_id)` before returning durable findings. Added repository, service, router, and OpenAPI proofs. | Keep support-plane queue/control listings paired with ownership fences. Durable run identifiers alone are not an adequate scope boundary in banking workflows. | Evidence: [CR-183-RECONCILIATION-SUPPORT-LISTING-REVIEW.md](./CR-183-RECONCILIATION-SUPPORT-LISTING-REVIEW.md); operations DTO/repository/service/router files; query-service unit tests; query-control-plane integration and OpenAPI tests. |
| CR-184 | 2026-03-13 | Support job lifecycle visibility | Hardened | The valuation/aggregation support job listing endpoints exposed durable rows, but flattened away the timing detail needed to operate them safely. Operators could see `PROCESSING`, yet could not tell from the listing whether that row was recently active or already stale without cross-referencing other endpoints. | Extended `SupportJobRecord` with `updated_at` and `is_stale_processing`, computed staleness centrally in `OperationsService`, and added unit, router, and OpenAPI proof that the support-plane listing now carries lifecycle freshness directly. | Keep support-plane job listings truthful about durable lifecycle state. If stale-processing health exists in overview counters, the underlying row listing should expose the same truth. | Evidence: [CR-184-SUPPORT-JOB-LIFECYCLE-VISIBILITY-REVIEW.md](./CR-184-SUPPORT-JOB-LIFECYCLE-VISIBILITY-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `src/services/query_service/app/services/operations_service.py`; operations unit and query-control-plane integration tests. |
| CR-185 | 2026-03-13 | Analytics export lifecycle visibility | Hardened | The analytics export support listing exposed durable rows and timestamps, but still forced operators to infer lifecycle freshness manually. One `running` export row could be healthy while another was stale, yet the row contract did not surface that difference or the per-row backlog age. | Extended `AnalyticsExportJobRecord` with `updated_at`, `is_stale_running`, and `backlog_age_minutes`, computed the derived lifecycle fields centrally in `OperationsService`, and added unit, router, and OpenAPI proof for the strengthened support contract. | Keep support-plane export listings as truthful as the queue-health overview. If aggregate stale-running truth exists, the row-level listing should expose the same lifecycle meaning. | Evidence: [CR-185-ANALYTICS-EXPORT-LIFECYCLE-VISIBILITY-REVIEW.md](./CR-185-ANALYTICS-EXPORT-LIFECYCLE-VISIBILITY-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `src/services/query_service/app/services/operations_service.py`; operations unit and query-control-plane integration tests. |
| CR-186 | 2026-03-13 | Reconciliation run blocking visibility | Hardened | The reconciliation support listing exposed durable run records, but still forced operators to infer blocking truth from raw `status`. That duplicated publication-policy logic on the client side even though the control plane already has a canonical blocking contract. | Extended `ReconciliationRunRecord` with `is_blocking`, computed it centrally from `_is_controls_blocking(...)`, and added unit, router, and OpenAPI proof for the strengthened support contract. | Keep support-plane control listings aligned with the server-side publication policy. Client tools should not need to re-derive blocking truth from raw statuses. | Evidence: [CR-186-RECONCILIATION-RUN-BLOCKING-VISIBILITY-REVIEW.md](./CR-186-RECONCILIATION-RUN-BLOCKING-VISIBILITY-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `src/services/query_service/app/services/operations_service.py`; operations unit and query-control-plane integration tests. |
| CR-187 | 2026-03-13 | Support listing operational priority ordering | Hardened | Support-plane listings were still ordered mostly by recency, which is the wrong contract for live incident response. Operators need failed, blocking, and stale rows first, and paging must preserve that truth. | Moved ordering priority into the repository queries so valuation, aggregation, analytics export, and reconciliation support listings are ordered by operational severity first and only then by business date or recency. Added repository and service proof for the new contract. | Keep support-plane paging aligned with operator triage. If a listing is used for incident response, prioritize failed/stale/blocking rows before recency. | Evidence: [CR-187-SUPPORT-LISTING-OPERATIONAL-PRIORITY-ORDERING-REVIEW.md](./CR-187-SUPPORT-LISTING-OPERATIONAL-PRIORITY-ORDERING-REVIEW.md); `src/services/query_service/app/repositories/operations_repository.py`; `tests/unit/services/query_service/repositories/test_operations_repository.py`; `tests/unit/services/query_service/services/test_operations_service.py`. |
| CR-188 | 2026-03-13 | Derived support state contract | Hardened | Support rows had accumulated raw lifecycle fields, but clients still had to reconstruct retry, stale, terminal, and blocking meaning from combinations of status, timestamps, and attempt counts. That duplicated server policy and increased client inconsistency risk. | Extended support DTOs with explicit derived operational-state fields (`is_retrying`, `is_terminal_failure`, `operational_state`) and centralized their computation in `OperationsService`, with unit, router, and OpenAPI proof. | Keep operational meaning server-owned. If the server already knows queue severity and lifecycle truth, expose that directly instead of forcing clients to re-derive it. | Evidence: [CR-188-DERIVED-SUPPORT-STATE-CONTRACT-REVIEW.md](./CR-188-DERIVED-SUPPORT-STATE-CONTRACT-REVIEW.md); `src/services/query_service/app/dtos/operations_dto.py`; `src/services/query_service/app/services/operations_service.py`; query-service unit and control-plane integration/OpenAPI tests. |
| CR-189 | 2026-03-13 | Portfolio control stage support listing | Hardened | The support plane exposed only the latest control-stage aggregate on the portfolio overview, which was insufficient for real operational triage. Operators could not inspect durable portfolio-day control rows over time or filter them by stage/date/status. | Added `GET /support/portfolios/{portfolio_id}/control-stages`, introduced dedicated DTOs, repository queries, service mapping, and control-plane router/OpenAPI proof. The listing is fenced to durable portfolio-stage rows and exposes derived blocking truth per row. | Keep control-plane support surfaces aligned with durable control state, not just latest aggregates. If a stage can block publication, it should be queryable as a first-class support object. | Evidence: [CR-189-PORTFOLIO-CONTROL-STAGE-SUPPORT-LISTING-REVIEW.md](./CR-189-PORTFOLIO-CONTROL-STAGE-SUPPORT-LISTING-REVIEW.md); operations DTO/repository/service/router files; query-service unit and control-plane integration/OpenAPI tests. |
| CR-190 | 2026-03-13 | Reprocessing key support listing | Hardened | The support plane exposed replay pressure only as an aggregate count. Operators could see active replay keys in overview counters but could not inspect the durable `position_state` rows that actually define replay scope, epoch, watermark date, and stale reprocessing status. | Added `GET /support/portfolios/{portfolio_id}/reprocessing-keys`, introduced dedicated support DTOs, repository queries, service mapping, and control-plane router/OpenAPI proof. The listing is fenced by portfolio id and exposes derived stale replay truth plus operator-facing lifecycle state per key. | Keep replay support surfaces aligned with durable replay state, not just aggregate counters. If a durable key can hold the system in REPROCESSING, it should be queryable as a first-class support object. | Evidence: [CR-190-REPROCESSING-KEY-SUPPORT-LISTING-REVIEW.md](./CR-190-REPROCESSING-KEY-SUPPORT-LISTING-REVIEW.md); operations DTO/repository/service/router files; query-service unit and control-plane integration/OpenAPI tests. |
| CR-191 | 2026-03-14 | Reprocessing job support listing | Hardened | The support plane exposed durable replay keys but not the durable replay jobs that actually queue and drive reset-watermark work. Operators still had to infer replay queue truth from aggregate counters or direct database inspection. | Added `GET /support/portfolios/{portfolio_id}/reprocessing-jobs`, reusing the shared support job contract so replay jobs speak the same lifecycle language as valuation and aggregation jobs. The repository fences jobs to a portfolio through replay-key ownership and the service exposes retry/stale-processing truth at the row level. | Keep replay support surfaces aligned with both durable replay keys and durable replay jobs. If replay pressure is operator-actionable, the queued work itself should be queryable without leaving the support plane. | Evidence: [CR-191-REPROCESSING-JOB-SUPPORT-LISTING-REVIEW.md](./CR-191-REPROCESSING-JOB-SUPPORT-LISTING-REVIEW.md); operations repository/service/router files; query-service unit and control-plane integration/OpenAPI tests. |
| CR-192 | 2026-03-14 | Lineage key support artifact visibility | Hardened | `GET /lineage/portfolios/{portfolio_id}/keys` only exposed epoch, watermark, and replay status. Operators still had to pivot into per-key lineage or direct database inspection to understand whether the current epoch had recent position history, daily snapshots, or valuation jobs. | Enriched `LineageKeyRecord` with current-epoch artifact truth (`latest_position_history_date`, `latest_daily_snapshot_date`, `latest_valuation_job_date`, `latest_valuation_job_status`) and folded those values into the existing repository listing query with unit, router, and OpenAPI proof. | Keep support-plane listings rich enough that operators do not need to reconstruct current-epoch artifact state from multiple endpoints. If a listing is the first stop for triage, it should surface the adjacent durable artifacts directly. | Evidence: [CR-192-LINEAGE-KEY-SUPPORT-ARTIFACT-VISIBILITY-REVIEW.md](./CR-192-LINEAGE-KEY-SUPPORT-ARTIFACT-VISIBILITY-REVIEW.md); operations DTO/repository/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-193 | 2026-03-14 | Lineage key derived health contract | Hardened | Even after CR-192, operators still had to compare raw artifact dates and valuation status by hand to decide whether a key was healthy, replaying, artifact-lagged, or valuation-blocked. | Added server-owned derived fields (`has_artifact_gap`, `operational_state`) to `LineageKeyRecord` and centralized their computation in `OperationsService`, with unit, router, and OpenAPI proof. | Keep operator-facing lineage health server-owned. If the service already knows how to interpret raw artifact state, expose that truth directly instead of forcing clients to re-derive it. | Evidence: [CR-193-LINEAGE-KEY-DERIVED-HEALTH-CONTRACT-REVIEW.md](./CR-193-LINEAGE-KEY-DERIVED-HEALTH-CONTRACT-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-194 | 2026-03-14 | Lineage detail derived health alignment | Hardened | CR-193 strengthened the lineage-key listing, but the per-key lineage detail still exposed only raw artifact fields. Drilling into the detail view meant losing the same server-owned health semantics the list already provided. | Added `has_artifact_gap` and `operational_state` to `LineageResponse`, reusing the same service-owned derivation already used for lineage-key listings, with unit, router, and OpenAPI proof. | Keep list and detail support surfaces semantically aligned. If the server owns a health interpretation in the listing, the detail view should not regress to raw fields only. | Evidence: [CR-194-LINEAGE-DETAIL-DERIVED-HEALTH-ALIGNMENT-REVIEW.md](./CR-194-LINEAGE-DETAIL-DERIVED-HEALTH-ALIGNMENT-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-195 | 2026-03-14 | Support job durable identity and failure contract | Hardened | Support job listings exposed lifecycle truth such as `is_retrying` and `is_stale_processing`, but they still omitted the stable job row identifier and whether the row was already in a terminal failed state. | Added `job_id` and `is_terminal_failure` to `SupportJobRecord`, wired valuation, aggregation, and reprocessing support listings to populate both fields from durable state, and extended unit, router dependency, and OpenAPI contract tests. | Keep support-plane job listings aligned with the richer analytics export and reconciliation contracts. If operators can act on a durable row, its stable identity and terminal failure truth should be first-class fields. | Evidence: [CR-195-SUPPORT-JOB-DURABLE-IDENTITY-AND-FAILURE-CONTRACT-REVIEW.md](./CR-195-SUPPORT-JOB-DURABLE-IDENTITY-AND-FAILURE-CONTRACT-REVIEW.md); operations DTO/service/repository files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-196 | 2026-03-14 | Support job correlation lineage visibility | Hardened | The durable job tables already persisted `correlation_id`, but the shared support job contract still hid that lineage. Operators could see that a row was stale or failed, yet still had to pivot into logs or direct database inspection to join it back to the originating request or replay chain. | Added `correlation_id` to `SupportJobRecord`, wired valuation, aggregation, and durable replay support listings to surface the stored durable lineage, extended the replay support query to select `ReprocessingJob.correlation_id`, and strengthened unit, router dependency, and OpenAPI tests. | Keep durable lineage visible on operator-facing support objects. If a job row owns correlation lineage in storage, the support plane should expose it directly instead of forcing side-channel joins. | Evidence: [CR-196-SUPPORT-JOB-CORRELATION-LINEAGE-VISIBILITY-REVIEW.md](./CR-196-SUPPORT-JOB-CORRELATION-LINEAGE-VISIBILITY-REVIEW.md); operations DTO/repository/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-197 | 2026-03-14 | Reconciliation run correlation lineage visibility | Hardened | Financial reconciliation runs already persisted durable `correlation_id`, but the support-plane reconciliation listing still hid it. Operators had to bridge from a blocking or failed reconciliation row into logs through side channels instead of using the support API directly. | Added `correlation_id` to `ReconciliationRunRecord`, wired `OperationsService.get_reconciliation_runs(...)` to surface durable reconciliation lineage directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep durable lineage visible on operator-facing control surfaces. If a reconciliation run row owns correlation lineage in storage, the support plane should expose it directly. | Evidence: [CR-197-RECONCILIATION-RUN-CORRELATION-LINEAGE-VISIBILITY-REVIEW.md](./CR-197-RECONCILIATION-RUN-CORRELATION-LINEAGE-VISIBILITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-198 | 2026-03-14 | Lineage valuation job identity visibility | Hardened | The lineage support surface could already tell operators that a key was blocked or lagging because of valuation state, but it still exposed only the latest valuation job date and status. That left the actual durable valuation work item hidden behind a second lookup. | Added `latest_valuation_job_id` and `latest_valuation_job_correlation_id` to both lineage detail and lineage-key listing contracts, extended the lineage repository query to project the latest valuation job identity directly, and strengthened unit, router dependency, and OpenAPI tests. | Keep lineage support surfaces actionable. If a lineage key is blocked by valuation work, expose the concrete durable valuation job directly instead of forcing operators to pivot through a second support listing. | Evidence: [CR-198-LINEAGE-VALUATION-JOB-IDENTITY-VISIBILITY-REVIEW.md](./CR-198-LINEAGE-VALUATION-JOB-IDENTITY-VISIBILITY-REVIEW.md); operations DTO/repository/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-199 | 2026-03-14 | Reconciliation finding blocking truth | Hardened | Reconciliation findings already exposed severity and structured detail, but the support contract still forced clients and operators to infer whether a finding was publication-blocking. That duplicated control policy on the client side. | Added `is_blocking` and `operational_state` to `ReconciliationFindingRecord`, centralized the blocking rule in `OperationsService`, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep control-plane support objects explicit about blocking truth. If a finding can hold back publication or release, the server contract should say so directly instead of expecting clients to infer it from severity. | Evidence: [CR-199-RECONCILIATION-FINDING-BLOCKING-TRUTH-REVIEW.md](./CR-199-RECONCILIATION-FINDING-BLOCKING-TRUTH-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-200 | 2026-03-14 | Portfolio control stage durable identity | Hardened | Portfolio control stage rows were already first-class support objects, but the contract still omitted the durable row identifier. That left blocking support rows without a stable handle back to the exact persisted control-stage row. | Added `stage_id` to `PortfolioControlStageRecord`, wired `OperationsService.get_portfolio_control_stages(...)` to surface the durable row id directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep first-class support objects durably identifiable. If a row is operator-actionable, the support plane should expose the stable durable identifier directly. | Evidence: [CR-200-PORTFOLIO-CONTROL-STAGE-DURABLE-IDENTITY-REVIEW.md](./CR-200-PORTFOLIO-CONTROL-STAGE-DURABLE-IDENTITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-201 | 2026-03-14 | Portfolio control stage lifecycle timing | Hardened | The portfolio control stage listing exposed stage status and latest update time, but it still hid `created_at` and `ready_emitted_at`, which are needed to distinguish stages that never progressed from stages that emitted readiness and later regressed. | Added `created_at` and `ready_emitted_at` to `PortfolioControlStageRecord`, wired the control-stage listing to surface both durable timestamps directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep support-plane control objects explicit about lifecycle progression. If durable timing exists on the row, expose it when it materially improves operator triage. | Evidence: [CR-201-PORTFOLIO-CONTROL-STAGE-LIFECYCLE-TIMING-REVIEW.md](./CR-201-PORTFOLIO-CONTROL-STAGE-LIFECYCLE-TIMING-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-202 | 2026-03-14 | Support job creation timing visibility | Hardened | The shared support job contract exposed `updated_at`, but it still hid when the durable row was first created. That made it harder to distinguish newly created work from older queue items with limited movement. | Added `created_at` to `SupportJobRecord`, wired valuation, aggregation, and durable replay listings to surface the durable creation timestamp, extended the replay query to select `ReprocessingJob.created_at`, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep shared support job objects explicit about both queue age and recent movement. If multiple durable job families reuse one contract, expose the durable source timestamps once rather than inventing endpoint-specific age workarounds. | Evidence: [CR-202-SUPPORT-JOB-CREATION-TIMING-VISIBILITY-REVIEW.md](./CR-202-SUPPORT-JOB-CREATION-TIMING-VISIBILITY-REVIEW.md); operations DTO/repository/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-203 | 2026-03-14 | Analytics export fingerprint visibility | Hardened | Analytics export jobs already persisted `request_fingerprint`, and the runtime used it to decide whether an export should be reused or superseded, but the support-plane listing still hid it. | Added `request_fingerprint` to `AnalyticsExportJobRecord`, wired the analytics export support listing to surface the durable fingerprint directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep operator-facing support rows aligned with the runtime’s actual control handles. If a durable fingerprint drives deduplication or supersession, the support plane should expose it directly. | Evidence: [CR-203-ANALYTICS-EXPORT-FINGERPRINT-VISIBILITY-REVIEW.md](./CR-203-ANALYTICS-EXPORT-FINGERPRINT-VISIBILITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-204 | 2026-03-14 | Reconciliation run request ownership visibility | Hardened | Financial reconciliation runs already persisted `requested_by` and `dedupe_key`, but the support-plane listing still hid both. That removed two concrete control handles from operator view: who requested the run and which durable deduplication scope it belonged to. | Added `requested_by` and `dedupe_key` to `ReconciliationRunRecord`, wired the reconciliation support listing to surface both durable fields directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep durable request-ownership and deduplication context visible on operator-facing control rows. If the runtime already uses these fields to scope or reuse work, the support plane should expose them directly. | Evidence: [CR-204-RECONCILIATION-RUN-REQUEST-OWNERSHIP-VISIBILITY-REVIEW.md](./CR-204-RECONCILIATION-RUN-REQUEST-OWNERSHIP-VISIBILITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-205 | 2026-03-14 | Replay key creation timing visibility | Hardened | Replay key support rows exposed current watermark, status, and latest update time, but still hid when the durable key row was first created. That made it harder to separate recently created replay scope from long-lived replay residue. | Added `created_at` to `ReprocessingKeyRecord`, wired the replay-key support listing to surface the durable creation timestamp directly, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep replay support rows explicit about both age and movement. If durable replay scope is operator-actionable, creation timing should be visible without leaving the support plane. | Evidence: [CR-205-REPLAY-KEY-CREATION-TIMING-VISIBILITY-REVIEW.md](./CR-205-REPLAY-KEY-CREATION-TIMING-VISIBILITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-206 | 2026-03-14 | Support job mapping convergence | Hardened | OperationsService rebuilt the shared SupportJobRecord separately for valuation jobs, aggregation jobs, and durable replay jobs. As the shared support contract grew richer, that duplication increased the risk of silent drift between job families. | Extracted _build_support_job_record(...) in OperationsService, switched all three durable job listings to reuse it, and re-ran the support-plane unit, router dependency, and OpenAPI contract pack. | Keep shared support contracts implemented once. If multiple durable job families reuse the same operator-facing row shape, centralize the mapping before the contract drifts. | Evidence: [CR-206-SUPPORT-JOB-MAPPING-CONVERGENCE-REVIEW.md](./CR-206-SUPPORT-JOB-MAPPING-CONVERGENCE-REVIEW.md); operations service; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-207 | 2026-03-14 | Reprocessing overview and SLO health | Hardened | Replay health in the support overview and calculator SLO surfaces exposed only a raw active-key count. That hid stale replay residue and oldest replay backlog scope even though valuation and aggregation already exposed those summary signals. | Added ReprocessingHealthSummary in OperationsRepository, surfaced stale replay counts and oldest replay watermark date in both SupportOverviewResponse and ReprocessingSloBucket, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep replay health first-class on summary surfaces. If valuation and aggregation expose stale and backlog truth, replay should not lag behind as a raw count-only surface. | Evidence: [CR-207-REPROCESSING-OVERVIEW-AND-SLO-HEALTH-REVIEW.md](./CR-207-REPROCESSING-OVERVIEW-AND-SLO-HEALTH-REVIEW.md); operations DTO/repository/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-208 | 2026-03-14 | Support overview control stage identity | Hardened | The support overview exposed whether the latest financial reconciliation controls were blocking, but it hid which durable control-stage row was driving that summary and when that row last changed. | Added controls_stage_id and controls_last_updated_at to SupportOverviewResponse, wired both fields from the latest control-stage row in OperationsService.get_support_overview(...), and strengthened unit, router dependency, and OpenAPI contract tests. | Keep overview summaries anchored to durable owners. If a summary decision depends on one control-stage row, the overview should expose that row identity and freshness without forcing a second support-plane call. | Evidence: [CR-208-SUPPORT-OVERVIEW-CONTROL-STAGE-IDENTITY-REVIEW.md](./CR-208-SUPPORT-OVERVIEW-CONTROL-STAGE-IDENTITY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-209 | 2026-03-14 | Support overview stale threshold alignment | Hardened | The support overview silently hardcoded a 15-minute stale threshold while calculator-slos already allowed custom stale thresholds. That created policy drift between two operator-facing support surfaces describing the same runtime state. | Added stale_threshold_minutes query support and response visibility to the support overview route, wired the selected threshold through OperationsService.get_support_overview(...), and strengthened unit, router dependency, and OpenAPI contract tests. | Keep stale-policy interpretation aligned across support surfaces. If two endpoints classify stale runtime state, they should expose and honor the same threshold contract. | Evidence: [CR-209-SUPPORT-OVERVIEW-STALE-THRESHOLD-ALIGNMENT-REVIEW.md](./CR-209-SUPPORT-OVERVIEW-STALE-THRESHOLD-ALIGNMENT-REVIEW.md); operations router/DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-210 | 2026-03-14 | Calculator SLO failed window alignment | Hardened | calculator-slos exposed ailed_jobs_last_24h, but the runtime used a hardcoded failed-job recency window and the request contract did not let operators choose or verify that policy. | Added ailed_window_hours query and response support to calculator SLO, renamed the bucket field to ailed_jobs_within_window, wired the selected window through OperationsService.get_calculator_slos(...), and strengthened unit, router dependency, and OpenAPI contract tests. | Keep failed-job recency policy explicit and auditable. If the runtime counts recent failures using a configurable window, the API contract should not hardcode one policy value in the field name. | Evidence: [CR-210-CALCULATOR-SLO-FAILED-WINDOW-ALIGNMENT-REVIEW.md](./CR-210-CALCULATOR-SLO-FAILED-WINDOW-ALIGNMENT-REVIEW.md); operations router/DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-211 | 2026-03-14 | Support overview failed window alignment | Hardened | The support overview consumed recent failed-job counts from repository summaries but still hardcoded a 24-hour failed window, hid that policy from operators, and discarded the recent failed-job counts from the response contract. | Added ailed_window_hours query and response support to the support overview route, surfaced recent failed-job counts for valuation/aggregation/analytics export, wired the selected failed window through OperationsService.get_support_overview(...), and strengthened unit, router dependency, and OpenAPI contract tests. | Keep overview failure pressure explicit and auditable. If the repository already computes recent failed-job counts, the support overview should expose both those counts and the policy window that produced them. | Evidence: [CR-211-SUPPORT-OVERVIEW-FAILED-WINDOW-ALIGNMENT-REVIEW.md](./CR-211-SUPPORT-OVERVIEW-FAILED-WINDOW-ALIGNMENT-REVIEW.md); operations router/DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-212 | 2026-03-14 | Support policy convergence | Hardened | Support-policy defaults and descriptions for stale thresholds and failed windows were duplicated across router query params, service method defaults, and DTO field descriptions. That made support-plane policy drift more likely whenever defaults or wording changed. | Added shared support_policy.py constants for support stale/failure policy, switched router query params, service defaults, and DTO descriptions to reuse them, and re-ran support-plane unit, router dependency, and OpenAPI contract tests. | Keep support-plane policy defined once. If stale and failure windows drive router behavior, service behavior, and response semantics, they should not be duplicated across layers. | Evidence: [CR-212-SUPPORT-POLICY-CONVERGENCE-REVIEW.md](./CR-212-SUPPORT-POLICY-CONVERGENCE-REVIEW.md); support policy/DTO/service/router files; query-service unit tests; query-control-plane integration/OpenAPI tests. |

| CR-213 | 2026-03-14 | Support snapshot timestamp consistency | Hardened | The support overview did not expose when the snapshot was generated, and overview/SLO age calculations used multiple implicit datetime.now(...) calls per response. That made support snapshots less auditable and introduced avoidable internal time skew. | Added generated_at_utc to SupportOverviewResponse, captured one reference timestamp per overview response and reused it for backlog-age calculations, reused one captured timestamp in calculator SLO generation, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep operator-facing summary snapshots timestamped and internally consistent. If a response computes age-based signals, those calculations should derive from one captured clock value per response. | Evidence: [CR-213-SUPPORT-SNAPSHOT-TIMESTAMP-CONSISTENCY-REVIEW.md](./CR-213-SUPPORT-SNAPSHOT-TIMESTAMP-CONSISTENCY-REVIEW.md); operations DTO/service files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-214 | 2026-03-14 | Stale-aware support listing policy alignment | Hardened | Stale-aware support listings still silently hardcoded the default stale threshold while support overview and calculator SLO already allowed operators to choose the threshold. The list responses also hid which threshold and snapshot time produced the stale-state fields. | Added `stale_threshold_minutes` query support to stale-aware support listings, added `stale_threshold_minutes` and `generated_at_utc` to stale-aware list responses, wired one captured timestamp through stale/operational-state calculations in the service layer, restored the stable external `status` query contract for valuation and aggregation listings, and strengthened unit, router dependency, and OpenAPI contract tests. | Keep stale-policy semantics aligned across all operator-facing surfaces. If a list exposes `is_stale_*` fields, it should also expose the threshold and snapshot time that produced them. | Evidence: [CR-214-STALE-AWARE-SUPPORT-LISTING-POLICY-ALIGNMENT-REVIEW.md](./CR-214-STALE-AWARE-SUPPORT-LISTING-POLICY-ALIGNMENT-REVIEW.md); operations DTO/service/router files; query-service unit tests; query-control-plane integration/OpenAPI tests. |
| CR-215 | 2026-03-14 | Support summary repository clock consistency | Hardened | The support overview and calculator SLO responses captured one `generated_at_utc` timestamp in the service layer, but the repository health summary queries still computed stale and failed cutoffs from their own internal `datetime.now(...)` calls. That meant one operator snapshot could still be assembled from multiple drifting time windows underneath it. | Added `reference_now` to replay, valuation, aggregation, and analytics-export health summary repository methods; wired the service-layer `generated_at_utc` through to those queries; strengthened service tests to prove the shared clock is forwarded; and added repository tests that prove the generated SQL uses the exact expected stale and failed cutoffs. | Keep operator-facing support snapshots temporally consistent end to end. If the service captures one response timestamp, repository summary queries should not silently compute their own clocks underneath it. | Evidence: [CR-215-SUPPORT-SUMMARY-REPOSITORY-CLOCK-CONSISTENCY-REVIEW.md](./CR-215-SUPPORT-SUMMARY-REPOSITORY-CLOCK-CONSISTENCY-REVIEW.md); operations repository/service files; query-service repository and service tests; query-control-plane integration/OpenAPI tests. |
| CR-216 | 2026-03-14 | Support listing stale threshold semantics | Hardened | Support listings exposed `stale_threshold_minutes` in their request and response contract, but the shared stale classifier in OperationsService still used a hidden hardcoded 15-minute threshold. That meant callers could request a different threshold, see it echoed back, and still get stale-state classification from the old default. | Removed the hidden class-level 15-minute stale threshold from the stale-classification path, added `stale_threshold_minutes` to the shared support stale helpers and support job record builder, wired caller-selected stale thresholds through valuation, aggregation, analytics export, replay key, and replay job support listing classification, and added targeted unit tests that prove custom thresholds change stale-state and operational-state results. | Keep operator-facing stale policy truthful. If a support listing exposes `stale_threshold_minutes`, that exact value must drive the stale-state and operational-state classification in the response. | Evidence: [CR-216-SUPPORT-LISTING-STALE-THRESHOLD-SEMANTICS-REVIEW.md](./CR-216-SUPPORT-LISTING-STALE-THRESHOLD-SEMANTICS-REVIEW.md); operations service; query-service unit/repository tests; query-control-plane integration/OpenAPI tests. |
| CR-217 | 2026-03-14 | Support listing ordering clock consistency | Hardened | After CR-216, support listing stale-state labels honored caller-selected stale thresholds in the service layer, but the repository list queries still computed stale ordering priority from their own internal `datetime.now(...)` clock. That meant one response could still prioritize rows using one hidden cutoff and label them using another. | Added `reference_now` to valuation, aggregation, analytics export, replay key, and replay job repository listing queries; wired each support listing to pass its captured `generated_at_utc` into the repository ordering path; strengthened service tests to prove the shared clock is forwarded; and added repository SQL tests that prove the ordering cutoff uses the expected reference timestamp. | Keep support listing ordering and labels derived from one clock. If a response prioritizes stale rows, the ordering cutoff must match the timestamp used to classify `is_stale_*` and operational state. | Evidence: [CR-217-SUPPORT-LISTING-ORDERING-CLOCK-CONSISTENCY-REVIEW.md](./CR-217-SUPPORT-LISTING-ORDERING-CLOCK-CONSISTENCY-REVIEW.md); operations repository/service files; query-service repository and service tests; query-control-plane integration/OpenAPI tests. |
| CR-218 | 2026-03-14 | Date-aware replay job support scope | Hardened | The support-plane replay job listing and count scoped jobs to a portfolio by security membership only. That allowed a portfolio to see durable RESET_WATERMARKS jobs for a security even when the portfolio was not actually impacted on the job's earliest_impacted_date. | Added a date-aware portfolio-scope fence for replay job count and listing queries, scoped each job by the latest current-epoch PositionHistory row on or before its impacted date, required the latest holding quantity to be positive, and strengthened repository tests to prove the impacted-date and current-epoch holding fence in generated SQL. | Keep replay support scope aligned with replay execution scope. If the replay worker fans out work only to portfolios holding a security on the impacted date, the support plane should not surface that work to unaffected portfolios. | Evidence: [CR-218-DATE-AWARE-REPLAY-JOB-SUPPORT-SCOPE-REVIEW.md](./CR-218-DATE-AWARE-REPLAY-JOB-SUPPORT-SCOPE-REVIEW.md); operations repository; query-service repository tests; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-219 | 2026-03-14 | Replay job support type fence | Hardened | The replay job support DTO and operator semantics only model `RESET_WATERMARKS`, but the repository query did not explicitly constrain `job_type`. That meant any future replay job type with a compatible payload shape could leak into an endpoint whose schema and operational meaning are narrower. | Added explicit `ReprocessingJob.job_type == 'RESET_WATERMARKS'` fencing to replay job support count and listing queries and strengthened repository tests to prove the generated SQL carries the durable job-type fence. | Keep support-plane scope explicit. If an endpoint models one durable replay job family, fence it at the query layer instead of relying on informal assumptions about current data. | Evidence: [CR-219-REPLAY-JOB-SUPPORT-TYPE-FENCE-REVIEW.md](./CR-219-REPLAY-JOB-SUPPORT-TYPE-FENCE-REVIEW.md); operations repository; query-service repository tests; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-220 | 2026-03-14 | Lineage key operational priority ordering | Hardened | The lineage key listing already exposed server-owned health semantics such as `REPLAYING`, `VALUATION_BLOCKED`, `ARTIFACT_GAP`, and `HEALTHY`, but the repository query still ordered keys alphabetically by `security_id`. That let unhealthy keys disappear behind healthy ones on the first page. | Added severity-first ordering to lineage key repository queries, prioritizing replaying keys, then blocked artifact-gap keys, then other artifact-gap keys, while keeping deterministic tie-breaking with latest position-history date and `security_id`; strengthened repository tests to prove the generated SQL carries the severity ordering. | Keep support-plane pagination aligned with support-plane semantics. If a listing exposes server-owned operational states, the first page should prioritize the unhealthy rows operators actually need to triage. | Evidence: [CR-220-LINEAGE-KEY-OPERATIONAL-PRIORITY-ORDERING-REVIEW.md](./CR-220-LINEAGE-KEY-OPERATIONAL-PRIORITY-ORDERING-REVIEW.md); operations repository; query-service repository tests; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-221 | 2026-03-14 | Support summary oldest work identity | Hardened | The support overview and calculator SLO endpoints exposed backlog counts and oldest dates, but still hid the concrete durable work items behind those backlogs. Operators had to make a second pivot to identify the oldest replay key, valuation job, aggregation job, or analytics export job responsible for the summary signal. | Extended replay, valuation, aggregation, and analytics export health summaries to carry the oldest actionable durable identity, surfaced those fields through `SupportOverviewResponse`, `CalculatorSloBucket`, and `ReprocessingSloBucket`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new contract end to end. | Keep summary surfaces actionable. If an overview or SLO endpoint reports backlog pressure, it should also identify the owning durable work item directly instead of forcing a second discovery call. | Evidence: [CR-221-SUPPORT-SUMMARY-OLDEST-WORK-IDENTITY-REVIEW.md](./CR-221-SUPPORT-SUMMARY-OLDEST-WORK-IDENTITY-REVIEW.md); operations DTO/repository/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-222 | 2026-03-14 | Support job id drill-through filters | Hardened | The support plane already exposed durable `job_id` fields on support rows and CR-221 added oldest-work identity to summary endpoints, but the listing endpoints still could not filter by those identities. Operators could see the durable row id and still be forced to page or add secondary filters instead of drilling straight into the owning work item. | Added optional `job_id` filters to valuation, aggregation, analytics export, and replay job listing routes; wired the new filters through `OperationsService`; added repository fencing on durable row id in both count and listing queries; and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through contract. | Keep support identities actionable. If the API exposes durable row ids in summaries and list rows, the same endpoints should accept those ids as direct filters so support flows remain self-contained. | Evidence: [CR-222-SUPPORT-JOB-ID-DRILL-THROUGH-FILTERS-REVIEW.md](./CR-222-SUPPORT-JOB-ID-DRILL-THROUGH-FILTERS-REVIEW.md); operations router/service/repository files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-223 | 2026-03-14 | Support overview control lifecycle context | Hardened | The support overview could already tell operators whether the latest financial reconciliation controls were blocking, but it still hid the control-stage lifecycle context already present on the durable row. Operators had to pivot into the control-stage listing to see what event last touched the row, when it was created, or whether readiness had ever been emitted. | Added `controls_last_source_event_type`, `controls_created_at`, and `controls_ready_emitted_at` to `SupportOverviewResponse`, wired those fields from the latest durable control-stage row in `OperationsService.get_support_overview(...)`, and strengthened service, router dependency, and OpenAPI tests to prove the new overview contract. | Keep the support overview self-explanatory for the latest blocking control row. If durable control lifecycle fields already exist on the row, the overview should expose the ones operators need most before forcing a second pivot. | Evidence: [CR-223-SUPPORT-OVERVIEW-CONTROL-LIFECYCLE-CONTEXT-REVIEW.md](./CR-223-SUPPORT-OVERVIEW-CONTROL-LIFECYCLE-CONTEXT-REVIEW.md); operations DTO/service files; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-224 | 2026-03-14 | Support overview control failure reason | Hardened | The support overview already exposed whether the latest financial reconciliation control row was blocking and, after CR-223, exposed its lifecycle context. But when that latest row was in `FAILED`, the overview still hid the durable `failure_reason`, forcing an unnecessary second pivot into the control-stage listing. | Added `controls_failure_reason` to `SupportOverviewResponse`, wired the field from the latest durable financial reconciliation control-stage row in `OperationsService.get_support_overview(...)`, and strengthened service, router dependency, and OpenAPI tests to prove the failed-row and no-row cases. | Keep the latest blocking control summary causally useful. If the latest durable control row is failed, the overview should expose its stored failure reason directly instead of hiding the first diagnostic clue behind another call. | Evidence: [CR-224-SUPPORT-OVERVIEW-CONTROL-FAILURE-REASON-REVIEW.md](./CR-224-SUPPORT-OVERVIEW-CONTROL-FAILURE-REASON-REVIEW.md); operations DTO/service files; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-225 | 2026-03-14 | Support overview latest reconciliation run link | Hardened | The support overview now exposed the latest financial reconciliation control-stage row well, but it still did not identify the reconciliation execution record for that same portfolio-day and epoch. Operators could understand the latest control row yet still lack the direct run handle needed to pivot into reconciliation-run lineage and findings for the same control event. | Added repository lookup for the latest reconciliation run scoped to `portfolio_id`, `business_date`, and `epoch`; added `controls_latest_reconciliation_run_id`, `controls_latest_reconciliation_type`, `controls_latest_reconciliation_status`, and `controls_latest_reconciliation_correlation_id` to `SupportOverviewResponse`; wired the overview service to populate those fields; and strengthened repository, service, router dependency, and OpenAPI tests. | Keep the latest control summary causally linked to the latest reconciliation execution record for the same portfolio-day and epoch. If operators need to pivot from blocking controls into run detail, the overview should hand them the right durable run handle directly. | Evidence: [CR-225-SUPPORT-OVERVIEW-LATEST-RECONCILIATION-RUN-LINK-REVIEW.md](./CR-225-SUPPORT-OVERVIEW-LATEST-RECONCILIATION-RUN-LINK-REVIEW.md); operations repository/DTO/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-226 | 2026-03-14 | Support overview oldest replay key state | Hardened | The support overview and calculator SLO already exposed the oldest replay security id, but that still hid the actual key state behind the replay backlog summary. Operators still had to pivot into the replay-key listing to see the oldest key's epoch and freshness. | Extended replay health summary projection with `oldest_reprocessing_epoch` and `oldest_reprocessing_updated_at`, surfaced both fields through `SupportOverviewResponse` and `ReprocessingSloBucket`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new contract. | Keep replay backlog summaries closer to the concrete oldest key state. If the overview exposes the oldest replay key, it should also expose the next most important state needed for triage before forcing a listing pivot. | Evidence: [CR-226-SUPPORT-OVERVIEW-OLDEST-REPLAY-KEY-STATE-REVIEW.md](./CR-226-SUPPORT-OVERVIEW-OLDEST-REPLAY-KEY-STATE-REVIEW.md); operations repository/DTO/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-227 | 2026-03-14 | Support summary job correlation drill-through | Hardened | The support overview and calculator SLO already exposed the oldest open valuation and aggregation job ids, but they still hid the durable correlation handles operators actually use to move into logs and trace downstream runtime activity. That left the summary surfaces only half-drillable. | Extended `JobHealthSummary` with oldest-job correlation and valuation security identity, widened valuation and aggregation oldest-job projections to fetch those fields, surfaced them through `SupportOverviewResponse` and `CalculatorSloBucket`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through contract end to end. | Keep backlog summaries directly actionable. If a summary surfaces the owning durable job id, it should also surface the durable correlation handle needed for log drill-through so operators do not need a second discovery step. | Evidence: [CR-227-SUPPORT-SUMMARY-JOB-CORRELATION-DRILL-THROUGH-REVIEW.md](./CR-227-SUPPORT-SUMMARY-JOB-CORRELATION-DRILL-THROUGH-REVIEW.md); operations repository/DTO/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-228 | 2026-03-14 | Support overview reconciliation ownership context | Hardened | The support overview already linked the latest reconciliation run for the same portfolio-day and epoch as the latest financial reconciliation control stage, but it still hid the run's durable request owner, dedupe scope, and failure reason. Operators still had to pivot into the reconciliation-run listing to understand who requested the linked run and why it failed. | Added `controls_latest_reconciliation_requested_by`, `controls_latest_reconciliation_dedupe_key`, and `controls_latest_reconciliation_failure_reason` to `SupportOverviewResponse`; wired those fields from the existing latest reconciliation run row in `OperationsService.get_support_overview(...)`; and strengthened service, router dependency, and OpenAPI tests to prove the widened overview contract. | Keep overview-to-detail causality strong. If the support overview already links the latest reconciliation run, it should also expose the ownership and failure context operators need most before forcing a second support call. | Evidence: [CR-228-SUPPORT-OVERVIEW-RECONCILIATION-OWNERSHIP-CONTEXT-REVIEW.md](./CR-228-SUPPORT-OVERVIEW-RECONCILIATION-OWNERSHIP-CONTEXT-REVIEW.md); operations DTO/service files; query-service service tests; query-control-plane integration/OpenAPI tests. |
| CR-229 | 2026-03-14 | Support overview reconciliation finding pressure | Hardened | The support overview could already identify the latest reconciliation run linked to the latest financial reconciliation control stage, along with its ownership and failure context. But it still could not tell operators whether that linked run had actually produced blocking findings. That forced another pivot into reconciliation findings just to assess whether the linked run had concrete blocking evidence. | Added repository-level `ReconciliationFindingSummary`, implemented `get_reconciliation_finding_summary(run_id)`, surfaced `controls_latest_reconciliation_total_findings` and `controls_latest_reconciliation_blocking_findings` through `SupportOverviewResponse`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the widened overview contract. | Keep control summaries causally useful. If the support overview already links the latest reconciliation run, it should also expose whether that run produced blocking findings before forcing a separate findings call. | Evidence: [CR-229-SUPPORT-OVERVIEW-RECONCILIATION-FINDING-PRESSURE-REVIEW.md](./CR-229-SUPPORT-OVERVIEW-RECONCILIATION-FINDING-PRESSURE-REVIEW.md); operations repository/DTO/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-230 | 2026-03-14 | Support overview top blocking finding handle | Hardened | After CR-229, the support overview could tell operators whether the linked reconciliation run had blocking findings, but it still hid the concrete blocking finding handle behind those counts. Operators still had to pivot into the findings endpoint to learn which finding, which type, and which security or transaction was implicated first. | Extended `ReconciliationFindingSummary` with top blocking finding identity and scope, widened `get_reconciliation_finding_summary(run_id)` to project the most recent blocking finding, surfaced `controls_latest_blocking_finding_id`, `controls_latest_blocking_finding_type`, `controls_latest_blocking_finding_security_id`, and `controls_latest_blocking_finding_transaction_id` through `SupportOverviewResponse`, and strengthened repository, service, router dependency, and OpenAPI tests end to end. | Keep overview surfaces one-step actionable. If the support overview already reports blocking finding pressure, it should also expose the first blocking evidence operators need before deciding whether a detailed findings drill-down is necessary. | Evidence: [CR-230-SUPPORT-OVERVIEW-TOP-BLOCKING-FINDING-HANDLE-REVIEW.md](./CR-230-SUPPORT-OVERVIEW-TOP-BLOCKING-FINDING-HANDLE-REVIEW.md); operations repository/DTO/service files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-231 | 2026-03-14 | Control stage drill-through filter | Hardened | The support overview already exposed `controls_stage_id`, but the control-stage listing endpoint could not filter by that durable row id. That left the overview with a named control-stage owner but no direct drill-through path to the exact row. | Added optional `stage_id` filtering to control-stage count and listing queries, wired the filter through `OperationsService.get_portfolio_control_stages(...)` and `GET /support/portfolios/{portfolio_id}/control-stages`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through filter. | Keep summary identifiers actionable. If the support overview surfaces a durable control-stage row id, the listing endpoint should accept that id directly so overview-to-listing drill-through stays self-contained. | Evidence: [CR-231-CONTROL-STAGE-DRILL-THROUGH-FILTER-REVIEW.md](./CR-231-CONTROL-STAGE-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-232 | 2026-03-14 | Reconciliation run drill-through filter | Hardened | The support overview already exposed `controls_latest_reconciliation_run_id`, but the reconciliation-run listing endpoint could not filter by that durable run id. That left the overview with a named reconciliation-run owner but no direct list-level drill-through path to the exact run. | Added optional `run_id` filtering to reconciliation-run count and listing queries, wired the filter through `OperationsService.get_reconciliation_runs(...)` and `GET /support/portfolios/{portfolio_id}/reconciliation-runs`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through filter. | Keep summary identifiers actionable. If the support overview surfaces a durable reconciliation run id, the listing endpoint should accept that id directly so overview-to-listing drill-through stays self-contained. | Evidence: [CR-232-RECONCILIATION-RUN-DRILL-THROUGH-FILTER-REVIEW.md](./CR-232-RECONCILIATION-RUN-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-233 | 2026-03-14 | Reconciliation finding drill-through filter | Hardened | The support overview already exposed `controls_latest_blocking_finding_id`, but the reconciliation findings endpoint could not filter by that durable finding id. That left the overview with a named blocking finding handle but no direct drill-through path to the exact finding row. | Added optional `finding_id` filtering to reconciliation-finding count and listing queries, wired the filter through `OperationsService.get_reconciliation_findings(...)` and `GET /support/portfolios/{portfolio_id}/reconciliation-runs/{run_id}/findings`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through filter. | Keep summary identifiers actionable. If the support overview surfaces a durable blocking finding id, the findings endpoint should accept that id directly so overview-to-finding drill-through stays self-contained. | Evidence: [CR-233-RECONCILIATION-FINDING-DRILL-THROUGH-FILTER-REVIEW.md](./CR-233-RECONCILIATION-FINDING-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |
| CR-234 | 2026-03-14 | Analytics export fingerprint drill-through filter | Hardened | The support overview already exposed `oldest_pending_analytics_export_request_fingerprint`, but the analytics export support listing could not filter by that durable request fingerprint. That left the overview with a useful deduplication handle but no direct list-level drill-through path to the matching durable export rows. | Added optional `request_fingerprint` filtering to analytics export count and listing queries, wired the filter through `OperationsService.get_analytics_export_jobs(...)` and `GET /support/portfolios/{portfolio_id}/analytics-export-jobs`, and strengthened repository, service, router dependency, and OpenAPI tests to prove the new drill-through filter. | Keep summary deduplication handles actionable. If the support overview surfaces a durable analytics export request fingerprint, the export listing endpoint should accept that fingerprint directly so overview-to-listing drill-through stays self-contained. | Evidence: [CR-234-ANALYTICS-EXPORT-FINGERPRINT-DRILL-THROUGH-FILTER-REVIEW.md](./CR-234-ANALYTICS-EXPORT-FINGERPRINT-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-235 | 2026-03-14 | Support correlation drill-through filters | Hardened | The support plane already exposed durable correlation_id values on valuation, aggregation, replay, and reconciliation support surfaces, but the downstream listing endpoints could not filter by those same handles. Operators could see the durable lineage identifier they needed for logs and trace correlation yet still could not query the exact durable row by that handle. | Added optional correlation_id filters to valuation, aggregation, replay, and reconciliation support count/list queries; wired them through OperationsService and the query-control-plane routers; updated route descriptions; and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep summary and row-level lineage handles directly actionable. If the support plane exposes a durable correlation_id, the downstream listing endpoint should accept it as a first-class filter so overview-to-list drill-through remains self-contained. | Evidence: [CR-235-SUPPORT-CORRELATION-DRILL-THROUGH-FILTERS-REVIEW.md](./CR-235-SUPPORT-CORRELATION-DRILL-THROUGH-FILTERS-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-236 | 2026-03-14 | Valuation security drill-through filter | Hardened | The support overview already exposed oldest_pending_valuation_security_id, but the valuation job support listing could not filter by that durable security handle. That left the overview with a useful backlog owner identifier but no direct list-level drill-through path to the matching valuation jobs. | Added optional security_id filtering to valuation count/list queries, wired it through OperationsService.get_valuation_jobs(...) and GET /support/portfolios/{portfolio_id}/valuation-jobs, updated route descriptions, and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep backlog owner identifiers actionable. If the support overview surfaces a durable valuation security handle, the valuation listing endpoint should accept that handle directly so overview-to-list drill-through stays self-contained. | Evidence: [CR-236-VALUATION-SECURITY-DRILL-THROUGH-FILTER-REVIEW.md](./CR-236-VALUATION-SECURITY-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-237 | 2026-03-14 | Support job business-date drill-through filters | Hardened | The support overview already exposed oldest_pending_valuation_date and oldest_pending_aggregation_date, and the downstream support job rows already exposed usiness_date, but the valuation and aggregation listings still could not filter by that durable date handle. That left oldest-backlog dates as dead-end summary signals. | Added optional usiness_date filters to valuation and aggregation count/list queries, wired them through OperationsService and the query-control-plane routers, normalized the API-level field name across both job families, updated route descriptions, and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep oldest-backlog date signals actionable. If support summaries surface a durable oldest job date and job rows already expose usiness_date, the downstream listing endpoints should accept that date directly so summary-to-list drill-through stays self-contained. | Evidence: [CR-237-SUPPORT-JOB-BUSINESS-DATE-DRILL-THROUGH-FILTERS-REVIEW.md](./CR-237-SUPPORT-JOB-BUSINESS-DATE-DRILL-THROUGH-FILTERS-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-238 | 2026-03-14 | Replay key watermark-date drill-through filter | Hardened | The support overview already exposed oldest_reprocessing_watermark_date, and replay-key rows already exposed watermark_date, but the replay-key listing could not filter by that durable handle. That left oldest replay backlog dates as dead-end summary signals. | Added optional watermark_date filtering to replay-key count/list queries, wired it through OperationsService.get_reprocessing_keys(...) and GET /support/portfolios/{portfolio_id}/reprocessing-keys, updated route descriptions, and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep replay backlog dates actionable. If the support overview surfaces a durable oldest replay watermark date and replay-key rows already expose watermark_date, the listing endpoint should accept that handle directly so summary-to-list drill-through stays self-contained. | Evidence: [CR-238-REPLAY-KEY-WATERMARK-DATE-DRILL-THROUGH-FILTER-REVIEW.md](./CR-238-REPLAY-KEY-WATERMARK-DATE-DRILL-THROUGH-FILTER-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-239 | 2026-03-14 | Reconciliation finding scope drill-through filters | Hardened | The support overview already exposed controls_latest_blocking_finding_security_id and controls_latest_blocking_finding_transaction_id, but the reconciliation findings endpoint still only accepted inding_id. That left the top blocking finding's causal scope as a dead-end support signal. | Added optional security_id and 	ransaction_id filters to reconciliation-finding count/list queries, wired them through OperationsService.get_reconciliation_findings(...) and GET /support/portfolios/{portfolio_id}/reconciliation-runs/{run_id}/findings, and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep top blocking finding scope actionable. If the support overview surfaces the implicated security or transaction for the latest blocking finding, the findings endpoint should accept those handles directly so summary-to-findings drill-through stays self-contained. | Evidence: [CR-239-RECONCILIATION-FINDING-SCOPE-DRILL-THROUGH-FILTERS-REVIEW.md](./CR-239-RECONCILIATION-FINDING-SCOPE-DRILL-THROUGH-FILTERS-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-240 | 2026-03-14 | Reconciliation run ownership drill-through filters | Hardened | The support overview already exposed controls_latest_reconciliation_requested_by and controls_latest_reconciliation_dedupe_key, but the reconciliation-run listing could not filter by those same durable ownership handles. That left requester and dedupe scope as dead-end support signals. | Added optional equested_by and dedupe_key filters to reconciliation-run count/list queries, wired them through OperationsService.get_reconciliation_runs(...) and GET /support/portfolios/{portfolio_id}/reconciliation-runs, updated route descriptions, and strengthened repository SQL tests, service forwarding tests, router dependency tests, and OpenAPI assertions. | Keep reconciliation ownership context actionable. If the support overview surfaces the latest run's requester and dedupe scope, the reconciliation-run listing should accept those handles directly so summary-to-list drill-through stays self-contained. | Evidence: [CR-240-RECONCILIATION-RUN-OWNERSHIP-DRILL-THROUGH-FILTERS-REVIEW.md](./CR-240-RECONCILIATION-RUN-OWNERSHIP-DRILL-THROUGH-FILTERS-REVIEW.md); operations repository/service/router files; query-service repository/service tests; query-control-plane integration/OpenAPI tests. |

| CR-241 | 2026-03-14 | Stale reset overwrite race across replay, valuation, and aggregation workers | Hardened | Replay, valuation, and aggregation stale-reset paths all used a two-step `select stale rows -> update by id` flow without re-checking current ownership at update time. A worker could complete or re-claim a row between those steps, and the stale sweep could overwrite live durable state back to `PENDING` or `FAILED`. | Added update-time guards requiring `status = 'PROCESSING'` and `updated_at < stale_cutoff` in all three stale-reset repositories, added unit tests proving the guarded update statements, and added DB-backed characterization tests proving a concurrent completion is not overwritten by the stale reset sweep. | Apply the same update-time ownership fence to any future stale-recovery or retry-reset path that first selects rows and then updates them later. Do not rely on the stale scan alone under contention. | Evidence: [CR-241-STALE-RESET-OVERWRITE-RACE-REVIEW.md](./CR-241-STALE-RESET-OVERWRITE-RACE-REVIEW.md); replay/valuation/aggregation repository changes; targeted unit suite => `29 passed`; DB-backed overwrite race proofs for replay, valuation, and aggregation; OpenAPI gate and touched-surface `ruff check` passed. |

| CR-242 | 2026-03-14 | Terminal ownership fence for worker side effects | Hardened | Even after CR-241 fenced stale reset sweeps, late workers could still finish after losing ownership of a `PROCESSING` row. That left a second race: timeout recovery could reset or fail a job, and a late worker could still emit terminal side effects or overwrite terminal status afterward. | Changed replay and valuation terminal status updates to require current `PROCESSING` ownership, changed aggregation terminal updates to do the same, made those update paths return whether ownership was actually claimed, and gated valuation, aggregation, and replay-worker completion side effects behind that claim. Added unit proofs for replay terminal fencing, valuation late-owner suppression, aggregation late-owner suppression, and replay-worker lost-ownership behavior. | Add DB-backed ownership-loss proofs for the most critical worker families where feasible. Keep applying the same ownership-first pattern anywhere a worker can publish durable side effects after a timeout or retry takeover. | Evidence: [CR-242-TERMINAL-OWNERSHIP-FENCE-REVIEW.md](./CR-242-TERMINAL-OWNERSHIP-FENCE-REVIEW.md); worker/consumer/repository updates; targeted unit suite => `35 passed`; touched-surface `ruff check` passed. |

| CR-243 | 2026-03-14 | Concurrent reconciliation run dedupe proof | Hardened | `ReconciliationRepository.create_run(...)` already used the right optimistic pre-read plus `IntegrityError` fallback pattern for duplicate `dedupe_key` requests, but that behavior had not been proved under real concurrent sessions. The control path was relying on structure rather than contention evidence. | Added a repository unit test proving the `IntegrityError` fallback returns the existing run, and added a DB-backed two-session integration test that forces both callers past the pre-read before insert and proves one durable run is created, one caller receives `created=True`, one receives `created=False`, and both receive the same `run_id`. | Apply the same DB-backed contention-proof pattern to other durable dedupe/upsert paths, especially instrument reprocessing triggers and valuation job upserts. | Evidence: [CR-243-CONCURRENT-RECONCILIATION-DEDUPE-PROOF.md](./CR-243-CONCURRENT-RECONCILIATION-DEDUPE-PROOF.md); reconciliation repository unit test; DB-backed concurrent dedupe integration proof; targeted pytest => `3 passed`; touched-surface `ruff check` passed. |

| CR-244 | 2026-03-14 | Concurrent instrument trigger coalescing proof | Hardened | Instrument reprocessing trigger upsert already had the correct-looking one-row-per-security UPSERT shape with earliest-date coalescing and correlation backfill, but that behavior had only been proved sequentially. The replay chain was still missing contention evidence for duplicate concurrent price-trigger arrivals. | Added a DB-backed two-session integration test that forces concurrent duplicate arrivals for the same security before either session executes the UPSERT, and proved the durable state still converges to one row, the earliest impacted date, and the correlation id associated with that earliest date. | Apply the same contention-proof pattern to valuation job upsert under duplicate scheduler pressure. | Evidence: [CR-244-CONCURRENT-INSTRUMENT-TRIGGER-COALESCING-PROOF.md](./CR-244-CONCURRENT-INSTRUMENT-TRIGGER-COALESCING-PROOF.md); instrument trigger repository integration test; targeted pytest => `6 passed`; touched-surface `ruff check` passed. |
| CR-245 | 2026-03-14 | Concurrent valuation job upsert proof | Hardened | `ValuationJobRepository.upsert_job(...)` already used a correct-looking UPSERT shape for one durable row per logical valuation job, but that behavior had only been proved sequentially. Duplicate scheduler polls could still race on the same `(portfolio_id, security_id, valuation_date, epoch)` scope without DB-backed contention evidence. | Added a DB-backed two-session integration test that forces both callers past `get_latest_epoch_for_scope(...)` before either session stages the UPSERT, and proved the durable state still converges to one row with the requested epoch, `PENDING` status, and the same durable correlation lineage. | Continue the same contention-proof pattern on remaining claim/update paths, then move to support/control summary consistency under changing state. | Evidence: [CR-245-CONCURRENT-VALUATION-JOB-UPSERT-PROOF.md](./CR-245-CONCURRENT-VALUATION-JOB-UPSERT-PROOF.md); valuation repository integration test; targeted pytest => `13 passed`; touched-surface `ruff check` passed. |
| CR-246 | 2026-03-14 | Support overview control snapshot causality | Hardened | `get_support_overview(...)` fetched the latest financial reconciliation control stage and then separately asked for the "latest" reconciliation run for the same portfolio-day and epoch. Under changing state, one response could therefore mix a control row from one durable moment with a newer reconciliation run that did not yet exist when that control row was written. | Added an `as_of` fence to `get_latest_reconciliation_run_for_portfolio_day(...)`, filtered linked reconciliation runs by `started_at <= latest_control_stage.updated_at`, and updated the support overview to pass the control-stage snapshot time into the reconciliation lookup. Added repository and service proofs for the tightened causal contract. | Keep applying snapshot fences anywhere a support summary combines a latest parent row with a follow-up latest child lookup. | Evidence: [CR-246-SUPPORT-OVERVIEW-CONTROL-SNAPSHOT-CAUSALITY-REVIEW.md](./CR-246-SUPPORT-OVERVIEW-CONTROL-SNAPSHOT-CAUSALITY-REVIEW.md); operations repository/service updates; query-service repository and service tests; touched-surface `ruff check` passed. |
| CR-247 | 2026-03-14 | Support overview finding snapshot causality | Hardened | After CR-246 fenced the linked reconciliation run to the latest control-stage snapshot, the support overview still summarized all findings for that run with no time fence. Under changing state, one response could still mix a control snapshot from one durable moment with reconciliation findings from a later one. | Added an `as_of` fence to `get_reconciliation_finding_summary(...)`, filtered both finding aggregates and top-blocking-finding selection by `created_at <= latest_control_stage.updated_at`, and updated the support overview to pass the control-stage snapshot time into the reconciliation finding lookup. Added repository and service proofs for the tightened causal contract. | Continue checking remaining support summaries for parent/child lookups that still lack a shared causal fence. | Evidence: [CR-247-SUPPORT-OVERVIEW-FINDING-SNAPSHOT-CAUSALITY-REVIEW.md](./CR-247-SUPPORT-OVERVIEW-FINDING-SNAPSHOT-CAUSALITY-REVIEW.md); operations repository/service updates; query-service repository and service tests; touched-surface `ruff check` passed. |
| CR-248 | 2026-03-14 | Support overview run update causality | Hardened | After CR-246, the support overview fenced linked reconciliation runs only by `started_at <= latest_control_stage.updated_at`. That still allowed a previously started run to leak later mutable row updates such as status or failure reason into the same response through `updated_at`. | Tightened `get_latest_reconciliation_run_for_portfolio_day(...)` so linked reconciliation runs must satisfy both `started_at <= as_of` and `updated_at <= as_of`, and strengthened repository proof for the stricter mutable-row snapshot fence. | Keep applying mutable-row snapshot fences where overview responses surface status-bearing rows that can change after creation. | Evidence: [CR-248-SUPPORT-OVERVIEW-RUN-UPDATE-CAUSALITY-REVIEW.md](./CR-248-SUPPORT-OVERVIEW-RUN-UPDATE-CAUSALITY-REVIEW.md); operations repository update; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-249 | 2026-03-14 | Support overview control anchor snapshot | Hardened | `get_support_overview(...)` captured `generated_at_utc` first, but the latest financial reconciliation control-stage lookup was still free to return a row updated after that timestamp. That meant the overview could report a snapshot time that predated the control row anchoring the rest of the control summary. | Added an `as_of` fence to `get_latest_financial_reconciliation_control_stage(...)`, filtered the latest control-stage lookup by `updated_at <= generated_at_utc`, updated the support overview to pass its snapshot timestamp into the control-stage query, and added repository and service proofs for the tightened anchor contract. | Keep anchoring support summaries to the same snapshot timestamp they report outward. | Evidence: [CR-249-SUPPORT-OVERVIEW-CONTROL-ANCHOR-SNAPSHOT-REVIEW.md](./CR-249-SUPPORT-OVERVIEW-CONTROL-ANCHOR-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-250 | 2026-03-14 | Support overview data snapshot consistency | Hardened | Even after the control chain was fenced, `latest_transaction_date` and `latest_position_snapshot_date` were still queried without any snapshot bound. Those fields could therefore reflect rows created after the overview's own `generated_at_utc`, undermining the response's snapshot semantics. | Added optional `as_of` fences to `get_latest_transaction_date(...)` and `get_latest_snapshot_date_for_current_epoch(...)`, filtered both lookups by `created_at <= generated_at_utc`, updated the support overview to pass its snapshot timestamp into both queries, and added repository and service proofs for the tightened snapshot contract. | Keep checking remaining support and lineage responses for “latest” fields that still are not fenced to the response timestamp they report. | Evidence: [CR-250-SUPPORT-OVERVIEW-DATA-SNAPSHOT-REVIEW.md](./CR-250-SUPPORT-OVERVIEW-DATA-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-251 | 2026-03-14 | Support business-date snapshot consistency | Hardened | `get_support_overview(...)` and `get_calculator_slos(...)` still used `get_latest_business_date()` with no snapshot fence. If the business calendar advanced after the response timestamp was captured, backlog-age calculations could jump forward against a date that did not exist at the reported snapshot moment. | Added an optional `as_of` fence to `get_latest_business_date(...)`, filtered the business-date lookup by `created_at <= generated_at_utc`, updated both support overview and calculator SLO responses to pass their generated snapshot time into the business-date lookup, and added repository and service proofs for the tightened anchor contract. | Keep checking remaining support and lineage responses for anchor values that still are not fenced to the response snapshot timestamp. | Evidence: [CR-251-SUPPORT-BUSINESS-DATE-SNAPSHOT-REVIEW.md](./CR-251-SUPPORT-BUSINESS-DATE-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-252 | 2026-03-14 | Support current epoch snapshot consistency | Hardened | `get_support_overview(...)` still used `get_current_portfolio_epoch(...)` without any snapshot fence. If a replay epoch bump landed after the response timestamp was captured, the overview could report a later epoch than the one that actually existed at its stated `generated_at_utc`. | Added an optional `as_of` fence to `get_current_portfolio_epoch(...)`, filtered the epoch lookup by `PositionState.updated_at <= generated_at_utc`, updated the support overview to pass its generated snapshot time into the current-epoch lookup, and added repository and service proofs for the tightened epoch anchor contract. | Keep checking remaining support and lineage anchors for values that are still read without a snapshot fence. | Evidence: [CR-252-SUPPORT-CURRENT-EPOCH-SNAPSHOT-REVIEW.md](./CR-252-SUPPORT-CURRENT-EPOCH-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-253 | 2026-03-14 | Support mismatch snapshot consistency | Hardened | `get_support_overview(...)` still computed `position_snapshot_history_mismatch_count` from unfenced latest-history and latest-snapshot subqueries. That meant the mismatch count could pick up a later `PositionHistory` or `DailyPositionSnapshot` row than the response timestamp it claimed to represent. | Added an optional `as_of` fence to `get_position_snapshot_history_mismatch_count(...)`, filtered both latest-history and latest-snapshot subqueries by `created_at <= generated_at_utc`, updated the support overview to pass its response snapshot timestamp into the mismatch-count query, and added repository and service proofs for the tightened derived-snapshot contract. | Keep checking remaining derived support and lineage aggregates for subqueries that still are not fenced to the response snapshot timestamp. | Evidence: [CR-253-SUPPORT-MISMATCH-SNAPSHOT-REVIEW.md](./CR-253-SUPPORT-MISMATCH-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-254 | 2026-03-14 | Lineage snapshot contract | Hardened | `get_lineage(...)` still built its response from multiple “latest” reads with no explicit response snapshot. That meant the lineage detail could mix a `PositionState` row, position history, daily snapshot, and valuation job that did not all exist at one coherent durable moment. The response also did not expose a snapshot timestamp. | Added `generated_at_utc` to `LineageResponse` and `LineageKeyListResponse`, added optional `as_of` fences to the lineage repository reads, updated `OperationsService.get_lineage(...)` to capture one snapshot timestamp and pass it through all lineage reads, and updated unit, router, and OpenAPI proofs for the strengthened contract. | Consider extending the same deep snapshot fencing to lineage-key repository data if later evidence shows list responses need it too. | Evidence: [CR-254-LINEAGE-SNAPSHOT-CONTRACT-REVIEW.md](./CR-254-LINEAGE-SNAPSHOT-CONTRACT-REVIEW.md); operations DTO/repository/service updates; query-service repository/service tests; query-control-plane integration/OpenAPI tests; touched-surface `ruff check` passed. |
| CR-255 | 2026-03-14 | Lineage key list snapshot consistency | Hardened | After CR-254, `LineageKeyListResponse` exposed `generated_at_utc`, but the underlying count and key queries were still completely live. The API could therefore claim a snapshot timestamp while returning key rows, counts, or projected latest artifacts that changed after that moment. | Added optional `as_of` fences to `get_lineage_keys_count(...)` and `get_lineage_keys(...)`, filtered `PositionState.updated_at` plus projected history/snapshot/valuation rows by the response snapshot time, updated `OperationsService.get_lineage_keys(...)` to pass `generated_at_utc` into both list queries, and added repository and service proofs for the tightened list-level snapshot contract. | Let concrete evidence, not symmetry for its own sake, determine whether any remaining surfaces still need deeper snapshot fencing. | Evidence: [CR-255-LINEAGE-KEY-LIST-SNAPSHOT-REVIEW.md](./CR-255-LINEAGE-KEY-LIST-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-256 | 2026-03-14 | Replay key list snapshot consistency | Hardened | `ReprocessingKeyListResponse` already exposed `generated_at_utc`, but the underlying count and row queries were still live. The API could therefore claim a snapshot timestamp while returning replay keys that had been updated after that moment. | Added optional `as_of` fences to `get_reprocessing_keys_count(...)` and `get_reprocessing_keys(...)`, filtered replay-key counts and rows by `PositionState.updated_at <= generated_at_utc`, updated `OperationsService.get_reprocessing_keys(...)` to pass the response snapshot time into both queries, and added repository and service proofs for the tightened replay-key snapshot contract. | Apply the same standard to any remaining support list response that already exposes `generated_at_utc` but still uses live count or row queries underneath. | Evidence: [CR-256-REPLAY-KEY-LIST-SNAPSHOT-REVIEW.md](./CR-256-REPLAY-KEY-LIST-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-257 | 2026-03-14 | Replay job list snapshot consistency | Hardened | `SupportJobListResponse` for replay jobs already exposed `generated_at_utc`, but the underlying replay-job count and row queries were still live. The API could therefore claim a snapshot timestamp while returning replay jobs updated after that moment. | Added optional `as_of` fences to `get_reprocessing_jobs_count(...)` and `get_reprocessing_jobs(...)`, filtered replay-job counts and rows by `ReprocessingJob.updated_at <= generated_at_utc`, updated `OperationsService.get_reprocessing_jobs(...)` to pass the response snapshot time into both queries, and added repository and service proofs for the tightened replay-job snapshot contract. | Apply the same standard to any remaining support list response that already exposes `generated_at_utc` but still uses live count or row queries underneath. | Evidence: [CR-257-REPLAY-JOB-LIST-SNAPSHOT-REVIEW.md](./CR-257-REPLAY-JOB-LIST-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; touched-surface `ruff check` passed. |
| CR-258 | 2026-03-14 | Control-stage list snapshot consistency | Hardened | The portfolio control-stage listing was still a live count plus live rows with no explicit response snapshot. That left one of the main support-plane drill-through surfaces out of step with replay, lineage, and support-summary responses that already behaved like coherent operator snapshots. | Added `generated_at_utc` to `PortfolioControlStageListResponse`, added optional `as_of` fences to `get_portfolio_control_stages_count(...)` and `get_portfolio_control_stages(...)`, filtered both queries by `PipelineStageState.updated_at <= generated_at_utc`, updated `OperationsService.get_portfolio_control_stages(...)` to capture one snapshot timestamp and pass it through both queries, and strengthened repository, service, router dependency, and OpenAPI tests. | Keep list-level snapshot timestamps honest. If a support listing returns `generated_at_utc`, its count and row queries need to honor that same durable cutoff rather than staying live underneath. | Evidence: [CR-258-CONTROL-STAGE-LIST-SNAPSHOT-REVIEW.md](./CR-258-CONTROL-STAGE-LIST-SNAPSHOT-REVIEW.md); operations DTO/repository/service updates; query-service repository/service tests; query-control-plane integration/OpenAPI tests; targeted pytest => `153 passed`; touched-surface `ruff check` passed. |
| CR-259 | 2026-03-14 | Reconciliation list snapshot consistency | Hardened | Reconciliation-run and reconciliation-finding support listings were still live count plus live row queries even though they sit on the same operator-facing control surface as newer snapshot-based support responses. That left reconciliation support responses out of step with the surrounding `generated_at_utc` support-plane contract during active control churn. | Added `generated_at_utc` to `ReconciliationRunListResponse` and `ReconciliationFindingListResponse`, added optional `as_of` fences to reconciliation run and finding count/list queries, filtered runs by `FinancialReconciliationRun.updated_at <= generated_at_utc` and findings by `FinancialReconciliationFinding.created_at <= generated_at_utc`, updated `OperationsService` to capture one snapshot timestamp per reconciliation list response and pass it through both count and row queries, and strengthened repository, service, router dependency, and OpenAPI tests. | Keep reconciliation support snapshots honest. If a reconciliation list response exposes or implies a snapshot contract, both count and row queries need to honor that same durable cutoff. | Evidence: [CR-259-RECONCILIATION-LIST-SNAPSHOT-REVIEW.md](./CR-259-RECONCILIATION-LIST-SNAPSHOT-REVIEW.md); operations DTO/repository/service updates; query-service repository/service tests; query-control-plane integration/OpenAPI tests; targeted pytest => `153 passed`; touched-surface `ruff check` passed. |
| CR-260 | 2026-03-14 | Support job list snapshot consistency | Hardened | Valuation, aggregation, and analytics export support listings already exposed `generated_at_utc`, but their count and row queries were still live. That left the main support job families weaker than the replay, reconciliation, control-stage, and lineage surfaces that had already been hardened to honor one operator snapshot. | Added optional `as_of` fences to valuation, aggregation, and analytics export count/list queries, filtered all three job families by `updated_at <= generated_at_utc`, updated `OperationsService` to pass each listing's snapshot timestamp into both count and row queries, and strengthened repository SQL tests plus service forwarding tests. | Keep support job snapshots honest. If a support list returns `generated_at_utc`, both its total and its rows need to honor that same durable cutoff rather than staying live underneath. | Evidence: [CR-260-SUPPORT-JOB-LIST-SNAPSHOT-REVIEW.md](./CR-260-SUPPORT-JOB-LIST-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; targeted pytest => `104 passed`; touched-surface `ruff check` passed. |
| CR-261 | 2026-03-14 | Remove dead reprocessing count repository API | Hardened | `OperationsRepository.get_active_reprocessing_keys_count(...)` was no longer referenced by any service, router, or production support flow. It survived only through its own unit test after replay health and replay-key listing contracts had already replaced that older access pattern. | Removed the dead repository method and its orphaned unit test, then re-ran the repository unit pack to prove no live path depended on it. | Keep removing dead repository APIs once the support plane converges on newer hardened paths. Dead helpers add noise, imply non-existent support contracts, and slow future review work. | Evidence: [CR-261-REMOVE-DEAD-REPROCESSING-COUNT-REPOSITORY-API.md](./CR-261-REMOVE-DEAD-REPROCESSING-COUNT-REPOSITORY-API.md); operations repository cleanup; repository unit pack => `60 passed`; touched-surface `ruff check` passed. |
| CR-262 | 2026-03-14 | Booked-date snapshot causality | Hardened | The support overview’s `latest_booked_transaction_date` and `latest_booked_position_snapshot_date` still relied on helpers fenced only by business date. That allowed later-ingested rows with older business dates to leak into an otherwise timestamped support snapshot. | Widened `get_latest_transaction_date_as_of(...)` and `get_latest_snapshot_date_for_current_epoch_as_of(...)` with optional `snapshot_as_of`, fenced both helpers by durable creation time as well as business date, updated `OperationsService.get_support_overview(...)` to pass `generated_at_utc` into both booked-date lookups, and strengthened repository/service tests to prove the dual-fence contract. | Keep “as of date” helpers honest when they participate in timestamped support snapshots. Business-date scoping alone is not enough once the API also claims a durable response timestamp. | Evidence: [CR-262-BOOKED-DATE-SNAPSHOT-CAUSALITY-REVIEW.md](./CR-262-BOOKED-DATE-SNAPSHOT-CAUSALITY-REVIEW.md); operations repository/service updates; query-service repository/service tests; targeted pytest => `103 passed`; touched-surface `ruff check` passed. |
| CR-263 | 2026-03-14 | Current-epoch snapshot date join fence | Hardened | The snapshot-date helpers fenced `DailyPositionSnapshot.created_at`, but not the `PositionState` join that defines the “current epoch.” A later epoch bump could therefore change which state row counted as current even when the snapshot timestamp and snapshot row were already fenced. | Tightened `get_latest_snapshot_date_for_current_epoch(...)` and `get_latest_snapshot_date_for_current_epoch_as_of(...)` so both also require `PositionState.updated_at <= snapshot cutoff`, and strengthened repository SQL tests to prove the join-side fence is present. | Keep checking “current” joins, not just fact rows. Fencing only the snapshot table is not enough when the state table itself can move after the response snapshot. | Evidence: [CR-263-CURRENT-EPOCH-SNAPSHOT-DATE-JOIN-FENCE-REVIEW.md](./CR-263-CURRENT-EPOCH-SNAPSHOT-DATE-JOIN-FENCE-REVIEW.md); operations repository update; repository unit pack => `60 passed`; touched-surface `ruff check` passed. |
| CR-264 | 2026-03-14 | Lineage valuation status snapshot | Hardened | The lineage detail and lineage key list selected their latest valuation job using `created_at <= as_of` only. A valuation job created before the response snapshot could still leak later status updates into lineage responses after that snapshot moment. | Tightened `get_latest_valuation_job(...)` and all lineage-key latest-valuation-job projected subqueries so they now require both `created_at <= as_of` and `updated_at <= as_of`, and strengthened repository SQL tests to prove the stronger fence in both detail and list query shapes. | Keep checking status-bearing “latest row” helpers. Creation-time fences alone are not enough once the row’s status or failure state can mutate after insertion. | Evidence: [CR-264-LINEAGE-VALUATION-STATUS-SNAPSHOT-REVIEW.md](./CR-264-LINEAGE-VALUATION-STATUS-SNAPSHOT-REVIEW.md); operations repository update; repository unit pack => `60 passed`; touched-surface `ruff check` passed. |
| CR-265 | 2026-03-14 | Support health summary snapshot fence | Hardened | The support overview and calculator SLO captured `generated_at_utc`, but their replay, valuation, aggregation, and analytics export health summaries still evaluated live rows with no `as_of` fence. That allowed summary counts and oldest-item identities to drift underneath a timestamped response. | Added optional `as_of` to all four health summary repository methods, fenced both aggregate and oldest-item selector queries by `updated_at <= as_of`, updated support overview and calculator SLO to pass `generated_at_utc` into all four health summaries, and strengthened repository/service tests to prove the shared cutoff. | Keep timestamped summary responses honest. If a support overview or SLO response advertises one snapshot time, the underlying health summaries and oldest-item selectors need to use that same durable cutoff. | Evidence: [CR-265-SUPPORT-HEALTH-SUMMARY-SNAPSHOT-FENCE-REVIEW.md](./CR-265-SUPPORT-HEALTH-SUMMARY-SNAPSHOT-FENCE-REVIEW.md); operations repository/service updates; query-service repository/service tests; targeted pytest => `103 passed`; touched-surface `ruff check` passed. |
| CR-266 | 2026-03-14 | Reconciliation finding parent snapshot fence | Hardened | The reconciliation findings endpoint already returned a timestamped snapshot and fenced its count and finding queries, but it still validated the parent reconciliation run with a live lookup first. That allowed one response to mix a live parent-run ownership check with an earlier findings snapshot during active control churn. | Added optional `as_of` to `get_reconciliation_run(...)`, fenced the parent-run lookup by `FinancialReconciliationRun.updated_at <= as_of`, updated `OperationsService.get_reconciliation_findings(...)` to pass `generated_at_utc` into the parent-run ownership check, and strengthened repository/service tests to prove the shared cutoff. | Keep parent-row ownership checks aligned with child snapshot responses. If a detail endpoint returns a timestamped child snapshot, any prerequisite parent lookup should honor that same durable cutoff. | Evidence: [CR-266-RECONCILIATION-FINDING-PARENT-SNAPSHOT-REVIEW.md](./CR-266-RECONCILIATION-FINDING-PARENT-SNAPSHOT-REVIEW.md); operations repository/service updates; query-service repository/service tests; targeted pytest and touched-surface `ruff check` passed. |
| CR-267 | 2026-03-14 | Concurrent reprocessing claim proof | Hardened | The replay worker claim path already used an atomic `UPDATE ... FOR UPDATE SKIP LOCKED` shape, but there was still no DB-backed contention proof showing that two workers racing to claim the same pending reprocessing job could not both take it. | Added a two-session DB-backed contention test for `find_and_claim_jobs("RESET_WATERMARKS", batch_size=1)` and proved that two concurrent workers converge to exactly one claimed job, one durable row in `PROCESSING`, and a single attempt increment. | Keep converting claim-path confidence into contention evidence. If a live queue relies on `SKIP LOCKED` for safety, prove the no-double-claim invariant under real concurrent sessions. | Evidence: [CR-267-CONCURRENT-REPROCESSING-CLAIM-PROOF.md](./CR-267-CONCURRENT-REPROCESSING-CLAIM-PROOF.md); reprocessing job repository integration tests; targeted pytest => `8 passed`; touched-surface `ruff check` passed. |
| CR-268 | 2026-03-14 | Concurrent valuation claim proof | Hardened | The valuation worker claim path also used an atomic `UPDATE ... FOR UPDATE SKIP LOCKED` shape, but there was still no DB-backed contention proof showing that two workers racing to claim the same pending valuation job could not both take it. | Added a two-session DB-backed contention test for `find_and_claim_eligible_jobs(batch_size=1)` and proved that two concurrent workers converge to exactly one claimed job, one durable row in `PROCESSING`, and a single attempt increment. | Keep converting worker-queue confidence into contention evidence. If a live queue relies on `SKIP LOCKED` for safety, prove the no-double-claim invariant under real concurrent sessions. | Evidence: [CR-268-CONCURRENT-VALUATION-CLAIM-PROOF.md](./CR-268-CONCURRENT-VALUATION-CLAIM-PROOF.md); valuation repository integration tests; targeted pytest => `12 passed`; touched-surface `ruff check` passed. |
| CR-269 | 2026-03-14 | Concurrent aggregation claim proof | Hardened | The aggregation worker claim path used a `SELECT ... FOR UPDATE SKIP LOCKED` plus update shape, but there was still no DB-backed contention proof showing that two workers racing to claim the same pending aggregation job could not both take it. | Added a two-session DB-backed contention test for `find_and_claim_eligible_jobs(batch_size=1)` and proved that two concurrent workers converge to exactly one claimed job, one durable row in `PROCESSING`, and a single attempt increment. | Keep converting worker-queue confidence into contention evidence. If a live queue relies on `SKIP LOCKED` for safety, prove the no-double-claim invariant under real concurrent sessions. | Evidence: [CR-269-CONCURRENT-AGGREGATION-CLAIM-PROOF.md](./CR-269-CONCURRENT-AGGREGATION-CLAIM-PROOF.md); aggregation repository integration tests; targeted pytest => `4 passed`; touched-surface `ruff check` passed. |
| CR-270 | 2026-03-14 | Concurrent trigger claim proof | Hardened | Instrument reprocessing trigger coalescing was already DB-backed, but the trigger claim path itself still lacked contention evidence showing that two workers racing to claim the same trigger row could not both take it. | Added a two-session DB-backed contention test for `claim_instrument_reprocessing_triggers(batch_size=1)` and proved that two concurrent workers converge to exactly one claimed trigger while the durable trigger row is deleted exactly once. | Keep reviewing the full claim lifecycle, not just enqueue/coalescing paths. If a trigger queue relies on `SKIP LOCKED` for safety, prove the no-double-claim invariant under real concurrent sessions. | Evidence: [CR-270-CONCURRENT-TRIGGER-CLAIM-PROOF.md](./CR-270-CONCURRENT-TRIGGER-CLAIM-PROOF.md); valuation-orchestrator integration tests; targeted pytest => `7 passed`; touched-surface `ruff check` passed. |
| CR-271 | 2026-03-14 | Valuation consumer ownership-loss proof | Hardened | The valuation consumer already gated completion side effects behind durable terminal ownership, but there was still no DB-backed proof that a late ownership loss would suppress snapshot persistence, outbox emission, and idempotency writes. | Added an integration proof that uses a second database session to complete the same valuation job immediately before the real repository `update_job_status(...)` runs, and proved that the consumer emits no snapshot, outbox, or idempotency side effects after losing ownership. | Keep raising terminal ownership guarantees from unit confidence to DB-backed proof wherever a worker consumer can still publish durable side effects after status transitions. | Evidence: [CR-271-VALUATION-CONSUMER-OWNERSHIP-LOSS-PROOF.md](./CR-271-VALUATION-CONSUMER-OWNERSHIP-LOSS-PROOF.md); valuation consumer integration tests; targeted pytest => `2 passed`; touched-surface `ruff check` passed. |
